# Journal du projet

> Extrait de CLAUDE.md (refacto 25/07). Historique des merges et des décisions datées.

## 🎯 Prochaines actions immédiates

_1er août 2026 — **[FS-2] cloche de notifications MERGÉE** (branche `feature/fs2-notifications`, au nom de **wacista**, aucune migration). Campagne **25 scénarios / 393 checks / exit 0**, console 0. 🔑 **Un bug dormant découvert par le codeur** : il y a **17 types de notifications, pas 16** — `match_cancelled_member_left` manquait au miroir Zod du front depuis BX-LEAVE, ces notifications étaient **rejetées en silence** et n'arrivaient jamais en direct. La review a sorti **deux liens pouvant rendre 403** (le droit détenu à l'envoi peut être perdu au clic) et surtout **deux chemins où la pastille se trompait définitivement** — une notification arrivée pendant un rechargement du compteur était comptée puis effacée, et jamais recomptée. Au passage : `Escape` ne rendait le focus nulle part, et **rien ne signalait une notification sous 1024 px**. Le seed social a été refait avant le ticket : ses notifications pointent maintenant sur de **vraies cibles** — en semer avec de faux identifiants aurait planté un piège à 404. Détail → `docs/frontend.md`._

_31 juillet 2026, soir — **[FS-4] liste des conversations et fenêtres multiples MERGÉE** (branche `feature/fs4-conversations-windows`, au nom de **wacista**, aucune migration). Campagne **24 scénarios / 385 checks / exit 0**, console 0. Le défaut serveur du format de date, documenté au moment d'écrire `seed:social`, a été **corrigé avant de lancer le codeur** — et le type qui l'avait rendu invisible (`created_at` déclaré `Date` alors que le pilote rend une `string`, masqué par un double cast) est corrigé aussi : c'est lui qui avait permis au bug d'exister. La review a sorti **un clic mort atteignable par un simple `Ctrl +`** (une conversation ouverte mais masquée par la largeur) et le retour, par une autre porte, du défaut « le brouillon meurt » déjà corrigé en FS-3 — brouillons désormais hissés dans le panneau. 🔑 **Leçon de méthode, la troisième en trois tickets et la plus importante** : mon check du clic mort était **faux vert**, il reproduisait une éviction au lieu d'un changement de largeur. Trouvé en **réintroduisant le défaut dans le code** pour le regarder rougir — la seule méthode qui marche. 4ᵉ faux vert débusqué ainsi sur ce projet. Détail → `docs/frontend.md`._

_31 juillet 2026, soir — **[FS-3] conversation DM MERGÉE** (branche `feature/fs3-chat-core`, au nom de **wacista**, aucune migration). Campagne **23 scénarios / 377 checks / exit 0**, console 0. Deux corrections serveur faites **avant** de lancer le codeur (champs `Message` requis, départage du tri par `id`). La review a validé la corrélation envoi/accusé — qui tient, y compris sur « deux personnes écrivent le même mot au même instant » — et sorti 2 défauts sérieux : **un rechargement raté en arrière-plan effaçait toute la conversation** (TanStack Query garde `data` quand un *refetch* échoue, `isError` était testé avant) et **l'historique était illisible au clavier**. Correctif structurel adopté : **les trois panneaux du rail restent montés**, sinon le brouillon de chat était perdu à chaque changement d'onglet. 🔑 **Leçon de méthode, la deuxième du genre en deux tickets** : mon check anti-doublon était **vert par construction** (il rouvrait la conversation en croyant recharger, alors que rien ne se recharge quand le panneau reste monté) — réécrit pour passer par coupure → reconnexion, seul moment où un message existe dans les deux sources. Détail → `docs/frontend.md`._

_31 juillet 2026, soir — **[FS-1] onglet Amis MERGÉ** (branche `feature/fs1-friends-tab`, au nom de **wacista**, aucune migration). Premier ticket du rail délégué à un agent codeur puis relu par un agent relecteur. Campagne **22 scénarios / 368 checks / exit 0**, console 0. 🔑 **Le blocage d'API a été trouvé AVANT de lancer le codeur** : `GET /friends` ne rendait pas l'id de la relation, donc retirer un ami était impossible — `friendshipId` ajouté au contrat, types régénérés. La review a sorti 4 défauts réels (région d'annonce dupliquée sur toutes les pages · message de panne affiché sur un chargement sain · focus garé sur un titre invisible · menu sans tabindex glissant) et 2 blocages **imputables à moi** (les tests de FS-0 cassés par la disparition du placeholder, et l'absence de scénario FS-1 — que j'avais interdit au codeur d'écrire). Deux leçons de méthode, toutes deux consignées : **un check dont la source disparaît doit changer de source, pas de libellé** (S8 serait devenu vert par construction), et **le budget de requêtes d'une page doit exclure celles du rail** plutôt que d'être remonté à chaque carte. Détail → `docs/frontend.md`._

_31 juillet 2026, soir — **[FS-0] REBASÉ sur le master du soir et REVALIDÉ, prêt à merger** (branche `feature/fs0-social-foundation`, commit unique au nom de **wacista**, aucune migration). Walid travaille avec nous sur place : **tout le rail social est committé, poussé et mergé à son identité** (`wacista <wacista@student.42.fr>`). Décision prise ici : **on garde son travail, on ne repart pas de zéro** — la fondation temps réel est le morceau le plus risqué du rail et elle a déjà passé deux reviews ; ce qui restait était de la finition, pas de la conception. Le rebase a coûté **4 conflits de doc** (`CLAUDE.md`, `docs/frontend.md`, le README de l'audit, `teams-matchmaking.mjs`) et **3 corrections de harnais, zéro correction applicative** : un lien de page qui concurrençait le rail depuis [F-HOME], un `import()` dans `page.evaluate` qui rendait une **seconde instance vierge** du store dès que le HMR de Vite était passé par là (rouge intermittent sur du code sain — le piège le plus coûteux), et un `header:visible` devenu ambigu depuis que `/home` a un hero. Détail et remèdes → `frontend/tests/console-audit/README.md`. ⚠️ **Deux rouges n'étaient pas imputables au ticket** : `dispute` et `match-detail` sortaient `0/0` parce que les comptes de fixture avaient perdu leur mot de passe **et** que les matchs de démo avaient expiré ; réparé par un `seed:dev` lancé **après avoir mesuré** qu'il ne détruisait rien. Validation finale : `tsc -b` + lint + build verts, campagne **21 scénarios / 359 checks / exit 0**, console **0 partout**._

_**Découpage du rail social arrêté le 31/07** : 6 cartes, `FS-0 → FS-1 → FS-3 → FS-4 → FS-2 → FS-5`. Un seul ajustement par rapport au découpage de Walid — « cliquer sur un ami ouvre sa conversation » **passe de FS-4 à FS-3**, sinon FS-3 livrait un composant qu'aucun clic n'atteignait, donc ni démontrable ni auditable avant la fin de FS-4. Aucun ticket backend : B-SOC couvre déjà tout. Carte parapluie → https://trello.com/c/NBGs3xNc._

_**Nouveau : `npm run seed:social`** (`backend/src/scripts/seed-social.ts`) — recette du rail en une commande. Pose sur `alice@dev.local` 4 amis, 2 demandes reçues, 1 envoyée, 1 blocage, 21 messages sur 3 conversations et 10 notifications dont 5 non lues ; `bob@dev.local` en seconde fenêtre pour la présence. **Idempotent**, et il **ne purge que sa propre production** : amitiés/messages/blocages **entre comptes de fixture uniquement** (un compte réel ou d'audit n'est jamais touché) et, pour les notifications — que `seed-dev` et l'app produisent aussi — **seulement celles qu'il a marquées** `seedSocial: true`. Il n'ouvre **aucun nouveau mot de passe** : il réutilise les 3 comptes connectables de `seed-dev`, la règle « les 8 autres joueurs n'ont pas de mot de passe » reste intacte. Il **exige** que `seed:dev` ait tourné (il relie des comptes, il n'en crée aucun)._

_🚨 **Défaut serveur trouvé en écrivant ce seed, non corrigé, bloquant pour [FS-4]** : `GET /messages/conversations` rend `lastMessage.createdAt` au format `2026-07-31 18:35:02.376+00` — **pas de l'ISO 8601**, alors que `openapi.yaml` déclare `format: date-time` et que toutes les autres routes rendent de l'ISO. Cause : seule route du domaine social en **SQL brut**, la valeur remonte telle quelle du pilote `pg` au lieu d'être une `Date`. Correctif d'une ligne, **à faire au début de FS-4**. Détail → `docs/backend.md` et la carte https://trello.com/c/4xEPN5nx. 🔑 **Leçon générale : toute route en `db.execute(sql\`…\`)` perd la conversion de types de l'ORM — vérifier au `curl`, jamais supposer.**_

_31 juillet 2026 — **[FS-0] corrigé après deux reviews de David** (branche `feature/fs0-social-foundation`, aucune migration). Les trois bloquants sont soldés : les onglets métier sont scopés à `<main>`, le rail ne concurrence plus la région live de la page, et une rotation de l'access token ne recycle plus la WebSocket — le nouveau JWT est gardé pour la prochaine vraie reconnexion. Après la seconde passe, la branche a été rebasée sur `master` `2c76794` en conservant B-MM, F-MM, F-HIST et FX-AUDIT ; les 11 sélecteurs de confort ont aussi été scopés, le `aria-controls` mobile est conditionnel et les attentes S4/S5 rendent un rouge imputable plutôt qu'un `exit 2`. Build + lint verts ; `fs0-social` **9/9**, `matchmaking` **31/31**, `teams-manage` **35/35**, campagne complète **18 scénarios / 294 checks / exit 0**, console imputable 0. L'onglet reste **Messages**, le rail desktop reste fixe par décision d'équipe et la migration vers `<dialog>` natif est reportée._

---

**Infrastructure — I2 et I3 mergés sur `master`** : images Compose figées et lancement autonome avec `docker compose up -d --build`. Prévenir l'équipe que le premier démarrage après mise à jour peut lancer un `npm ci` plus long pour resynchroniser les anciens volumes `node_modules` ; les démarrages suivants redeviennent rapides.

**Infrastructure — alignement Node 24 mergé sur `master`** (merge `cc9141c`, commit `21b6a19`) : `.nvmrc` racine + `engines.node` identique dans les manifests frontend/backend. Les Dockerfiles étaient déjà sur `node:24-alpine`. Les lockfiles ne reflètent que cette métadonnée racine, sans mise à jour de dépendance directe ou transitive. Validation finale avec Node `v24.18.0` / npm `11.16.0` : backend `tsc --noEmit` + Vitest **21/21**, frontend build + lint **0 erreur, 0 warning**.

**Backend — B5d et B6 mergés sur `master`** (B6 : merge `f9128bf`, review Walid approuvée, 246 ✅ / 0 ❌ ; 46 avec les jobs). Disponibilité par fenêtre (B5d) et soumission de résultats + ELO + jobs 24 h (B6) en place. La table `rankings` **se remplit désormais** dès qu'un match est `completed` → le leaderboard renvoie de vrais classements. Aussi mergé : `fix/cors-methods` (CORS `methods` explicite → débloque tous les DELETE/PATCH depuis le navigateur).

- ✅ **B7 — disputes : MERGÉ sur master** (merge `15238e7`, commit `5900d23`, 3 passes de review Walid traitées). Décisions actées : timeout neutre, dépôt capitaine-seul, MIME = limite connue → la règle du timeout reste **à écrire dans la ToS ([FT])**.
- **B9 — notifications : MERGÉ** (merge `88c33e8`, commit `eca332d`, carte Trello #51). Livré : migration `0016` (enum + table `notifications`), `utils/notification-schemas.ts` (schémas Zod par type, purs/sans DB) + `utils/notifications.ts` (`notify<T>` validé + dans la tx / `pushNotifications` après commit), `routes/notifications.ts` (3 routes : liste paginée par curseur + `unreadCount`, read idempotent sans oracle, read-all), event WS `notification` sur `/ws/chat` (`sendToUser` exporté), **8 déclencheurs** câblés dans `matches.ts` (accept, result × 3 issues), `disputes.ts` (resolve) et les 3 jobs 24 h, `openapi.yaml`. **3 correctifs de review appliqués** (spam re-soumission, validation Zod à l'écriture, pagination sur multiple exact) — détail dans la section Notifications ci-dessus. `test_notifications.py` (**81 ✅ / 0 ❌ avec `B9_JOBS=1`**), Vitest **19/19**. Au passage : fix d'un bug `helpers.py cleanup()` cassé depuis des jours (270 users/325 matchs de test jamais nettoyés). Le module **Notification system (1 pt)** passe au vert. **Total : 16 pts** depuis que « Advanced search » est compté (22/07) — soit **2 points de marge** au-dessus de la barre des 14, au lieu d'un seul.

- ✅ **BA1 — changer son mot de passe : MERGÉ** (merge `37c4e8c`, commit `9c1ae0b`, review Walid approuvée). `PATCH /users/me/password` — détail dans la section Auth & user ci-dessus.

**Frontend — F0-B, Fast Refresh, FR1 Register, F0-D, F-Nav, FR2 Login + 2FA, FL Landing publique et F0-C mergés.**

- Register complet et mergé : validation, inscription classique, erreurs backend, Google OAuth, session et redirection `/home` testés avec le backend réel.
- **F-Nav mergé** (commit `feat(frontend): partial F-nav`) : coquille de navigation (rails `LeftNav`/`RightNav`, `AuthNav`, `Logo`), pages **Mes équipes** + **Détail équipe** (création, roster, kick/quit/dissolve — consomme B5a), page **Ranking** (`RankingTable` consomme B2), sélecteur `LadderSelect`, et **codegen OpenAPI** (`api-types.gen.ts`). Détail dans la section Frontend plus haut.
- **FL mergé** (merge `85e1c76`) : vitrine publique `/` (cartes de jeux alimentées par `GET /games`, CTA login/register, footer PP/ToS, `LandingNav` distincte de `LeftNav`). ⚠️ Mergée **avec sa dette** — voir « Défauts connus » et la note FL en bas de fichier.
- **F0-C mergé** (merge `9d3b97c`, commit `7abdb3f`) : shell authentifié trois colonnes + garde unique sur la route layout `_authenticated`; `/home`, `/games`, `/profile`, `/ranking` et `/teams` sont protégés, les visiteurs sont renvoyés vers `/`, tandis qu'un utilisateur connecté sur `/` est redirigé vers `/home`. L'ancien `/dashboard` a été supprimé. Build/lint à 0 erreur et 0 warning. ⚠️ Le reload d'une session connectée reste bloqué par le contexte cross-scheme HTTP/HTTPS de dev et est suivi dans I4 : https://trello.com/c/wvEfFTVq.
- **Tickets frontend suivants** :
  - **Recherche + ajout de membre** : le back de recherche est **fait** — c'est **`GET /search?q=`** (préfixe joueurs + teams, réponse taggée `type`), **pas** `GET /users?search=`. Câbler `SearchBar` (tenu à l'écart, local) dessus → décommenter le bloc « Add member » + `addMember` de `team-detail.tsx`. ⚠️ Le `SearchBar` stashé tapait `GET /users?search=` : adapter l'URL + le parsing (la réponse est `{ results: [...] }` taggée, pas une liste d'users brute).
  - **Page `/profile`** (stub « a faire mdr »), **flux de liaison de compte** (`LinkAccountBanner` → external-accounts), rendre les lignes de ranking cliquables (**bloqué** : le back ne renvoie pas d'id de compétiteur), Upload Avatar (user : back prêt ; team : endpoint à créer).
  - **Après FR2** : pages match et notifications.
- Les pages `/home`, `/privacy`, `/terms` et `/profile` restent à remplir ou compléter (`/games` est fait depuis [F-GAMES], 30/07).
- ⚠️ **Types d'API côté front** : **codegen en place** — `frontend/src/lib/api-types.gen.ts` généré via `openapi-typescript` depuis `openapi.yaml` ; importer `components['schemas'][...]`, **ne jamais importer les types du backend** (ils décrivent la DB, pas le JSON). ⚠️ La codegen **ne corrige pas** le piège `Date`→`string` : `scheduledAt` est un `Date` côté back mais arrive en **`string` ISO** — ne pas le traiter comme un `Date`. **Régénérer `api-types.gen.ts` après toute modif de `openapi.yaml`.**

---

_31 juillet 2026 (nuit, après [BX-LEAVE]) — **`fix/team-409-messages` mergé** (front pur, aucune migration) — **sans carte Trello, décision de David** : la dette front créée par [BX-LEAVE] et celle laissée par [BX-DEL] tenaient en deux chaînes. Les deux refus **409** d'équipe tombaient sur un message générique (« Could not remove this player. » / « Could not dissolve the team. ») : le refus ne disait **ni pourquoi ni quoi faire**, exactement ce que le back venait d'éviter en donnant un `code`. 🚨 **LES DEUX PHRASES DIFFÈRENT PARCE QUE LES DEUX ROUTES NE REFUSENT PAS SUR LES MÊMES STATUTS — ne pas uniformiser** : retirer un membre refuse sur `LOCKING_STATUSES` seuls (un créneau `pending` est **annulé**, pas bloquant) → « ongoing match » ; dissoudre refuse sur `ENGAGING_STATUSES`, **`pending` inclus** → « **open or** ongoing match ». La 1ʳᵉ version disait « ongoing » des deux côtés : un capitaine avec un seul créneau ouvert aurait cherché un match qui n'est pas joué. **Corrigé après le codeur, en 2 endroits** (la phrase et la comparaison `exact` du check). 🔑 **`removeTeamMemberErrorMessage` prend un `intent: 'kick' | 'leave'` OBLIGATOIRE** : la même fonction sert l'exclusion et le départ volontaire, et « ce joueur est engagé dans un match » est faux quand c'est **moi** qui pars. Paramètre sans valeur par défaut **exprès** — le compilateur force les 2 appelants à se déclarer, aucune déduction implicite depuis l'état du composant, ni maintenant ni après un refacto. Discrimination sur le **`code`** du payload (jamais le seul statut) via `errorPayloadCode` — l'ex-`invitationErrorCode`, **renommé et remonté** parce qu'il ne connaissait déjà rien aux invitations, corps inchangé, 4 appelants suivis — et **constantes typées contre `api-types.gen.ts`** : un renommage backend casse le build ici au lieu de dégrader l'écran en silence (vérifié en review par une sonde `@ts-expect-error`). **Aucun JSX écrit** : les 3 blocs d'erreur existaient déjà (`TeamManage`, dialogue « Leave this team? », dialogue de dissolution). ⚠️ **UN SEUL des deux refus a un check d'audit** : M14 sur la dissolution, parce que `teams-matchmaking` **finit déjà** avec 4 créneaux `pending` — l'état bloquant coûtait 0 appel de mise en scène, et l'erreur réseau volontaire est déclarée par `expectHttp` **cloisonné à sa phase**, ancré sur l'id de l'équipe. Le 409 d'**exclusion** exige un match **actif**, donc une équipe adverse complète (roster + comptes liés) qui accepte : montage plus coûteux que le refus qu'il garderait, **écarté et écrit dans le bloc « ce que ce scénario ne prouve pas »**. Conséquence assumée : **le choix `kick`/`leave` est le point du ticket sans filet automatisé** — une inversion des 2 appelants passerait tous les verts. `teams-matchmaking` 20/20, `teams-manage` 35/35, `tsc -b` et lint verts. ⚠️ **`npm run build` échoue en `EACCES` sur `frontend/dist`** (dossier root-owned) — **problème d'hôte antérieur**, pas du ticket : `sudo chown -R $USER frontend/dist frontend/.tanstack`._

---

_31 juillet 2026 (nuit, après [BX-HASPWD]) — **[BX-LEAVE] mergé** (commit `46686fb`, merge `7081182`) — ⚠️ **migration `0024`** (valeur d'enum `match_cancelled_member_left`) : `docker compose restart backend` au prochain pull. 🚨 **Le défaut : `DELETE /teams/:id/members/:userId` ne regardait AUCUN match.** Un joueur retiré du roster restait **aligné** dans les créneaux `pending` de son ancienne équipe → il rejoignait une autre équipe du **même ladder** et **acceptait le créneau de son ex-équipe**, devenant participant des **deux camps du même match**. Séquence d'**usage normal**, pas une course exotique. Second dégât indépendant : un créneau dont la composition contient un non-membre pouvait être **joué** tel quel (la garde §5.1 n'est vérifiée qu'à la création et à l'acceptation de l'autre camp, **jamais rétroactivement sur le camp créateur**). Solde la dette « slots orphelins » ouverte à la review de B5b. **La règle, et la frontière n'est pas la même selon l'état du match** : match **actif** (`LOCKING_STATUSES`) → **409 `engaged_in_match`**, même code que [BX-DEL] ; créneau **`pending`** → **on n'empêche PAS le départ, on annule le créneau**. 🔑 **Pourquoi pas un refus sur le `pending` — ne pas rouvrir** : sur [BX-DEL] la personne bloquée est le propriétaire du compte, **elle** peut annuler ses matchs ; ici c'est un **simple membre**, et seul le **capitaine** peut annuler un créneau — on lui dirait « tu ne peux pas partir » **sans aucune action possible**, indéfiniment. 🔑 **Pourquoi annuler plutôt que laisser une compo à 4/5** : `POST /matches` garantit **exactement `format_size` par camp**, un 4/5 est un état que rien n'attend, et un adversaire qui l'accepte joue un **5v4**. 🚨 **LA CARTE SE TROMPAIT SUR UN POINT, VÉRIFIÉ AVANT DE CODER** : elle disait « sous le verrou **existant** de la route » — **cette route n'avait aucun verrou**, sa transaction n'écrivait qu'une ligne adressée par son id (le régime documenté dans le docblock de `lockRoster`). Le ticket l'en fait sortir : **une seule clé `team:<id>`, prise en premier**, dont la review a vérifié qu'elle vaut `hashtext()` de la branche team de `competitorKey()` (`matches.ts`) — donc sérialisation avec création **et** acceptation, et une clé unique ne peut pas fermer de cycle d'interblocage. 🚨 **DÉBORDEMENT DE PÉRIMÈTRE ASSUMÉ, ET C'EST LE CŒUR TECHNIQUE DU TICKET** : le verrou est **nécessaire mais pas suffisant**, parce que `validateSide()` lit le roster **hors transaction** — sérialiser ne rafraîchit pas une lecture antérieure. D'où `lineupOffRoster()` dans `matches.ts`, re-vérification de la composition **sous le même verrou**, à la création **et** à l'acceptation. **Mesuré, pas argumenté** : verrou seul → créneau fantôme **3/3** ; re-vérification seule → **1/3** ; les deux → **0/3**. La sonde du reviewer confirme 6/6 que le refus vient bien de la re-vérification, jamais du chemin froid. ⚠️ **Seul le camp ACCEPTEUR est revérifié** : le camp créateur est tenu par la route de départ (même clé de verrou), et les 2 autres chemins qui retirent une ligne `team_members` (dissolution, suppression de compte) refusent sur `ENGAGING_STATUSES` — **assouplir l'un des trois rouvre le trou en silence**, c'est écrit dans le code. **Rouge avant vert** : `test_teams_leave.py` contre la route non corrigée = **29 ✅ / 20 ❌**, dont « bob n'est participant d'AUCUN camp de M1 [attendu 0, reçu **2**] » ; après = **70/70**. Non-régression : invitations 114/114, notifications 71/71, teams/matches 53/53, create 28/28, accept 36/36, concurrency 18/18, open_slots 100/100, scheduling 38/38. **3 corrections appliquées après la review** : un commentaire qui affirmait à tort la **même liste de statuts** que `DELETE /users/me` (c'est le même `code`, une liste **volontairement plus étroite** — ne pas « réaligner ») ; le `.returning()` restauré sur la suppression du membre (la cascade de `DELETE /users/me` n'a **ni transaction ni verrou** et peut passer entre le SELECT et le DELETE → on notifiait un compte disparu → 23503 → 500) ; et le docblock sur le camp accepteur. ⚠️ **Signalé, NON corrigé** : quand un match vient d'être pris **et** qu'un joueur vient de partir, l'acceptation rend le 400 `offRoster` au lieu du 409 « n'est plus disponible » — corriger proprement coûtait une requête de plus **sur chaque acceptation** ou une restructuration de la transaction (un `return` dans le callback **commit**), trop cher pour un libellé dans un cas doublement rare. ⚠️ **Le front n'explique pas le 409** : `removeTeamMemberErrorMessage` ne mappe que 400 et 404 → « Could not remove this player. » sans remède. **Carte de suivi à créer.** ⚠️ Résidu accepté : sur deux départs simultanés du même créneau, la compo du **second** partant reste dans un match déjà `cancelled` — **inerte, vérifié dans le code** (ni `hasConflictingMatch`, ni `countOpenSlots`, ni l'UPDATE d'acceptation, ni les gardes de dissolution/suppression ne regardent `cancelled`). ⚠️ **`helpers.cleanup()` a dû gagner un `purge_created()`** : il repère les matchs de test **par leurs `match_participants`**, or ce ticket est le premier à en **supprimer** — la suite laissait 3 lignes `matches` orphelines par run. **Corrigé au passage** : `openapi.yaml` listait 13 types de notification alors qu'il en manquait **3** depuis B-INV._

---

_31 juillet 2026 (nuit, après [FX-TABLE]) — **[BX-HASPWD] mergé** (commit `fe14ece`, merge `ee710b8` ; aucune migration, `hasPassword` est **dérivé**, rien n'est stocké). 🔑 **Le fond du ticket : `oauthProvider` et « compte sans mot de passe » NE SONT PAS LA MÊME INFORMATION**, et le front n'avait que le premier. `routes/auth/google.ts` rattache Google à un compte existant retrouvé **par email sans toucher à son `passwordHash`** — ce compte a donc `oauthProvider = 'google'` **et** un mot de passe local, et `PATCH /users/me/password` (qui ne refuse en 400 que si le hash est `null`) l'accepte. La garde livrée par [F4] cachait « Change password » dès qu'un provider était présent : **nécessaire** (sans elle, un compte Google pur reçoit un 400 à chaque essai → ligne rouge en console → motif de rejet) mais **trop large**, et le faux négatif est **silencieux** — la section disparaît, sans erreur. 🚨 **L'ORDRE DES DEUX ÉTAPES EST LE TICKET** : le retrait de `passwordHash`/`totpSecret` était recopié **8 fois** (5 dans `routes/users.ts`, 2 dans `auth/index.ts`, 1 dans `2fa.ts`) ; on a d'abord extrait `toAuthUser()` (`utils/user.ts`) et migré les 8 sites, **puis seulement** ajouté le champ — sinon c'était 8 ajouts à la main, dont l'oubli d'un seul rendait `hasPassword` **indéfini** après, par exemple, un changement d'avatar, et faisait disparaître la section sans raison visible. ⚠️ **La projection du profil public (`GET /users/{pseudo}`) est restée à part et NE DOIT PAS passer par le helper** : dire si un **autre** joueur a un mot de passe est une fuite sans usage (vérifié au curl : ni `hasPassword`, ni `email`, ni `totpEnabled`). 🔑 **« 5 `$ref` pour 8 sites » était une fausse alerte, résolue en review** : les 8 sites ne font que **7 réponses** (`DELETE /users/me/avatar` a 2 chemins pour une seule), toutes derrière `#/components/schemas/User` — 4 en `$ref` direct, 3 via `AuthSuccess` ; aucun schéma de user en ligne ailleurs dans le YAML. **Corrigé au passage, hors périmètre mais dans le même fichier** : le contrat décrivait le callback Google en **`200 AuthSuccess`** alors que le code **redirige en 302 sans corps** — il promettait donc un `hasPassword` requis sur une réponse **qui n'existe pas**. Passé en `302` (+ `Location` et `Set-Cookie` documentés), et le piège du cas B y est désormais écrit. ⚠️ **Tranche front NON FAITE, volontairement** : le composant qui consommera le champ (`components/profile/PasswordChange.tsx`) **n'existe pas sur master**, il vit sur la branche [F4] d'Adrien — y toucher depuis une autre branche garantissait un conflit sur `/profile`. Seuls `frontend/src/types/auth.ts` (+1 champ) et la codegen ont bougé côté front. **Les 2 lignes à appliquer chez Adrien** : la garde se fonde sur `hasPassword === false`, plus sur `oauthProvider`, et le commentaire « KNOWN LIMIT » devient faux. Vérifs : `tsc --noEmit` back, `tsc -b --noEmit` + lint front, Vitest 40/40, et la preuve au **curl** sur un compte jetable (créé puis supprimé) — `hasPassword: true` sur register/login/`GET`/`PATCH /users/me`/avatar, absent du profil public, et **le cas du ticket reproduit** : `password_hash` mis à `NULL` + `oauth_provider='google'` → `PATCH /users/me/password` passe de **200** à **400**, `oauthProvider` valant « google » **dans les deux états**. **Ni `npm run audit` ni `run_all.py`** (aucun écran ne change, ni matchs ni équipes touchés). ⚠️ **Dette signalée, non traitée** : 11 handlers font encore un `select()` nu sur `users` (invariant #6) — aucun ne fuit aujourd'hui (ils passent par le helper ou ne testent qu'une existence), mais c'est le prochain endroit où une colonne sensible s'échapperait._

---

_31 juillet 2026 (nuit, après [F-ADMIN]) — **[FX-TABLE] mergé** (commit `8fd6e85`, merge `66fc9e9` ; aucune migration, front pur). **Sous 640 px, `MatchHistoryTable` ne rend plus une table mais des cartes** — un correctif unique qui sert **les 3 écrans** qui la montent (`/history`, page d'équipe, `/solo/$ladderId`). 🔑 **La dette était mesurée depuis F-HIST** : à 375 px la table laissait **262 px hors champ**, et à 1280 px `/history` voulait déjà 646 px de colonnes pour 575 disponibles. 🚨 **Contrainte n°1 du ticket, tenue : au-dessus de `sm` le DOM est inchangé À LA BALISE PRÈS** (vérifié nœud par nœud contre master) — trois scénarios d'audit existants (`history`, `solo`, `teams-matchmaking`) gardent le rendu desktop, et c'est ce qui permettait de toucher un composant partagé par 3 pages sans rejouer leurs specs. Livré : `MatchCard.tsx`, `MatchDateLink.tsx`, `MatchOpponentLink.tsx`, `match-sheet-click.ts`, `match-status.ts` et le hook `use-media-query.ts` ; `MatchRow` passe de 131 lignes de balisage à la réutilisation de ces briques. **`npm run audit` : 20 scénarios, 349 checks, exit 0.** ⚠️ **MERGÉ SANS QUE PERSONNE N'AIT REGARDÉ L'ÉCRAN** — ni `coder-front` ni `reviewer-front` n'ouvrent un navigateur, la review a lu le diff et fait tourner la campagne. Les **3 points visuels** à valider restent listés sur la carte https://trello.com/c/xdrVnG3H. 🚨 **Le merge embarque un 2ᵉ commit VOLONTAIREMENT SÉPARÉ et hors périmètre du ticket** (`dca2495`, `fix(audit)` sur `f-nav.mjs`) : il réparait un **`exit 2` qui tombait aussi sur master**, donc pas une régression du ticket. Mécanisme — le panneau de résultats de la recherche du rail **recouvre les items de navigation** ; dès que la saisie « au » du §8 ramène assez de comptes, le lien Solo devient **intercepté**, `locator.click` expire à 30 s, et la campagne est **rouge sans un seul check rouge pour l'expliquer**. La cause profonde est que **chaque campagne laisse ses comptes `audit…` en base** (13 au 31/07) : ils s'accumulent et matchent tous. Conformément à [FX-AUDIT], **on a durci le scénario, pas purgé la base** — mais ⚠️ **le mécanisme reste amorcé pour tout scénario qui tape dans la recherche**, et le nombre de comptes ne fait que croître : **carte de purge à créer**._

---

_31 juillet 2026 (nuit) — **[F-ADMIN] mergé** (aucune migration). L'arbitrage devient démontrable : un admin voit un **7ᵉ onglet « Arbitration »** dans son rail (rouge `arena-red`, avec le **compte** des litiges ouverts), une file `/admin/disputes` triée du plus ancien au plus récent, et les **3 issues de `POST /disputes/{id}/resolve`** au pied du dossier de litige existant — jamais sur un 2ᵉ écran. 🔑 **Décision produit de David, contre le plan initial de la carte** : l'onglet remplace la section « Disputed matches » qui était prévue sur `/home`. Un admin est **aussi un joueur ordinaire** ; mélanger sa file de travail à sa page de joueur cassait précisément l'équivalence qu'on veut garder. 🚨 **Le « PRÉREQUIS BLOQUANT » annoncé dans `CLAUDE.md` ÉTAIT FAUX, et c'est la leçon de cadrage du ticket** : `isAdmin` était **déjà servi au runtime** par `/auth/register`, `/auth/login`, `GET /users/me` **et le profil public** — les handlers font un `db.select()` nu puis ne retirent que `passwordHash`/`totpSecret`, donc le champ survivait. Seul le **contrat** mentait (`openapi.yaml` ne le déclarait pas). Vérifié **au curl sur le backend qui tourne**, pas par lecture : la note bloquante a coûté un faux prérequis dans deux sessions. **Back (3 touches, aucune route neuve)** : `isAdmin` déclaré dans `User` **et** `PublicUser` (décision de David : on l'assume sur le profil public — le badge visuel appartient à [F-PLAYER]) ; `GET /disputes` enrichi de `ladderName`/`format`/`gameId`/`gameName` + les **2 camps nommés**, en **7 requêtes quel que soit le nombre de litiges** (mesuré au `log_statement=all` : 7 à 1 litige, 7 à 4) — une file d'UUID et d'horaires était intriable ; et **`GET /matches/{id}` ouvert à l'admin**, parce que l'en-tête du dossier de litige pointe vers la feuille de match et lui répondait **403** (ligne rouge = motif de rejet). ⚠️ **La garde a été RESSERRÉE en review** : la 1ʳᵉ version ouvrait **tous** les statuts, ce qui livrait à l'admin la **composition nominative de chaque créneau `pending`** — que `GET /matches?status=pending` cache justement à tout le monde. Un admin étant aussi un compétiteur, il aurait scouté les rosters avant d'accepter. Restreint à **`disputed` + `cancelled`** (`resolve` produit `completed`, déjà public, ou `cancelled`), vérifié au curl statut par statut : `pending`/`in_progress`/`awaiting_confirmation` → 403, `disputed`/`cancelled` → 200. 🚨 **AUCUN non-admin ne doit déclencher `GET /disputes`** (403 = ligne rouge) : le hook porte `enabled: isAdmin`, l'écran « réservé » est rendu **sans requête**, et `useIsAdmin` teste `=== true` parce que `user` est `null` pendant le boot de session. `MenuItem` **étendu** (`tone` + `trailing`), jamais cloné ; contrastes **mesurés** (`arena-red` 5,40:1 sur le fond réel du rail, 5,00:1 en survol, AA passé). 🔑 **Règle produit posée par David pendant le ticket, désormais générale : TOUT nom d'équipe et TOUT nom de joueur est cliquable vers sa page.** `DisputeClaims` portait un docblock « NO LINKS ON THESE CARDS, and it is a decision » — la décision est renversée. Exceptions : compte supprimé, équipe dissoute, et **ligne déjà entièrement cliquable** (pas de lien dans un lien — c'est le cas de la file d'arbitrage). ⚠️ **Défaut visuel trouvé à l'œil par David, invisible à l'audit** : le `hover:underline` posé sur le lien décorait **tous** ses descendants, donc les **initiales dessinées dans la pastille** quand une équipe n'a pas de logo. Corrigé en `group`/`group-hover` sur le nom seul — **et le même défaut existait sur `MatchSideCard`** depuis FT-4A, corrigé aussi (`match-detail` 28/28). 🚨 **DEUX PIÈGES D'ENVIRONNEMENT À CONNAÎTRE, ils font rougir des scénarios sans rapport** : (1) **un admin laissé en base** → `f-nav` compte 7 liens au lieu de 6, et `dispute` trouve **2 zones de texte** au lieu d'1 quand le compte testé est à la fois partie prenante **et** arbitre ; (2) **des litiges de démo ajoutés à la main** → `dispute.mjs` tombe sur un dossier où son joueur n'a rien à faire et prend un **403**. Le scénario `admin-disputes.mjs` remet donc `is_admin = false` dans un **`finally`**, avec vérification (`A13`). ⚠️ **`dispute.mjs` suppose être seul sur la base de dev**, ce que [FX-AUDIT] interdit : fragilité réelle, non corrigée, à ticketer. ⚠️ **Deux commentaires de `dispute.mjs` (l. 52 et 410) disent encore que [F-ADMIN] n'existe pas** — devenus faux. **`npm run audit` : 20 scénarios, 344 checks, exit 0** (+1 scénario, +15 checks) ; `tsc -b`, lint et codegen verts, backend 137 checks e2e sans régression. 🚨 **Leçon de méthode, coûteuse** : le ticket a brûlé **~1 h d'agents** — un `coder-front` chargé de 4 sous-parties d'un coup (~1 800 lignes) et une review lancée avec l'ordre de **rejouer la campagne complète**. David a interrompu. **Une review lit un diff et ne relance que les scénarios touchés** ; ici le correctif de garde faisait une ligne et ne concernait qu'**un** scénario sur 20._

---

_31 juillet 2026 (soir) — **[F-DISPUTE] mergé** (commit `fbcc356`, merge `f6345c8` ; aucune migration). `/disputes/$disputeId` existe : **un litige n'est plus un cul-de-sac.** On pouvait en ouvrir un depuis FT-4B et **aucun écran n'existait ensuite** — ni fil de preuves, ni dépôt, ni suivi ; or un litige que personne ne tranche est **annulé au bout de 24 h** (job B7), donc qui ne peut pas déposer sa preuve **perd son match par forfait de fait**. 🔑 **La carte annonçait « front pur, zéro back » et c'était FAUX** — vérifié dans le handler avant de lancer le ticket, `GET /disputes/{id}` ne servait pas `createdAt`, donc l'échéance exigée par sa propre DoD était **littéralement incalculable**. C'est la **deuxième fois** (après B-MM) qu'un cadrage repose sur une lecture du contrat sans dérouler le handler : **grep le HANDLER**. L'extension back a été écrite **avant** de lancer le codeur, et couvre trois choses : `dispute.createdAt` ; un bloc **`match`** (statut, coup d'envoi, ladder + jeu) parce que 🚨 **la page ne doit JAMAIS appeler `GET /matches/{id}`** — cette route répond **403 à un admin non-participant** sur un match `disputed`, et [F-ADMIN] se greffera sur cette page — et parce que **`ladder.format` est la seule autorité pour dire « 1v1 »** (le piège FT-4A est vivant : une dispute reste consultable après arbitrage, donc après qu'une équipe a pu être dissoute) ; et **`settledBy: 'admin' | 'timeout'`. 🚨 CE DERNIER EST SORTI DE LA REVIEW, PAS DU CODAGE, et c'était le défaut le plus grave du ticket** : le job B7 écrit `resolution: 'cancelled'` **et** une `resolutionNotes` qui est une **ligne de log interne en français** (`'auto-résolu : aucun arbitrage admin sous 24 h'`), et la page rendait « **Settled by an admin** » puis cette phrase sous le libellé « **Admin's note** ». **Ce n'était pas un cas limite : tant que [F-ADMIN] n'existe pas, le timeout est le SEUL chemin vers `resolved`** — la moitié « résolue » de la page mentait donc systématiquement, et la dispute de démo y arrive **toute seule** 24 h après un `seed:dev`. ⚠️ **Le champ se dérive de `resolved_by_user_id` ET de la `resolution`** : la colonne est en `set null`, donc un admin qui supprime son compte ferait passer son arbitrage pour un timeout — or le job n'écrit **jamais** qu'un `cancelled`, donc un vainqueur désigné implique un humain. Le codeur avait raison contre ma consigne sur ce point, et sa prémisse a été vérifiée dans le job avant d'être acceptée. 🔑 **QUATRE FAUX VERTS ont été trouvés en review, aucun par les 329 checks** : un check qui comptait des `<img>` que l'`onError` du composant retirait **avant** la lecture du DOM (réécrit pour compter la **requête**, avec témoin positif) ; un sélecteur `h2:has-text("Next up") ~ a` qui résout **0 élément** parce que `SectionTitle` enferme son `<h2>` dans un `<div>` — la carte vedette n'était **jamais** inspectée ; un dossier « arbitré » forgé **sans `resolved_by_user_id`**, donc lu par le contrat comme un timeout ; et une assertion qui ne tenait que grâce aux **créneaux de démo posés à la main par un coéquipier** (`bbbb2222-…`), la dépendance exacte que [FX-AUDIT] interdit. ⚠️ **`useDispute` porte `staleTime: 0` ET `gcTime: 0`, à ne pas « optimiser »** : les `evidenceUrl` sont **présignées ~5 min** et React Query **peint le cache avant de revalider**, donc un retour sur la page monterait des `<img>` sur des URLs périmées → 403 → ligne rouge. Le flash de chargement à chaque visite est le prix assumé. ⚠️ **`ImagePicker` n'a pas été réutilisé** (quatre différences simultanées, dont **un PDF n'a pas d'aperçu**) ; seule la **barre de progression** était réellement partagée → `ui/progress-bar.tsx` extraite, DOM inchangé, non-régression prouvée (`teams-manage` 35/35, `ft1c-team-logo` 12/12). 🔑 **Un `<img>` pointé sur un PDF n'écrit PAS de ligne console** (mesuré sur un build saboté : MinIO répond 200, seul le décodage échoue) — la règle « lien seul pour un PDF » reste bonne, mais son **motif** est le téléchargement inutile, pas la console. ⚠️ **Dette assumée, décision de David** : l'en-tête du dossier est la **4ᵉ copie** de l'idiome `GameBanner` + `<h1>` + rangée de méta, et le `HeroFrame` promis par F-HOME **n'a pas été extrait** — gain cosmétique contre le risque de casser `MatchHeader`, `game-detail` et `ladder-detail`. **`npm run audit` : 19 scénarios, 329 checks, exit 0** (`EXIT=0` lu sur le process, sortie capturée dans un fichier et **jamais passée dans un `tail`**) ; `tsc -b`, lint et build conteneur verts, codegen byte-identique, backend 40/40._

---

_30 juillet 2026 (soir) — **[F-HIST] codée sur `feature/f-hist-history-page`** (aucune migration, **aucun ticket back**). `/history` était la **dernière page vierge** du rail : elle est désormais la seule vue **transversale** de l'app — avant elle, savoir ce qu'on avait joué demandait de visiter une page par équipe **et** un `/solo/$ladderId` par ladder. 🔑 **Zéro route neuve** : `GET /matches/me` **sans paramètre** est exactement cette page, c'est pour elle que B-SOLO l'avait rendue polymorphe — un appel, ni curseur ni pagination. 🚨 **La vraie valeur est la section « Waiting on you », pas le tableau** : `awaiting_confirmation` et `disputed` sont les seuls matchs sur lesquels court une **échéance de 24 h** (job d'auto-confirmation) et rien ne les regroupait ; elle lit l'historique **complet**, donc **aucun filtre ne peut la vider**. 🚨 **Pas de bilan V-D global** (décision tenue) : le payload ne dit pas si j'étais aligné ou sur le banc, et surtout un V-D **inter-ladders ne veut rien dire**. **Jeu/format par ligne** rendus par une colonne « Ladder » **opt-in, dérivée comme `showLineup`** (`rows.some(r => r.ladder !== undefined)`) : les deux consommateurs existants de `MatchHistoryTable` ne la passent pas et **leur DOM est inchangé** — gardé des DEUX côtés sur le même run (`H8` sur `/history`, `H9` sur l'historique solo). `ladderName`/`gameName` résolus depuis les caches `GET /ladders` + `GET /games` (**zéro requête par ligne**, discipline de F-GAMES), avec repli lisible sur le slug. **Filtres 100 % client** (aucune requête, `?ladderId=` jamais utilisé) et **options dérivées de la donnée, jamais des enums du contrat** — piège F-MM, ici le contrat connaît 4 formats et l'écran n'en offre qu'un. 🔑 **UN CHECK EST SORTI VERT SUR DU CODE VOLONTAIREMENT CASSÉ**, et c'est la leçon du ticket : `H13` (« la section à traiter n'est pas filtrable ») lisait la section **APRÈS** avoir cliqué « Clear the filters », donc sur un écran **non filtré** — il est resté vert alors que la section était branchée sur la liste filtrée. **Un check qui mesure après avoir défait la condition qu'il teste ne garde rien** : il lit désormais **pendant** que le filtre vide le tableau, et il rougit. Les 17 checks ont été passés au banc de sabotage (colonne rendue inconditionnelle, lignes sans `ladder`, section branchée sur la liste filtrée, `completed` ajouté aux statuts « en cours », `canOpenSheet: false`, lien de l'état vide détourné) — **8 vus rouges avant vert**. ⚠️ **`f-nav.mjs` N5/N6 re-ciblés pour la TROISIÈME fois** : `/history` émet désormais des requêtes, il ne reste **aucune paire de pages libres**, donc le mécanisme « deux navigations client » est mort — on applique le repli déjà écrit dans le commentaire du scénario : repartir d'un `page.goto` (cache vidé), qui est aussi la **précondition** de N6, posée et assertée dans le même geste. ⚠️ **`myMatchesKey()` prend un argument optionnel** (`['matches','me','all']` vs `['matches','me',<ladderId>]`) : deux entrées, **même préfixe**, donc `useAcceptMatch` les balaie déjà. ⚠️ **Aucune région live sur l'écran** (rien à annoncer), et l'avis « N matches in dispute » du tableau est **éteint** ici — il aurait contredit la section dès qu'un filtre réduisait la liste. **`npm run audit` : 17 scénarios, 282 checks, exit 0** ; `tsc -b`, `lint` et `npm run build` (dans le conteneur) verts. 🚨 **La campagne est sortie `exit 2` au premier essai, et ce n'était PAS imputable au ticket** : `matchmaking.mjs` a crashé en « strict mode violation » parce que la base de dev porte des **données de démo laissées à la main le 30/07 à 19:37** (8 équipes + 5 créneaux ouverts, uuids déterministes `bbbb2222-…`, antérieurs à la session), dont **un créneau ouvert sur cs2 2v2** — le ladder que `matchmaking.mjs` choisit, en supposant être le seul à y tenir un créneau. La campagne verte a été obtenue en passant ce seul créneau à `cancelled` le temps du run, **puis en le remettant à `pending`** : on ne supprime pas les données de démo d'un coéquipier. **À trancher par David** — soit purger ces créneaux (`update matches set status='cancelled' where id='bbbb2222-0000-4000-8000-000000000005';`), soit rendre `matchmaking.mjs` insensible à un créneau étranger sur son ladder ; sinon **toute campagne future sortira exit 2** pour cette raison. C'est exactement le « clear complet » réservé au futur ticket « seed propre »._

---

_30 juillet 2026 — **[B-MM] + [F-MM] codés et reviewés, PAS ENCORE MERGÉS** (B-MM : commit `a03a4de` sur `feature/b-mm-open-slots` · F-MM : commit `fbb97c8` sur `feature/f-mm-matchmaking-page`, **basée sur la première** ; aucune migration). **Ordre de merge imposé : B-MM d'abord.** `/matchmaking` cesse d'être une page vierge et **le cycle challenge/accept devient bouclable de bout en bout à la souris** — ce qui n'était pas vrai avant : le front n'appelait **jamais** `POST /matches/{id}/accept`, seuls le seed et l'audit acceptaient, par appel API direct. Les affirmations « atteignable à la souris » (FT-2C) et « bouclable entièrement à la souris » (FT-4B) étaient donc **fausses** ; elles ne le sont plus. 🔑 **Le ticket a d'abord été cadré sur un FAIT FAUX** : « aucune route ne liste les créneaux ouverts » — `GET /matches?ladderId=` existait, documentée (`openapi.yaml`) et couverte par 4 suites Python ; le `get` n'avait pas été déroulé sous le `post`. **Grep le HANDLER, pas seulement le contrat, avant de conclure qu'une route manque.** La carte est passée d'une route neuve `/matches/open` à un **enrichissement** de l'existante — ce qui est aussi l'arbitrage rendu par B-SOLO et noté comme réapplicable. 🚨 **Seconde correction de cadrage, produit celle-là** : la carte front prévoyait d'afficher l'adversaire et son Elo sur chaque ligne, ce qui **contredisait l'anonymat B5b** (créateur ET maps volontairement masqués, décision documentée et testée : on accepte sans savoir qui on affronte, ce qui empêche de choisir ses adversaires et protège l'Elo). L'anonymat a été **conservé** et les deux cartes corrigées. **Back** : `ladderId` optionnel, filtres `gameId`/`format`/`acceptable`/`limit`, `ladderName`+`gameName` par jointure, et un verdict `canAccept` + `reason` à **6 codes** qui **miroite `validateSide` branche par branche** — s'il dit « tu peux » et que l'accept refuse, l'UI offre un bouton qui écrit une ligne rouge en console (motif de rejet). ⚠️ **`roster_too_small` et `roster_not_linked` sont distincts à dessein** : la 1ʳᵉ version les fusionnait parce que l'`INNER JOIN` sur `user_external_accounts` ne remontait **que les membres liés** — « 1 membre lié » et « 5 membres dont 1 lié » donnaient le même compte ; corrigé en `LEFT JOIN` + deux compteurs, **sans requête de plus**. ⚠️ **Un paramètre présent mais vide vaut « absent »** (`?gameId=`), sinon un front qui construit `?gameId=${state}` prend un 400. Requêtes **constantes** (plancher 5, plafond 7, mesuré au `log_statement=all`). **Front** : chaque refus porte **la phrase ET le lien qui le résout** (`no_team` → `/teams?create=<ladderId>`, les deux codes de roster → sa page d'équipe), ce qui fait qu'un compte neuf voit l'activité de la plateforme sans jamais se voir offrir un bouton refusé ; `LineupPicker` **extrait de `CreateMatchPanel` au 2ᵉ usage** (DOM inchangé, `CreateMatchPanel` toujours pas généralisé). ⚠️ **Le dernier chemin vers une ligne rouge a été trouvé EN REVIEW** : le bouton ne se fiait qu'à `canAccept`, or le serveur ne filtre les 15 min qu'**au moment de la réponse** — onglet ouvert 2 min sur un créneau à T+16 min, clic, **409**. Fermé par `useSlotClock()` (tick 30 s) avec `dataUpdatedAt` en **plancher**. ⚠️ **Nuance qui contredit FT-4B, à ne pas « réaligner »** : ici `dataUpdatedAt` seul est un no-op (c'est l'instant où le serveur a appliqué la même règle) et le sens de sûreté est **inversé** — un `now` en retard fait paraître le créneau plus loin, donc **offre** le bouton. ⚠️ `f-nav.mjs` N5/N6 re-ciblés : ils visaient « les 2 seules pages du rail sans requête », `/matchmaking` en émet désormais — il n'y aura plus de page libre quand `/home` cessera d'être un stub. Deux reviews Opus, **zéro bloquant** chacune, 6 non-bloquants back et 8 front appliqués. `test_matches_open_slots.py` **100 checks** ; **`npm run audit` : 16 scénarios, 257 checks, exit 0**, vérifié en propre après les corrections. ⚠️ **L'audit tourne sur l'HÔTE** : dans le conteneur front il sort « Aucun Chromium trouvé »._

---

_30 juillet 2026 — **[F-GAMES] mergé** (commit `48e027b`, merge `57ba70e` ; aucune migration). `/games` et `/games/$gameId` remplacent le stub `<div>a faire</div>` et bâtissent le parcours `/games → /games/$gameId → /ladders/$ladderId`. 🔑 **Ça solde la dette laissée ouverte par FT-3** : depuis la suppression de `/ranking`, un compte **sans équipe** n'avait plus aucun chemin cliquable vers un classement. **Le top 3 est un APERÇU** — règles, maps et classement complet restent sur `/ladders/$ladderId`, le podium ne sert qu'à faire cliquer. Petit volet backend : `GET /games/{id}` sert le pool de maps du jeu, miroir de `GET /ladders/{id}` (le pool appartient au **jeu**, donc il est affiché **une fois** au lieu d'être répété sur chaque carte de ladder). 🔑 **Un seul `useQueries` sur `ladderRankingsOptions()`** : la clé `['ladder', id, 'rankings']` est celle de la page ladder, donc suivre « See the full standings » coûte **zéro requête** — mesuré par `G9`, avec `G4` en témoin positif. 🔑 **Le slug se résout depuis la liste `GET /games` en cache** : un slug inconnu rend son erreur **sans dépenser de requête**, donc sans ligne rouge en console (motif de rejet du projet). 🚨 **Le CTA est décidé par le `format` du ladder, jamais par l'absence d'équipe** — le piège déjà corrigé deux fois (FT-4A, F-SOLO). ⚠️ **Cartes triées par taille de format décroissante** : `GET /ladders` trie par NOM, ce qui ouvrait la page sur « cs2 2v2 (Wingman) », vide, devant le 5v5 qui porte toutes les équipes classées. **Trois défauts trouvés à l'œil sur une capture, invisibles à l'audit** : les médailles (icône lucide en contour fin → or et bronze indistinguables à 16 px ; puis disque plein → seul aplat opaque de l'écran ; retenu : **la recette de `ui/pill.tsx`**, dont le ton `live` est déjà `rank-gold` aux mêmes proportions), l'ordre des cartes, et **un Elo affiché sans jamais dire que c'était un Elo** — trou double, visuel et accessible (« First place, Team Alpha, 1328 » au lecteur d'écran). ⚠️ **Contraste mesuré du chiffre sur sa pastille** : or 8,0:1, argent 11,4:1, **bronze 4,3:1 — sous AA d'un cheveu** ; même famille que la dette `text-text-muted` (4,23:1), le correctif est d'éclaircir `--color-rank-bronze` au ticket design system. 🔑 **Deux checks du ticket sont nés d'une leçon** : `G8b` comptait les régions live sur une page **déjà chargée**, où elles sont démontées par définition — vert par construction ; il retient désormais `GET /ladders` pour mesurer **pendant** le chargement. Et `M11c` (nouveau, dans `match-detail.mjs`) a d'abord rougi à tort parce que `waitForURL` rend la main **avant le rendu React** : le `<h1>` de la fiche de match était encore monté, donc « attendre un `<h1>` » n'attendait rien. **`npm run audit` : 15 scénarios, 234 checks, exit 0.**_

_30 juillet 2026 — **[B-SOLO] puis [F-SOLO] mergés** (B-SOLO : commit `7719256`, merge `e9fce76` · F-SOLO : commit `58ad523`, merge `8d7dd34` ; aucune migration). `/solo` et `/solo/$ladderId` remplacent la page vierge de F-Nav : liste des ladders 1v1 avec mon standing, puis dossier de compétiteur (hero, Overview + Matches, ouverture et annulation d'un créneau). **Découpage back-puis-front assumé et mesuré** : `GET /matches/me` ne servait que `{id, ladderId, status, scheduledAt, startedAt, maps}` alors que son jumeau `GET /teams/{id}/matches` sert l'adversaire, le score, le résultat, l'Elo et le litige — sans enrichissement l'onglet Matches solo se réduisait à une liste de dates, sur le même écran où une équipe a un historique complet._

_🔑 **La décision structurante : enrichir la route existante plutôt que créer des routes solo.** Trois arguments, dans l'ordre où ils ont pesé. (1) `/matches/me` **n'est pas une route teams** — elle renvoyait déjà les matchs solo (`asPlayer` lit `match_participants`), elle était juste incomplète. (2) Le **précédent existe dans l'API** : `GET /ladders/{id}/rankings` expose depuis FT-3 un `competitor` polymorphe `oneOf[user|team]` que le front rend déjà ; le domaine le dit aussi (`match_sides.team_id` nullable, `rankings.user_id` XOR `team_id`, deux index uniques partiels). `opponent` réutilise donc **littéralement** le type `Competitor` de `utils/leaderboard.ts`. (3) **L'argument qui a fermé le débat : `/history` aura besoin de la forme polymorphe de toute façon** — une route solo-only aurait fait écrire le shaping deux fois et serait devenue du poids mort. Mesuré : le handler passe de 45 lignes / 4 requêtes à ~150 lignes / 9 requêtes constantes, ce qui justifiait un ticket backend séparé plutôt qu'un ticket général._

_🚨 **Le piège central, gardé des deux côtés** : c'est le **`format` du ladder** qui dit si l'adversaire est un joueur ou une équipe, **jamais la nullité de `match_sides.team_id`** — cette colonne est en `SET NULL`, donc une équipe dissoute laisse un camp orphelin sur un 5v5 terminé. Le lire comme « solo » renverrait le premier joueur du camp adverse à la place de l'équipe : c'est exactement le bug introduit puis corrigé côté front pendant FT-4A. Le check qui le garde a été **vu ROUGE avant vert** (bug réintroduit temporairement, l'assertion est tombée en renvoyant le capitaine de l'équipe dissoute comme joueur solo). ⚠️ **`opponent: null` a QUATRE causes** : personne n'a accepté · créneau annulé · équipe adverse dissoute · **adversaire ayant supprimé son compte** (`match_participants.user_id` en CASCADE depuis BX-DEL, et partir est autorisé une fois ses matchs terminés). Les deux dernières sont **inatteignables par le seed** : `test_matches_me.py` les fabrique, et passe de **23 à 65 checks**._

_⚠️ **L'asymétrie produit qui pilote tout le front** : **on n'adhère pas à un ladder solo.** Une ligne `rankings` naît du **premier résultat de match**, pas d'une inscription — donc `/solo` liste **les ladders qui existent**, jamais « ceux où je suis classé », sinon un compte neuf voit une page vide **sans porte d'entrée**. Corollaires : pas de bouton de création (un ladder solo ne se crée pas), pas d'onglet **Manage** (rien à renommer, pas de roster, pas de dissolution), et la section Roster remplacée par **l'état du compte lié** — c'est lui qui garde `POST /matches` (§5.1). `hasLinkedProvider` est **trivaluée** : si la requête des comptes échoue, « inconnu » n'est jamais traité comme « lié », et le bouton n'apparaît pas (un 400 laisserait une ligne rouge, motif de rejet). **Cas hors carte tranché en cours de route** : `/solo/<id-d-un-ladder-5v5>` répond **200** côté API, il n'y avait donc aucune erreur à hériter — écran dédié, décidé sur le format._

_**Extractions au second usage** : `lib/match-slots.ts` (grille des quarts, 15 min d'avance, chevauchement, plafond de 5) et `lib/match-history.ts` ; `MatchRow` + `MatchHistoryTable` → `components/matches/` ; `LadderExcerpt` → `components/ladders/` ; `StatStrip` → `components/ui/` ; `GamePosterTile` ; et les 4 mutations de créneau enfin déplacées vers `match-mutations.ts` (intention écrite dans `team-mutations.ts` depuis FT-2C), désormais **pilotées par clé de cache**. ⚠️ **`CreateMatchPanel` n'a PAS été généralisé** (décision de David) : ce qui ne doit jamais diverger, ce sont les **règles** — elles sont partagées ; le JSX solo est autonome, parce que fusionner 410 lignes gardées par un scénario de 532, mergées la même semaine, était du risque de merge pour du balisage qui n'est pas commun. Le DOM du cas équipe est **inchangé**, vérifié ligne à ligne en review._

_🔑 **Deux leçons de harnais.** (1) **Playwright matche le nom accessible en SOUS-CHAÎNE par défaut** : `getByRole('link', {name: pseudo})` attrapait **deux** liens, celui de la date portant `aria-label="Match sheet against <pseudo>…"` — viser le libellé exact. (2) ⚠️ **Le seed se périme tout seul** : la campagne sur master est sortie `exit 2` **sans aucune régression**, le job d'auto-confirmation 24 h ayant fermé le match de démo `awaiting_confirmation` pendant la session, alors que `match-detail` exige les 7 états cs2 → `docker compose exec backend npm run seed:dev` **avant** toute campagne. Après re-seed : **14 scénarios, 211 checks, exit 0** sur master. ⚠️ Un non-bloquant de review a été **réfuté plutôt que reporté** : conditionner les 3 requêtes de `/solo/$ladderId` à la garde de format n'économise rien (les hooks partent dans le même rendu, le format n'est pas encore connu) et l'alternative — les enchaîner — coûte un aller-retour à chaque visite légitime._

---

_29 juillet 2026 — **[FT-4A] puis [FT-4B] mergés** (4A : commit `5729540`, merge `436531e` · 4B : commit `c2f9019`, merge `8a39a92` ; aucune migration). `/matches/$matchId` cesse d'être un placeholder **et** cesse d'être en lecture seule : la fiche (les 7 états du cycle, les deux camps face à face, le score Bo3, l'**Elo par CAMP jamais par joueur**, les maps pilotées par la donnée, les lineups, la variante **1v1**) puis la **saisie du score, la confirmation et le litige**. Le cycle challenge/accept est désormais bouclable **entièrement à la souris**, de l'ouverture du créneau à l'Elo appliqué. **Découpage en deux assumé** sur trois mesures, pas une intuition : la page équipe de complexité comparable avait pris 3 tickets, le scénario d'audit de l'écriture était le plus cher jamais écrit, et le helper SQL demandé était une **capacité neuve** du harnais — les bundler aurait fait bloquer les deux ensemble. La couture est **lecture pure / écriture pure** : 4A ne contient aucun bouton d'action. **`npm run audit` : 13 scénarios, 185 checks, exit 0** (campagne complète rejouée verte deux fois)._
<br>
_🔑 **LE PIÈGE DE 4A, À NE JAMAIS RÉINTRODUIRE : « ce camp n'a pas d'équipe » ne signifie PAS 1v1.** `match_sides.team_id` est en `set null` et une équipe dont tous les matchs sont terminés **peut** être dissoute — son camp survit alors avec `team: null` sur un 5v5 `completed`, lisible par n'importe quel compte depuis B15. Lire ça comme « solo » renommait le camp d'après son 1ᵉʳ joueur et **supprimait la lineup des 5 joueurs**. C'est le **format du ladder** qui tranche (`isSoloMatch`, enum fermé du contrat). ⚠️ **Un créneau ANNULÉ a la même forme qu'un créneau ouvert** (un seul camp) : la fiche doit lire le **statut**, jamais déduire « encore acceptable » de l'absence d'adversaire — elle se titrait « open slot » et annonçait « any team can accept it » **sous une pastille CANCELLED**. 🔑 **Deux checks ne gardaient rien**, trouvés en 2ᵉ review : l'un comptait 2 nœuds (`text=Cancelled` matchait la pastille **et** le mot dans la phrase), l'autre lisait un attribut avec `getAttribute()`, qui **lève** quand rien ne matche → `exit 2` (harnais en échec) au lieu d'un rouge imputable au ticket. **Compter avant de lire.**_
<br>
_**Ce que 4B apporte de structurant.** Le panneau **ne rend RIEN à qui ne peut pas agir** (visiteur, banc, match hors des statuts scorables, camp ayant déjà soumis) et **aucun contrôle avant le coup d'envoi** : un bouton que l'API refuserait garantit une ligne rouge en console, motif de rejet du projet. Les scores sont **relatifs au soumetteur**, jamais indexés sur `sideIndex` — c'est ce qui rend « confirmer » exprimable (confirmer = renvoyer le **miroir exact** de la soumission adverse). **`nowMs` vient de `dataUpdatedAt`** et non plus d'un instantané au mount : une horloge figée gardait le formulaire caché tant que l'onglet restait ouvert. ⚠️ Elle avance sur un **refetch**, pas sur l'horloge murale — assumé, `refetchOnWindowFocus` suffit puisqu'on rapporte des parties jouées ailleurs, et un `nowMs` en retard ne peut que **cacher** le formulaire, jamais offrir un bouton que le serveur refuserait. **Aucun 400/409 de `POST /matches/{id}/result` ne porte de `code` stable** : le mapping route sur le **statut seul** (invariant #8). `OptionTile` extrait de `CreateMatchPanel` au 2ᵉ usage._
<br>
_🔑 **Le harnais d'audit sait maintenant faire un appel système** (`tests/console-audit/sql.mjs`, `docker compose exec postgres psql` via `spawnSync` **sans shell**, uuid passés par `assertUuid`). C'était la capacité manquante : **reculer `scheduled_at` est la seule opération de la fixture que l'API ne sait pas faire**, et sans elle aucun match n'atteint un état scorable dans un test. Le scénario tourne sur un ladder **1v1** (2 comptes jetables au lieu de 10). Le teardown force le match hors des statuts engageants **avant** de le supprimer, sinon la garde de [BX-DEL] refuse la suppression des comptes._
<br>
_**Deux leçons de la review de 4B, toutes deux gardées par un check.** ⚠️ **`R11b` garde un ORDONNANCEMENT**, et c'est pourquoi il existe **séparément de `R9`** : `ConfirmDialog` ne retombe sur `returnFocusRef` **que si son ouvrant a quitté le DOM** (`opener.isConnected`), donc sur le chemin « Confirmer » la restitution du focus par la plateforme court contre le démontage du bouton par le refetch. Mesuré vert 4 fois de suite, jamais déduit de `R9` (qui couvre l'autre chemin, sans boîte). Au passage : **un `.focus()` appelé pendant qu'un `<dialog>` modal est ouvert est un NO-OP** (page inerte) — le commentaire qui affirmait le contraire était faux. ⚠️ **Un plafond d'attente relevé sans mesure est un cache-misère** : `awaitFocusRestored` est passé de 5 s à 15 s pendant ce ticket (instabilité d'`I6-bis`), mais à 15 s « le focus arrive en 200 ms » et « en 9 s » sont le **même vert**. Le helper trace désormais le délai réel au-delà de 1 s **sans faire rougir** (un seuil de latence figé en dur rendrait la campagne dépendante de la charge de la machine, soit exactement le non-déterminisme qu'on venait de chasser). Constat des deux campagnes : **aucune ligne « ⏱ »**, le plafond n'est jamais approché._
<br>
_**Dette laissée** : le libellé de repli **« Disbanded team »** reste non couvert par une donnée ni par un check (il faut une équipe dissoute **après** un match terminé pour l'obtenir) → futur ticket « seed propre ». Le **403** et le **429** du mapping d'erreurs sont écrits mais jamais déclenchés (atteignables seulement depuis une page périmée ou sous martèlement). `useCreateMatch`/`useCancelMatch` restent dans `lib/team-mutations.ts` alors que leur commentaire demande leur déplacement au second consommateur : non fait, ça toucherait la page équipe et ses 3 scénarios sans gain fonctionnel._
<br>
_⚠️ **Le seed sème 8 matchs de démo** (7 cs2 5v5 Alpha vs Bravo, un par état du cycle dont un **annulé** ajouté par 4A, + 1 chess **1v1** terminé) et **3 comptes connectables** (`alice`/`bob`/`carol`). Le créneau « accepté, avant l'heure » est à **+2 JOURS** et le créneau ouvert à **+3 jours** : à +1 h, l'état cessait d'exister une heure après le seed et la campagne partait en `exit 2` l'après-midi. **Ne pas raccourcir.** ⚠️ **Au prochain pull : `docker compose exec backend npm run seed:dev`** — le scénario `match-detail` **exige** la base semée (sinon `exit 2`)._

---

_29 juillet 2026 — **[F-Nav] rail gauche mergé** (commit `4a5c4bb`, merge `822a6eb`). **`LeftRail` absorbe `LeftNav` : le rail existait EN DOUBLE.** Un seul composant porte désormais le wordmark, la recherche, les 6 items et le bloc compte. **Livré une 1ʳᵉ fois sans suivre la maquette, puis RECODÉ** : `vsmode-home-demo.html` (`.left`/`.nav-item`/`.sep`) fait foi pour la structure et les dimensions, `index.css` pour couleurs/radius/ombres — 288 → **264 px**, `p-6` → **18/16**, wordmark 48 px italique → **30 px droit**, icônes 20 → **16 px**, recherche sortie du `<nav>` (ce n'est pas un lien). 🔑 **La maquette contenait déjà la solution d'un défaut trouvé en review** : l'actif se distingue du survol par la **bordure** (`.nav-item.active` ajoute `border-color`, `:hover:not(.active)` n'y touche pas) — la 1ʳᵉ passe avait gardé le fond et jeté la bordure, rendant les deux états **identiques au pixel**. **`ranking` disparaît du rail** (absent de la maquette), ce qui règle du même coup le fait que `to="/ranking"` **cassait `npm run build` de master** au merge (route supprimée par FT-3, branche partie avant). **Décision : aucun item grisé** — les 3 pages absentes (`/solo`, `/matchmaking`, `/history`) sont créées **vierges** ; `MenuItem` garde `muted`/`disabled` sans appelant (l'état `.off` de la maquette), mais la prop `params` est **supprimée** (morte ET non sûre : le wrapper cassait l'appariement `to`/`params`). **`UserSearch` devient `SearchBar`** : une seule implémentation du debounce, de l'abort et de la garde anti-réponse-hors-ordre, servant le rail **et** `TeamInvitePlayer`. ⚠️ **`AuthNav` n'est PAS un composant du rail** : `LandingNav` le monte aussi sur la landing publique — le `<ConfirmDialog>` de déconnexion était rendu **sans garde**, donc son `<h2>` « Log out » était le **premier titre du DOM d'un visiteur anonyme**, avant le `<h1>` ; il est passé sous `{isLogged && …}` et `LandingNav` de `<nav>` à **`<aside>`**. ⚠️ **Lien d'évitement : jamais `sr-only` + `focus:not-sr-only`** — la paire compile un `padding:0` de spécificité (0,2,0) qui **bat** `px-4`/`py-2` en (0,1,0), le lien se rendait en 149 × 22 px, **sous les 24 px** de WCAG 2.5.8 ; motif retenu, la **translation**. 🔑 **Le scénario `f-nav.mjs` (18 checks) MESURE les 13 dimensions de la maquette** : l'échelle dynamique de Tailwind v4 accepte n'importe quel nombre, donc `w-66` mal tapé en `w-64` passe lint **et** build en silence — **un critère visuel non mesuré n'est pas gardé**. ⚠️ **`L7` de `ladder-detail` est scopé à `<main>` depuis ce ticket** : il compte `queue|matchmaking|auto-match` pour garder la décision produit challenge/accept, mais balayait le **document entier** alors que le rail persistant porte un item `Matchmaking` sur toutes les pages authentifiées — **même piège pour tout check qui balaie la page**._
<br>

---

_28 juillet 2026 — **[FT-3] mergé** (commit `715aa9c`, merge `65de356`) : `/ladders/$ladderId` remplace le placeholder de FT-2A (le lien « See the full ladder » menait à une coquille vide). Identité + artwork, règles en clair, pool de maps servi par l'API, classement complet (joueurs **et** équipes), ligne de sa propre équipe surlignée. **`GET /ladders/{id}` enrichi du pool de maps du JEU**, lu dans la **même table que `POST /matches`** : la page ne peut pas annoncer des maps que le serveur n'attribuera pas. Section pilotée par la **donnée**, jamais par une liste de jeux en dur — seuls **cs2 (7)** et **valorant (6)** ont des maps. `openapi.yaml` mentait sur `lockoutMinutes` (« aussi le délai minimum avant soumission de score » : la seule garde est `scheduledAt`) → corrigé, `api-types.gen.ts` régénéré, aucun type modifié. ⚠️ **Le code venait d'un chat mobile, sans `coder-front` ni review** : le rapport `reviewer-front` (Opus) a sorti **3 bloquants + 9 non-bloquants**, tous soldés avant merge. **Trois écarts assumés, à ne pas « corriger » par erreur** : (1) ⚠️ **la carte Trello ment sur une règle** — elle annonce « slot sans adversaire annulé au bout de 24 h », **c'est FAUX**, `cancelExpiredSlots` l'annule dès qu'il passe **sous `MIN_LEAD_MINUTES` de son PROPRE coup d'envoi** (les 24 h sont l'autre mécanisme, match joué mais non rapporté) — c'est la règle du **code** qui est affichée ; (2) ⚠️ **`/ranking` SUPPRIMÉE** (page, route, `RankingTable`, entrée de nav) au lieu d'être migrée, décision de David — **dette ouverte** : plus aucun point d'entrée vers un classement pour un compte **sans équipe** ; (3) ⚠️ **NI pagination NI conteneur défilant**, décision prise **avec la mesure** — à 200 compétiteurs la page fait **11 431 px (16 écrans)**, 2941 nœuds DOM, 1278 ms : on accepte de descendre la page et on garde le **Ctrl+F sur tout le classement** (une pagination ferait atterrir un 137ᵉ sur une page 1 sans lui), rationale figée dans le docblock de `LadderBoard.tsx`, **ne pas re-ticketer avant qu'un ladder dépasse la centaine**. 🔑 **Défaut invisible trouvé au passage** : à 375 px le nom du compétiteur était rendu **0 px de large** (tracks fixes + gaps + padding = 268 px d'une boîte de 276, avatar `shrink-0`) et **73 px sortaient EN SILENCE** car `LadderBoard` clippe — le check de largeur ne pouvait pas le voir, il mesurait `documentElement` et était **vert par construction**. Corrigé **le check d'abord** (`L9b` mesure la boîte ET la largeur rendue du nom), le défaut ensuite : sous `sm` les 3 nombres passent sous le nom via **`sm:contents`**, au-dessus la grille à 5 colonnes est restaurée à l'identique. Extractions (règle du second usage) : `LadderRow` + `LadderBoard` sortent de `LadderExcerpt`, helpers dans `lib/ladders.ts`, `ui/error-panel.tsx` extrait._

---

_28 juillet 2026 (soir) — **[FX-MIDNIGHT] mergé** (commit `2f4ec5c`, merge `4d07715`) — **fin du lot de fix**. Dans `CreateMatchPanel`, `nowMs` est figé à l'ouverture (volontaire : un `now` qui bouge ferait disparaître l'option qu'on s'apprête à cliquer) et n'est rafraîchi que sur le 400 « ce créneau vient de passer ». Panneau resté ouvert **au passage de minuit** → la valeur `day` en mémoire (minuit de la veille) ne correspond plus à aucune `<option>` régénérée : le menu affichait un jour, le formulaire en retenait un autre, la liste d'heures sortait vide. ⚠️ **Écart assumé vs la carte, validé en review** : elle demandait de remettre `day` sur le premier jour utilisable **inconditionnellement** ; c'est **conditionnel** (seulement si la valeur n'est plus listée), sinon un capitaine ayant choisi « dans 3 jours » serait ramené à aujourd'hui à **chaque** 400 de quart expiré — cas courant et sans rapport avec minuit. Le reviewer confirme : l'inconditionnel aurait introduit une régression. ⚠️ Piège : `days`/`firstUsableDay` du rendu courant dérivent de l'**ancien** `nowMs` (le `setNowMs` ne prend effet qu'au rendu suivant), d'où le recalcul local. **Prouvé sans UI** en exerçant la vraie `slotDays`/`slotTimes` dans le navigateur via le serveur Vite, à cheval sur minuit : valeur retenue absente de la liste régénérée (la prémisse), repli listé avec 94 quarts disponibles, et **contrôle négatif** — sans franchir minuit, rien n'est touché. 📌 **Un check d'audit de ce cas est hors de portée**, et le reviewer a dit pourquoi : `routes/matches.ts` compare à un `Date.now()` **serveur**, que `page.clock` de Playwright ne truque pas. `npm run audit teams-matchmaking` reste à 16/16._
<br>

_28 juillet 2026 (soir) — **[FX-FOCUS] mergé** (commit `f4f98a2`, merge `e22bef0`). Quand une action confirmée détruit l'élément qui portait son bouton, le `<dialog>` natif n'a plus rien à qui rendre le focus et le lâche sur **`<body>`** : l'utilisateur au clavier est renvoyé en haut de page. **Une seule stratégie pour toutes les occurrences** : prop `returnFocusRef` sur `ConfirmDialog`, et la règle « **l'élément survivant le plus proche qui NOMME la liste** » — titre `Roster` (kick, annulation d'invitation), région `Match history` du tableau (annulation de créneau depuis l'onglet Matches), panneau d'onglet en repli quand aucun titre ne survit (Overview, où la section « Next match » disparaît en entier). 🔑 **Le composant ne prend la main que si le navigateur a échoué** : sur les actions dont le déclencheur survit (renommer, dissoudre), la restauration native est laissée strictement intacte. ⚠️ **Défaut mesuré en review, et instructif** : ma 1re détection lisait `document.activeElement === body` après `close()` — or Chrome ne bascule sur `<body>` que **~60 ms plus tard**, et si ça marchait c'est uniquement parce que la phase `pending` désactive le bouton et provoque le blur avant. La détection dépendait donc d'une **prop sans rapport**, et le premier appelant sans `pending` aurait eu un no-op silencieux. Remplacée par : on retient l'élément déclencheur à l'ouverture et on teste **`isConnected`** — vrai ou faux immédiatement, sans dépendre du timing. ⚠️ **Régression que ce ticket avait introduite, trouvée en review** : `cancelledSlotAt` n'était jamais remis à zéro et passait devant `openedSlotAt`, donc après une annulation la région live restait bloquée et **contredisait** la bannière visible — un ticket d'a11y qui casse une annonce d'a11y. D'où **`lib/use-announcement.ts`** : une région = un message, avec `reset()`, et le re-post sur la frame suivante (un texte **identique** deux fois de suite n'est pas ré-annoncé — exclure @x, le réinviter, le ré-exclure était muet la 2ᵉ fois). 🔑 **Règle qui en sort : UNE seule région live par écran.** En en ajoutant une sur `/teams` j'en avais deux (le `Callout` de création portait déjà `role="status"`), ce qui a cassé 2 scénarios lisant `[role="status"]` en `.first()` — les `Callout` sont redevenus purement visuels. Et `teams-states.mjs` attendait qu'une région **apparaisse** pour se synchroniser : montée en permanence, ce `waitFor()` ne bloquait plus, il attend maintenant qu'elle **porte le nom de l'équipe**. **4ᵉ occurrence corrigée, hors carte** : sur `/teams`, répondre à une invitation supprime la ligne et ses boutons **sans même un `<dialog>`** ; refuser la **dernière** démonte le bloc entier, donc ni son titre ni sa région ne survivent — la page prête les siens via une prop `host` optionnelle (le composant reste utilisable sans props, sa promesse « déposable sur `/profile` » est intacte). **Vu ROUGE avant vert** (correctif neutralisé → `<BODY>`). **Les 5 nouveaux checks passent par le CLAVIER** (`focus()` + Entrée, jamais un clic — la DoD l'exigeait), et assertent le **rôle, le nom accessible et la phrase exacte** : `tag === 'DIV'` était satisfait par n'importe quel conteneur. `focusLanding()` et `pressEnterOn()` vivent dans `runner.mjs`. **Audit complet : exit 0, 9 scénarios, 112 checks, 0 entrée.** ⚠️ Une passe intermédiaire a sorti 21 `ERR_CERT_VERIFIER_CHANGED` sur des URL du serveur Vite : **bruit d'environnement** (cert auto-signé rechargé en cours de run), disparu à la relance — surtout ne pas l'ajouter à `OUT_OF_SCOPE`._
<br>

_28 juillet 2026 (soir) — **[BX-DEL] mergé** (commit `392dfb3`, merge `607a63a` ; + `7066037`/`f6ba488` pour le nettoyage de l'audit). `DELETE /users/me` rendait un **500 opaque** pour tout joueur ayant été aligné **une seule fois** dans un match, même terminé depuis des mois : `match_participants.user_id` était en `restrict` **sans condition de statut**, la contrainte remontait en 23503 et tombait dans le `catch` générique. **Migration `0023`** : la FK passe en `cascade` — ce qui compte (score, vainqueur, deltas d'Elo) vit sur `matches` / `match_sides`, seule la ligne « qui était aligné » part. 🔑 **Une contrainte de FK ne sait pas lire un statut** : la règle produit vit dans la route, jamais dans le schéma (le TODO « refuser ou anonymiser » de `docs/schema.md` est tranché ainsi). **Deux refus en 409, deux `code` distincts parce que les remèdes diffèrent** : `engaged_in_match` (aligné dans un match `pending`/`in_progress`/`awaiting_confirmation`/`disputed`) et **`captain_of_team`**. ⚠️ **Le second vient de la review et c'était un bloquant** : `teams.captain_id` est en CASCADE, donc le départ d'un capitaine **effaçait son équipe**, son roster, sa ligne `rankings` (Elo, W/L, place au ladder) et l'identité de l'adversaire dans son historique (`opponent: null`), **sans notifier personne** — les coéquipiers découvraient la disparition par un 404. Sur `master` le `restrict` freinait ce chemin **par accident** ; passer en `cascade` sans cette garde l'aurait ouvert à **tous** les capitaines. 🔑 **Pendant obligatoire** : `DELETE /teams/:id` refuse désormais de dissoudre une équipe engagée (**`team_engaged_in_match`**, sous le verrou existant) — sinon le refus était contournable en un appel (dissoudre puis partir), et le reviewer l'avait poussé jusqu'au bout : slot orphelin accepté par un tiers, match joué contre un camp fantôme, **Elo d'une vraie équipe corrompu**. Ça solde au passage la dette « slots orphelins à la dissolution » ouverte à la review de B5b. **Décision produit verrouillée par David** : « si tu veux delete, il faut annuler tes matchs en cours » → parcours de sortie **annuler → dissoudre → supprimer**, testé de bout en bout (le risque de cette option, c'est le cul-de-sac). Autres correctifs de review : avatar supprimé **après** le `DELETE` (même ordre que la dissolution d'équipe — sinon un échec tardif laisse un compte vivant avec un avatar détruit) ; requête de garde repartie de `match_participants` filtré sur l'user (**535 buffers → 3** à l'`EXPLAIN`, le coût suit le compte et non le nombre de slots du ladder) ; `ENGAGING_STATUSES`/`LOCKING_STATUSES` extraites dans `utils/match-status.ts` et **typées depuis `matchStatusEnum`** (renommer un statut casse la compilation). **Vu ROUGE avant vert** : contrainte remise en `restrict` en SQL → `❌ attendu 200, reçu 500`, user toujours en base. Nouvelle suite `test_users_deletion.py` (**30 cas**, dont les 4 statuts engageants et la preuve que match/camps/vainqueur/score/Elo survivent à une suppression) ; suite complète **788 ✅ / 0 ❌**. Le runner de l'audit console suit maintenant le même ordre de sortie → `teams-matchmaking` ne laisse **plus aucun compte** (43 comptes historiques purgés). **Non ticketé, décision David** : la course `DELETE /users/me` × `POST /matches` (reproduite 14/20 par le reviewer) — « cas trop complexe pour un projet d'école », voir [[feedback-skip-exotic-races]]._
<br>

_28 juillet 2026 (soir) — **[B13] mergé** (commit `fd69624`, merge `b499833`) : `POST /auth/refresh` rend **204** quand il n'y a pas de cookie, 401 quand il y en a un mais qu'il est refusé. Le front appelait cette route à l'ouverture de **chaque page anonyme** pour tenter de restaurer une session : le 401 faisait écrire une ligne rouge **par Chrome lui-même**, donc ineffaçable par un `try/catch`, avant le moindre clic du correcteur — et « zéro erreur console » est un **motif de rejet du projet**. Côté front, **deux gardes ÉCRITES** (`refreshAccessToken` dans `lib/api.ts`, placée **avant** `!response.ok` puisqu'un 204 est un 2xx ; `restoreSession` dans `stores/auth-store.ts`) : sans elles le code « marchait » en jetant un TypeError avalé par le `catch` — l'état final était correct **par accident, via un chemin d'erreur**. L'exemption `auth/refresh` est **retirée** de `OUT_OF_SCOPE` (`runner.mjs`), donc c'est désormais l'audit qui garde le contrat : ramener le 401 fait tomber les trois scénarios anonymes. ⚠️ **Trou trouvé en review et fermé dans le même ticket** : un cookie **présent mais refusé** (rotation du `JWT_SECRET`, changement de format de token, compte supprimé ailleurs) laissait une ligne rouge **PERMANENTE** — le front vide son état local mais n'appelle jamais `/auth/logout`, le cookie mort survivait ses 7 jours de TTL et Chrome réécrivait son 401 à chaque chargement, sans recours pour l'utilisateur (déconnecté, donc plus de bouton Logout). La route **purge** maintenant le cookie sur les refus → le chargement suivant retombe sur le 204 silencieux, c'est auto-guérissant. 🔑 **Mais la vérification JWT a son propre `try`** : un refus est définitif (on purge), une **panne de base ne l'est pas** (on ne purge surtout pas, sinon une coupure passagère déconnecte durablement tout le monde) — le `catch` général ne sait pas distinguer les deux. Mesuré : `set-cookie: refresh=; Max-Age=0` sur cookie illisible, **rien** sur cookie valide (200). `helpers.req()` accepte un paramètre **`cookies`** et `test_auth_contract.py` fige les deux sens (**35 ✅ / 0 ❌**) — sans ça rien n'empêchait de « simplifier » un jour le 401 du cookie invalide en 204. **Audit console complet : exit 0, 9 scénarios, 107 checks, 0 entrée.** `openapi.yaml` + `api-types.gen.ts` faits. **Dettes signalées en review, NON traitées** : `requestAuthJson` rend `undefined as T` sur un corps vide (mensonge de signature, le prochain 204 retombera dans le trou sans commentaire pour prévenir), `db.select()` nu sur `usersTable` dans `/refresh` (pré-existant, invariant #6), et `frontend/.tanstack/` root-owned qui fait échouer `npm run build` **sur l'hôte** (passer par `docker compose exec frontend`)._
<br>

_28 juillet 2026 (soir) — **[B12] mergé** (commit `531344f`, merge `487036b`) : le compteur de `POST /auth/2fa/verify` est désormais indexé sur le **compte visé par le tempToken**, plus sur l'adresse. Il l'était par IP parce que la route n'utilise pas `server.authenticate` (elle vérifie son tempToken elle-même) : le `keyGenerator` global ne trouvait rien dans `request.user` et repliait sur l'IP — 5 essais/min bornaient une *adresse*, et un pool de 500 IP ramenait le brute-force des 6 chiffres à ~1 h 30. **Montage final à deux étages** : plancher par IP à **30/min** en `onRequest` (le seul qui voit les corps illisibles, qui n'atteignent jamais le handler) + compteur **par compte à 5/min** appelé à la main dans le handler via `server.createRateLimit()`. ⚠️ **Deux pièges tranchés en review, tous deux invisibles sans mesure** : (1) la 1re livraison déplaçait le comptage du plugin en `preValidation` pour lire le corps — mesuré, **100 POST de 500 Ko de JSON invalide en 1 s passaient sans AUCUN comptage**, une route qui déclare `config.rateLimit` sortant du quota global, il ne restait plus aucun filet ; (2) monter le second compteur comme un hook via `server.rateLimit()` le **désactive silencieusement** — `rateLimitRequestHandler` ouvre sur `if (req[rateLimitRan]) return` et ce symbole est **partagé** par tous les compteurs, le premier hook qui tourne neutralise les suivants ; seul `createRateLimit()` échappe à la garde. ⚠️ **Le repli `ip:` du compteur par compte n'est volontairement PAS appliqué** (3ᵉ correction) : sans `trustProxy`, `req.ip` est l'IP du **conteneur front** pour tous les navigateurs, donc un bucket IP à 5/min sur cette route est **commun à toute la plateforme** — 5 tempTokens périmés dans la minute (cas légitime, le tempToken vit 5 min) et le 6ᵉ utilisateur lisait « trop de tentatives » au lieu de « session expirée ». Ces requêtes sont laissées au plancher à 30/min. **Preuve rejouable** dans `test_auth_contract.py` (nouveau `helpers.forge_temp_token()`, **aucun user créé** : un tempToken forgé sur un uuid inexistant rend 401 tout en étant compté) — ⚠️ c'est l'assertion « un autre compte, même IP → 401 » qui discrimine, l'autre serait verte sur master aussi. Suite complète **755 ✅ / 0 ❌**, Vitest **40/40**. `openapi.yaml` : 429 documenté + schéma **`RateLimitError`** (le corps d'un 429 vient du plugin, `{statusCode, error, message}` — ce n'est **pas** le schéma `Error`, que `/users/me/password` référence à tort, dette pré-existante non traitée). **Carte de suite créée : [B12B]** https://trello.com/c/xC70Wqjf — `/2fa/enable` et `/2fa/disable` vérifient aussi un code à 6 chiffres mais héritent du global à **100/min**, 20× le quota de `/verify`._
<br>

_28 juillet 2026 (suite) — **[FT-2C] mergé** (commit `1d74b90`, merge `f0e2369`) : ouvrir et annuler un créneau de match depuis `/teams/$teamId`. **Le cycle challenge/accept devient atteignable à la souris** — le back était complet depuis B5b/B6 mais inaccessible sans curl. Panneau **déroulé sous l'en-tête** (pas une modale : `ConfirmDialog` est une boîte de confirmation, pas un conteneur de formulaire), deux menus jour + quart d'heure (**pas de champ libre** : la carte l'exigeait), composition à cocher à la taille exacte du format, joueur sans compte lié grisé et expliqué. **`npm run audit` : 9 scénarios, 107 checks, exit 0.**_
<br>
_**Le cœur du ticket : le lockout est pré-empté CÔTÉ CLIENT.** Le back refuse un créneau `t` quand un match engageant `s` vérifie `|t − s| < lockoutMinutes` — **inégalité stricte**, donc en 5v5 (lockout 60) un match à 21h grise 21h30 mais **laisse 20h et 22h ouverts** : deux fenêtres qui se touchent ne se chevauchent pas, c'est le cas d'usage de l'enchaînement de soirée. Les créneaux en conflit sont **désactivés dans le menu** au lieu de partir en 409. Découverte qui a rendu ça possible : **`lockoutMinutes` était déjà exposé par `GET /ladders`**, c'est le type **écrit à la main** de `lib/games.ts` qui l'omettait — avant de conclure qu'une donnée manque au front, vérifier si c'est la route ou seulement notre type local. **Les deux 409 de `POST /matches` n'ont aucun `code` stable** (contrairement à B-INV) : départagés par un **compteur local** de slots ouverts, jamais par la prose (invariant #8)._
<br>
_**Trois leçons de la review.** (1) **« Pas chargé » n'est pas « vide »** : `matches ?? []` éteignait les deux pré-emptions **en silence** et faisait affirmer la mauvaise cause au message d'erreur — l'inconnu doit rester `undefined` et se dire à l'écran. (2) **Une région live doit exister AVANT son texte** : un `role="status"` inséré avec son contenu n'est pas annoncé de façon fiable. (3) Un `<span class="sr-only">` est en `position:absolute` et **n'est clippé que par un ancêtre positionné** — dans un conteneur `overflow-x-auto` resté `static`, il se résolvait contre le bloc conteneur initial et provoquait **253 px de débordement du document à 375 px** (trouvé et corrigé par l'auteur en cours de ticket)._
<br>
_**Deux règles CSS GLOBALES ajoutées à `index.css`, assumées.** `color-scheme: dark` — sans elle la liste déroulante native d'un `<select>` sort blanc sur blanc, donc le sélecteur d'heure est illisible ; bénéfice collatéral sur les scrollbars et l'autofill. `scrollbar-gutter: stable` — **bug trouvé par David à l'œil** : en passant d'Overview à Matches à Manage, le rail central « respirait ». Cause : la colonne est en `flex-1` donc **fluide**, et la barre de défilement apparaissait/disparaissait selon la hauteur de l'onglet. ⚠️ **Conséquence chiffrée : la colonne centrale passe de 616 à 601 px à 1280, en permanence** — à budgéter dans **[FT-3]** (table large). Bénéfice collatéral vérifié par la review : `showModal()` posait `overflow:hidden` sur le document et faisait sauter la page à chaque ouverture de modale, la gouttière réservée supprime aussi ce saut ; et sur un vrai mobile la règle ne coûte rien (les scrollbars overlay l'ignorent par spec)._
<br>
_**Trouvaille hors périmètre, à ticketer** : **`DELETE /users/me` répond 500 dès qu'un joueur a été aligné dans un match.** `match_participants.user_id` est en `onDelete: 'restrict'` **sans condition de statut** (`schema.ts:319`) et la route fait un `delete` nu, donc la 23503 tombe dans le `catch` générique. **Un joueur ayant joué une seule fois ne peut plus jamais supprimer son compte** — inacceptable, et le module User management est revendiqué pour la soutenance. Règle voulue (David, 28/07) : refus **seulement** si engagé dans un match `pending` / `in_progress` / `awaiting_confirmation` / `disputed`. Symptôme quotidien : chaque `npm run audit` laisse 2 comptes `audit…@example.com` en base. **Dette front laissée** : le focus retombe sur `<body>` après une action qui supprime son propre bouton — annulation de slot, mais **aussi** kick et annulation d'invitation depuis FT-2B/FT-INV, donc une carte pour les trois._

_28 juillet 2026 — **[B-INV] et [FT-INV] mergés dos à dos** (B-INV : commit `ad97df0`, merge `a724fe4` · FT-INV : commit `0108084`, merge `ab9a4c6`). Rejoindre une équipe n'est plus un **ajout forcé** : `POST /teams/{id}/members` est **supprimée**, remplacée par inviter / annuler (capitaine) et accepter / refuser (l'invité) + `GET /teams/invitations/me`. ⚠️ **Les deux branches devaient être mergées ensemble** — B-INV seul supprime une route que le front de FT-2B, déjà sur `master`, appelait encore : `master` n'aurait pas buildé. C'est pour ça que `feature/ft-inv-team-invitations` a été branchée **sur B-INV** et non sur `master`._
<br>
_**Back** : table dédiée `team_invitations` (migration `0022`), **4 statuts** — `cancelled` couvre l'annulation par le capitaine **et** l'annulation automatique quand le joueur accepte ailleurs, sinon le capitaine croirait à un refus. **Index unique partiel** sur les seules `pending` : c'est ce qui permet de réinviter après une annulation. Plafond `membres + en attente ≤ 10` vérifié à l'invitation **et re-vérifié sous verrou** à l'acceptation. **Trois défauts de concurrence trouvés en review et corrigés** : interblocage à l'acceptation croisée (deux équipes du même ladder acceptant le même joueur — 500 au lieu de 409, reproduit 8/8, corrigé par des verrous consultatifs **triés** `team:` + `user:<id>:<ladder>`), interblocage dissolution × acceptation (le **capitaine** recevait un 500 sur une dissolution légitime), et invitation zombie à la création d'équipe. Suite `test_teams_invitations.py` → **20 suites e2e / 753 cas**. **Piège #21** consigné dans `docs/pieges.md` : une barrière de synchronisation ne teste qu'**un seul point d'alignement**, il faut **balayer un décalage** (celui-ci tombe à 5 ms et **ne sort jamais** avec une barrière seule) ; et une route peut écrire une table sans que ça se voie dans son code (`ON DELETE CASCADE`)._
<br>
_**Front** : « Add a player » → « Invite a player », puces **« Pending »** dans `RosterChips` (même `<ul>`, **annuler ≠ exclure**), compteur **`Roster slots`** incluant les invitations, `excludeIds` élargi aux invités, bloc « Team invitations » (Accept / Decline) sur `/teams`, `components/ui/callout.tsx` extrait au second usage. **`npm run audit` : 8 scénarios, 92 checks, exit 0.** **Le mapping d'erreurs passe de la prose serveur aux `code` stables** (`TeamInvitationError`) — l'ancien code affichait le message serveur *verbatim* faute de code, ce n'est plus une excuse. **Trois invalidations de cache volontairement DIFFÉRENTES** : inviter/annuler → `['team', id]` en `exact: true` **sans** les matchs (un invité n'est dans aucune compo) ; **accepter → par PRÉFIXE**, parce que `isMember` de `/teams/{id}/matches` bascule pour moi ; refuser → `['team-invitations','me']` seul. **Écart assumé vs la carte** : la réponse aux invitations vit sur **`/teams`** et non sur `/profile`, qui est un stub — le composant est sans props et fait sa propre query, donc remontable tel quel plus tard. **Rail social hors périmètre** (FS-0→5). **Deux compteurs différents assumés** : l'en-tête compte les **membres** (il est public, un visiteur ne peut pas compter ce qu'il n'a pas le droit de voir), l'onglet Manage compte le **plafond** — l'ambiguïté est levée par le mot « slots », pas par un chiffre faux. **7 findings de `reviewer-front` corrigés avant commit**, dont un vrai bug d'état (bandeau de succès et erreur qui survivaient d'une invitation à l'autre) et deux duplications de classes Tailwind qui ont déclenché l'extraction de `Callout`. **Leçon de méthode** : la passe a duré 37 min, presque entièrement dans `npm run audit` — `run.mjs` accepte **un nom de scénario en argument**, on filtre pendant l'itération et on ne lance la suite complète qu'à la fin. **Dette laissée** : contraste `text-text-muted` à 4,48:1 sur la puce en attente (pas une régression, une occurrence de plus sur la dette design system), focus retombant sur `<body>` après annulation d'une puce, et pas de temps réel sur le roster (c'est le rail social)._

_27 juillet 2026 (soir) — **FT-2B mergé** (commit `d7c6f93`, merge `6746e81`) — gestion capitaine sur `/teams/$teamId` : onglet Manage réservé au capitaine, `Leave team` pour un membre, rien pour un visiteur. Codé en **deux passes** `coder-front` sur Opus — socle (`ConfirmDialog` en `<dialog>` natif, `lib/team-mutations.ts`, `SearchBar` → `components/search/UserSearch.tsx` recâblé sur `GET /search`) puis UI (`TeamManage` + 3 sections, prop `actions` sur `TeamHero`, prop `onKick` sur `RosterChips`, scénario `teams-manage.mjs`). **`npm run audit` : 7 scénarios, 71 checks, exit 0.** ⚠️ **Le module File upload devient démontrable** — c'était la dernière puce manquante des 16 points. **Deux décisions produit prises en cours de ticket, en écart avec la carte** : la recherche « add member » passe en **préfixe** (la contrainte « pseudo exact » décrivait l'ancienne route `/users/{pseudo}`) et le bouton **`Edit team` de l'en-tête est retiré** (l'onglet Manage n'est visible que du capitaine). **Défauts trouvés par `reviewer-front` et corrigés avant commit** : après une dissolution, le bouton **Précédent** ramenait sur l'équipe morte → 404 en console (corrigé par `replace: true` — le scénario ne faisait jamais `goBack()` après dissolution, il ne pouvait pas le voir) ; un double-clic sur un résultat de recherche envoyait deux `POST /members`, le second en 409 → message d'échec sur un ajout réussi **et** ligne rouge ; les pseudos s'affichaient en MAJUSCULES (`buttonClasses` porte `uppercase`), trompeur pour un identifiant sensible à la casse. **Sujet ouvert, non ticketé** : rejoindre une équipe est aujourd'hui un **ajout forcé** (`INSERT` direct, aucune colonne de statut sur `team_members`) — David veut une **invitation** `pending`/`accepted`/`declined` ; le point dur est que `team_members_user_ladder_unique` ne doit pas s'appliquer aux invitations en attente, sinon on déplace le problème. **Décidé de NE PAS faire** : la pagination de l'historique de matchs (aucun module ne l'exige, le volume est borné par équipe, et la bande « Last 5 » comme le bloc « Next match » dérivent de la même requête). ⚠️ Commit et merge à enregistrer ici._

_27 juillet 2026 — **B14 puis B15 mergés** (`76ab34c` score Bo3 + delta d'Elo par match ; `a999cad`, merge `3d79f19`, `GET /teams/{id}/matches` + garde de `GET /matches/{id}` relâchée sur un match `completed` + `competitor.id` dans `shapeRankings()` — suite `test_teams_matches.py`, 53 cas, total e2e 647). **FT-2 découpé en trois** : [FT-2A] consultation (carte d'origine réécrite), [FT-2B] gestion capitaine, [FT-2C] créer/annuler un match ; plus [FT-3] page d'un ladder, née du lien « See the full ladder ». **FT-2A mergé** (commit `478c62b`, merge `9ef6406`) : page réécrite de zéro, 3 hooks Query, divulgation progressive visiteur/membre vérifiée sur trois vrais comptes, 3 routes placeholder, `pill`/`section-title`/`tabs` extraits dans `components/ui/`. **`npm run audit` sort 0 sans filtre pour la première fois** (6 scénarios, 48 checks) grâce à `expectHttp()` ajouté au harnais — trois scénarios antérieurs (FR1, FR2, FT-1B) comptaient contre eux-mêmes un 401/409 qu'ils provoquaient volontairement. Défauts trouvés par la review agent et corrigés avant commit : contraste des pastilles sous AA (l'`opacity-70` de la ligne éteignait la pastille de litige, 3,07:1 → 4,87:1 ; pastille bleue 4,32 → 4,96:1 via un token dédié), table mobile injoignable au clavier, lien imbriqué dans un `<h2>`, et `expectHttp` qui exemptait tous les flux au lieu du seul réseau. Dette laissée, mesurée : `text-text-muted` sur carte à 4,23:1 (45 usages, 25 fichiers → ticket design system) et 262 px hors champ à 375 px sur l'historique._

_23 juillet 2026 (soir) — **AUDIT BACKEND vs sujet RÉEL (`trans.subject.pdf` = v21.1 « Surprise », PAS v20).** Les 11 modules revendiqués existent tous en v21.1 avec les points annoncés (5 majors + 6 minors = **16 pts**, seuil 14). Confirmé contre le PDF, pas contre un résumé. **Tickets sortis de l'audit** — ✅ **LES 4 SONT FAITS ET MERGÉS (24/07)** : ~~(1) **File upload back**~~ → **mergé** (`e187480`) : `DELETE /users/me/avatar` + scission `IMAGE_MIME`/`EVIDENCE_MIME` (PDF sur l'evidence) — détail dans la section modules ; ~~(2) **tokens JWT typés**~~ → **fait** (claim `type` aux 4 points de vérification, **le WS `/ws/chat` en faisait partie et n'était pas dans le périmètre d'origine** — détail dans Auth & user) ; ~~(3) **dédup `GET /me`**~~ → **fait** : `GET /auth/me` supprimé (handler strictement identique à `GET /users/me`), front `restoreSession` bascule sur `/users/me`, entrée retirée d'`openapi.yaml` + `api-types.gen.ts` régénéré + requête morte retirée de la collection Postman ; ~~(4) **valider les params d'URL**~~ → **fait** : 9 points d'appel (`blocks` ×2, `ladders` ×2, `friends` ×3, `messages` ×1, `games` ×1) passés au `.parse()` Zod — **8 × 500 → 400** mesurés en live, aucun 404/200 nominal déplacé (détail et invariant dans « Règles transverses » ci-dessous). **Points OK confirmés, rien à coder** : online status (fourni par le WS présence, le rail social front le consommera), Notifications (couverture large, « jamais l'acteur »), openapi (régen byte-identique + front `tsc` vert). ⚠️ Rappels rejet-projet (hors back) toujours ouverts : Privacy/ToS non-placeholder, 0 warning console, README complet (rôles PO/PM/Tech Lead/Devs, calcul de points, qui-fait-quoi, usage IA). Rapport détaillé gardé HORS repo (comme l'audit du 05/07)._

_Dernière mise à jour : 23 juillet 2026 — **I4 (origine unique HTTPS + proxy `/api`/`/media` + cycle de vie du cert + validation d'env) mergé sur `master`** (merge `b2800eb`, commit `fe585b6`). Le navigateur ne connaît plus qu'une origine `https://localhost:5173` ; plus de `:3000`/`:9000` côté front, plus de contenu mixte ; cookie refresh `Path=/api/auth` (invariant proxy/back/front), migration `0020` appliquée. Recettes automatisée (7/7) + navigateur Chrome (register + Google + F5 restaure la session) vertes. **La dette cross-scheme F0-C est résolue** — carte https://trello.com/c/wvEfFTVq à passer en Done._

_22 juillet 2026 — **alignement Node 24 mergé sur `master`** (merge `cc9141c`, commit `21b6a19`) : `.nvmrc`, engines frontend/backend et lockfiles sans bump de dépendance ; validation finale Node `v24.18.0`, backend 21/21, frontend build/lint à 0 warning. **F0-C est mergé** (`9d3b97c`) et sa dette HTTPS/cookie est isolée dans **I4** (https://trello.com/c/wvEfFTVq)._

_21 juillet 2026 — **B7 (disputes) mergé** (`15238e7`) ; **B9 (notifications) mergé** (`88c33e8`, commit `eca332d` — 12 suites / 397 cas, Vitest 19/19, push live vérifié) ; **FR2 Login + 2FA mergé après review** (commit `26f908d`, parcours classique et TOTP testés avec le backend réel ; observation cookie cross-scheme dev documentée ci-dessus). **BA1 (changer son mot de passe) mergé** (merge `37c4e8c`, commit `9c1ae0b` — review Walid approuvée) : rebasé sur `master` `85e1c76` **sans conflit** (aucun commit de master n'avait touché `users.ts` ni `openapi.yaml` depuis son point de départ), `tsc --noEmit` vert avant merge._

_21 juillet 2026 — **FL (landing publique) rebasée sur `master` `1c8f7a6`** (trois fois : `88c33e8`, puis le sommet après FR2, puis celui après `b-notif-social` + `get-search` — les 2e et 3e passes n'ont conflicté que sur ce bloc, la 3e résolue automatiquement par `git rerere`). Résolutions retenues : `LeftNav` et le corps de `CLAUDE.md` gardent master, `index.html` garde le titre descriptif FL, `button.tsx` **fusionné** (structure FL + 4 variantes master dont `danger`), les 5 composants F-Nav remis dans `components/home/` (git avait tenté de les emporter par *directory rename detection*). Un commit est devenu vide et a été supprimé par git : il ne portait que `CLAUDE.md`. `tsc -b` et lint verts (2 warnings hérités de master sur `routes/profile.tsx`). **FL est depuis MERGÉE sur master** (merge `85e1c76`) — les points qui restaient « à faire avant merge » sont donc entrés dans master **sans être traités** et sont désormais de la **dette connue**, pas des pré-requis : le texte de debug affiché à l'utilisateur par `GameAsset` (« <jeu> — missing image »), le slogan **« Queue by skill »** du hero qui promet une file d'attente que le produit n'a pas (contredit la décision du 13/07), l'arbitrage `Logo` / `SiteLogo` (deux composants concurrents), et le responsive mobile (nav `hidden md:flex`, overlay `GameInfo` en `group-hover` → inatteignable au tactile). ⚠️ **À ticketer** — rien ne les suit aujourd'hui._

---

## Historique des « Prochaines actions » — bloc déporté de `CLAUDE.md` (refacto du 31/07)

> La section « Prochaines actions » de `CLAUDE.md` pesait **40,4 Ko** : elle avait accumulé le RÉCIT de chaque ticket mergé (FT-3, FT-4A/B, FX-ROW, B-SOLO/F-SOLO, F-GAMES, B-MM/F-MM, le lot de fix du 28/07…) au lieu de ne porter que ce qui reste à faire. Tout est conservé ici verbatim ; `CLAUDE.md` ne garde plus que le point de reprise et les décisions encore ouvertes.

### Prochaines actions

- 🎯 **POINT DE REPRISE — 31/07 au soir.** **[F-DISPUTE] est mergé** (`f6345c8`), la campagne sort **19 scénarios / 329 checks / exit 0**, et **il ne reste plus aucune page vierge ni aucun cul-de-sac dans le rail**. Deux cartes prêtes en Todo, aucune n'est bloquée :
  1. ✅ ~~**[F-DISPUTE]**~~ **FAIT** (voir les lignes Backend et Frontend). 🔑 **Ce qu'il faut en retenir pour la suite** : la carte annonçait « front pur, zéro back » et **c'était faux** — l'échéance qu'elle exigeait était incalculable, et la page attribuait à un arbitre l'annulation automatique du job. **Grep le HANDLER avant de croire une carte sur ce que l'API sert déjà** (même leçon que B-MM, deuxième fois). ⚠️ **Dette assumée, décision de David** : l'en-tête du dossier est la **4ᵉ copie** de l'idiome `GameBanner` + `<h1>` + rangée de méta (après `MatchHeader`, `game-detail`, `ladder-detail`) et le `HeroFrame` promis par F-HOME **n'a PAS été extrait** — le gain est cosmétique, le risque serait de casser trois écrans qui vont bien. **Ne pas le « corriger » en croyant à un oubli.** ⚠️ **Passation pour [F-ADMIN]** : le lien d'en-tête du dossier mène à `/matches/$matchId`, que `GET /matches/{id}` **refuse en 403 à un admin non-participant** sur un match `disputed` — inatteignable aujourd'hui, mais à conditionner **dès que [F-ADMIN] rend cette page atteignable par un arbitre**. Rien d'autre sur cette page n'appelle `/matches/{id}`.
  2. **[F-ADMIN]** (https://trello.com/c/gkuGGusY, front + **1 ligne de back**) — la file d'arbitrage, invisible aujourd'hui. 🔴 **PRÉREQUIS : `isAdmin` n'est PAS exposé par l'API.** La colonne `users.is_admin` existe (`schema.ts:59`) et `frontend/src/types/auth.ts:11` la déclare **déjà**, mais ni `openapi.yaml` ni le handler `/users/me` ne la renvoient — elle vaut `undefined` en permanence. **Aucun contournement** : sonder `GET /disputes` produirait un 403 en console chez tous les non-admins. Ensuite : section « Disputed matches » sur `/home` (conditionnelle, **une requête de plus réservée aux admins**) + les 3 issues de `POST /disputes/{id}/resolve` **sur la page de F-DISPUTE**, pas sur un 2ᵉ écran. ⚠️ Le scénario d'audit aura besoin d'un **compte admin**, absent des fixtures (le runner sait faire un `psql` depuis FT-4B).
  3. **[F-PLAYER]** (https://trello.com/c/AZ3U4BZl, **branche de William déjà à 90 %**) — voir la ligne `page/player` plus bas : il ne reste qu'à retirer la redirection `/profile`, supprimer un composant mort, squasher, et ajouter **ses classements (Elo + V/D par ladder) et ses équipes** en enrichissant `GET /users/{pseudo}`. 🚨 **PAS d'historique de matchs ni de comptes de jeu liés sur le profil d'un inconnu** — décision de David (vie privée) : ça se consulte sur **son propre** compte (`/history`, `/profile`). **Ne pas le reproposer.**

- ✅ **LOT DE FIX TERMINÉ (28/07)** : les 5 cartes de *Issues / bug fixes* sont mergées — **[B12]** (rate limit 2FA par compte), **[B13]** (204 sur `/auth/refresh`), **[BX-DEL]** (suppression de compte, **migration `0023`**), **[FX-FOCUS]** (focus rendu après une suppression), **[FX-MIDNIGHT]** (jour incohérent à minuit). La liste ne contient plus que **[B12B]** (https://trello.com/c/xC70Wqjf), née de la review de B12 : `/2fa/enable` et `/2fa/disable` vérifient un code à 6 chiffres à 100/min, 20× le quota de `/verify`. ⚠️ **Au prochain pull : `docker compose restart backend`** (migration 0023).

- ✅ **[FT-4A] MERGÉ le 29/07** (commit `5729540`, merge `436531e`, **aucune migration**) — `/matches/$matchId` n'est plus un placeholder : fiche **en lecture seule**, les 7 états du cycle, les deux camps face à face (score Bo3, vainqueur, **Elo par CAMP jamais par joueur**), maps pilotées par la donnée, lineups avec capitaine, variante **1v1**, 403/404. L'historique d'équipe rend **toutes** ses lignes cliquables pour un membre (le lien passe sur la **date** quand il n'y a pas de nom d'adversaire). `MatchStatusPill` + `match-status.ts` remontés dans **`components/matches/`** (2ᵉ consommateur), `GameBanner` extrait au 3ᵉ usage, `RosterChips` rendu générique, `Callout` gagne un ton `danger`, `lib/match-detail.ts` porte le hook et les dérivations pures. **⚠️ Au prochain pull : `docker compose exec backend npm run seed:dev`** — le seed a changé, et le scénario `match-detail` **exige** la base semée (sinon `exit 2`).
  - 🔑 **LE PIÈGE À NE JAMAIS RÉINTRODUIRE** : « ce camp n'a pas d'équipe » **ne signifie PAS 1v1**. `match_sides.team_id` est en `set null` et une équipe dont tous les matchs sont terminés **peut** être dissoute — son camp survit alors avec `team: null` sur un 5v5 `completed`, lisible par n'importe quel compte depuis B15. Lire ça comme « solo » renommait le camp d'après son 1ᵉʳ joueur et **supprimait la lineup des 5 joueurs**. C'est le **format du ladder** qui tranche (`isSoloMatch`, enum fermé du contrat). Le libellé de repli est « Disbanded team », **non couvert par une donnée ni un check** → FT-4B ne l'a pas couvert non plus (il faut une équipe dissoute **après** un match terminé pour le voir), reporté au futur ticket « seed propre ».
  - ⚠️ **Un créneau ANNULÉ a la même forme qu'un créneau ouvert** (un seul camp) : la fiche doit lire le **statut**, jamais déduire « encore acceptable » de l'absence d'adversaire. Elle se titrait « open slot » et annonçait « any team can accept it » **sous une pastille CANCELLED**. Bloquant de review, corrigé, gardé par un check **vu ROUGE avant vert** — et c'est pour lui que le seed porte un **7ᵉ** match cs2.
  - ⚠️ **Le seed sème 8 matchs de démo** (7 cs2 + 1 chess 1v1) et le créneau « accepté, avant l'heure » est à **+2 JOURS**. Il était à +1 h : l'état cessait d'exister une heure après le seed et la campagne partait en `exit 2` l'après-midi. **Ne pas raccourcir** — rien ne plafonne l'avance côté API.
  - ⚠️ **`backend/openapi.yaml` a été modifié par un ticket FRONT** : le payload de B16 ne déclarait aucun `required`, la codegen sortait donc tout en optionnel. Vérifié en review — les `required` décrivent exactement ce que le handler renvoie, aucun handler touché, codegen byte-identique.
  - 🔑 **Deux checks ne gardaient rien**, trouvés en 2ᵉ review : l'un comptait 2 nœuds (`text=Cancelled` matchait la pastille **et** le mot dans la phrase) et serait resté vert en supprimant ce qu'il surveille ; l'autre lisait un attribut avec `getAttribute()`, qui **lève** quand rien ne matche → `exit 2` (harnais en échec) au lieu d'un rouge imputable au ticket. **Compter avant de lire.**

- ✅ **[FT-4B] MERGÉ le 29/07** (commit `c2f9019`, merge `8a39a92`, **aucune migration**, https://trello.com/c/3mjx0M31) — la fiche de match cesse d'être en lecture seule : **soumettre son score, confirmer celui de l'adversaire, le contester**. Le cycle challenge/accept est désormais bouclable **entièrement à la souris**, de l'ouverture du créneau à l'Elo appliqué. Livré : `components/matches/MatchResultPanel.tsx`, `lib/match-mutations.ts`, `lib/match-result-schema.ts`, `components/ui/option-tile.tsx` (extrait de `CreateMatchPanel` au 2ᵉ usage), `canReportResult` dans `lib/match-detail.ts`. **Le panneau ne rend RIEN à qui ne peut pas agir** (visiteur, banc, match hors des statuts scorables, camp ayant déjà soumis) et **aucun contrôle avant le coup d'envoi** — un bouton que l'API refuserait laisse une ligne rouge en console, motif de rejet.
  - 🔑 **`nowMs` vient de `matchQuery.dataUpdatedAt`, plus d'un instantané au mount** : une horloge figée gardait le formulaire caché tant que l'onglet restait ouvert, rechargement pour seul remède. ⚠️ Mais elle avance sur un **refetch**, pas sur l'horloge murale — un onglet resté au premier plan à travers le coup d'envoi n'ouvre toujours pas le formulaire tout seul. C'est assumé (`refetchOnWindowFocus` suffit : on rapporte des parties jouées **ailleurs**, donc l'onglet est quitté puis repris), et un `nowMs` en retard ne peut que **cacher** le formulaire, jamais offrir un bouton que le serveur refuserait.
  - 🔑 **`sql.mjs` : le runner d'audit sait désormais faire un appel système** (`docker compose exec postgres psql`, `spawnSync` sans shell, uuid passés par `assertUuid`). C'était la capacité manquante : reculer `scheduled_at` est la **seule** opération de la fixture que l'API ne sait pas faire, et sans elle aucun match ne peut être scoré dans un test. Le scénario tourne sur un ladder **1v1** (2 comptes jetables au lieu de 10).
  - ⚠️ **`R11b` garde un ORDONNANCEMENT, et c'est pour ça qu'il existe séparément de `R9`** : `ConfirmDialog` ne retombe sur `returnFocusRef` **que si son ouvrant a quitté le DOM** (`opener.isConnected`) — sur le chemin « Confirmer », la restitution de la plateforme court contre le démontage du bouton par le refetch. Mesuré vert 4 fois de suite, jamais déduit. ⚠️ **Un `.focus()` appelé sous un `<dialog>` modal est un NO-OP** (la page est inerte) : le commentaire qui affirmait le contraire était faux et a été corrigé.
  - ⚠️ **`awaitFocusRestored` trace désormais le délai réel au-delà de 1 s** (sans faire rougir). Le plafond avait été relevé 5 s → 15 s pendant ce ticket pour une instabilité d'`I6-bis` : à 15 s, « le focus arrive en 200 ms » et « en 9 s » étaient le **même vert**. Constat des deux campagnes complètes : **aucune ligne « ⏱ »**, donc le plafond n'est jamais approché — c'est une marge, pas un cache-misère.
  - ⚠️ **Toujours pas couvert : le libellé de repli « Disbanded team »** (hérité de la passation FT-4A) — ni donnée ni check. Il faut une équipe dissoute **après** un match terminé pour le voir ; à porter au futur ticket « seed propre » (clear complet), pas à re-ticketer seul.

- ✅ **[FX-ROW] MERGÉ le 30/07** (commit `c94d832`, merge `7ee0517`, **aucune migration**) — reviewé par `reviewer-front` Opus (**zéro bloquant**, 10 non-bloquants dont l'essentiel appliqué), build conteneur + lint + `tsc -b` propres, et **campagne `npm run audit` complète rejouée verte (exit 0)**.
  - 🔑 **L'exit code de `run.mjs` est l'agrégat qui fait foi**, pas la lecture du texte : `worst = max(report(...))` sur les 13 scénarios puis `process.exit(worst)`, et `report()` ne rend **0** que si **tous les checks sont verts ET zéro entrée console imputable** (1 sinon, 2 sur crash). ⚠️ **Ne jamais passer la sortie de l'audit dans un `tail`** — ça tronque les scénarios de tête, dont celui du ticket, et il ne reste que l'exit code pour conclure.
  - ⚠️ **Vérifier que la base est semée AVANT de lancer une campagne** : elle était **vide** le 30/07 (0 users / 0 teams / 0 matches, seuls les 9 ladders de référence) et `match-detail` **exige** les 8 matchs de démo → `docker compose exec backend npm run seed:dev`, sinon `exit 2`.
  - **Ce que ça corrige** : dans l'onglet Matches d'une équipe, le nom de l'équipe adverse ouvrait la fiche du **match** — cliquer « Team Bravo » n'amenait pas sur Team Bravo. Désormais **le nom → sa page d'équipe**, **le reste de la ligne → la fiche du match**.
  - 🔑 **La date porte le lien vers la fiche sur TOUTES les lignes ouvrables**, plus seulement celles sans adversaire : c'est l'**accès clavier et lecteur d'écran**. Un gestionnaire de clic sur une ligne n'est ni focusable ni ouvrable dans un onglet — il ne peut jamais être le seul accès. **Ne pas « simplifier » en retirant ce lien.**
  - ⚠️ **Les 4 gardes du clic de ligne, chacune gardée par un check** : élément interactif (`closest('a,button,input,select,textarea,summary,[role=button]')` — à **tenir à jour** si la ligne gagne un contrôle, sinon il est avalé **silencieusement**), sélection de texte par glisser, **clic modifié** (Ctrl/Maj/Alt demande un onglet que `navigate()` ne sait pas donner : la ligne reste **inerte**, le lien de la date l'ouvre normalement). Le **double-clic navigue quand même** — assumé et documenté, le 1ᵉʳ clic part avant que la sélection existe.
  - ⚠️ **Écart non couvert par un check** : le lien du nom est gardé par la présence de l'**équipe**, plus par le droit d'ouvrir la fiche — un **non-membre** gagne donc ce lien là où il avait du texte brut. Correct (une page d'équipe est lisible par tout compte connecté, aucun 403 possible), mais le scénario ne parcourt cette phase **qu'en membre**.
  - 🔑 **3 checks étaient verts PAR CONSTRUCTION avant d'être corrigés** : un comptage sur un locator déjà réduit par `.first()` ; un check pris sur la 1ʳᵉ ligne de l'historique, qui est le **créneau ouvert** (sans adversaire, donc sans cible concurrente — le seul cas que l'ancien code traitait déjà) ; et `waitForURL(/\/teams\/…/)` **rend la main immédiatement** quand l'URL courante matche déjà le motif → passer un **prédicat sur la cible exacte** (`url.pathname === href`). Corollaire vécu deux fois : un check doit sortir **ROUGE**, jamais faire échouer le harnais (`boundingBox`/`waitForURL` **lèvent** → `.catch()`, et chaque check du groupe repart de l'historique via `ensureHistory()`, sinon UN rouge en cascade fait sortir les suivants en `exit 2`).
  - ⚠️ **`TeamOverview` porte le MÊME `aria-label` « Cancel the slot of … »** que la ligne, et l'onglet Overview **reste monté, masqué**, derrière l'onglet Matches : un sélecteur non scopé au tableau attrape ce bouton invisible et le clic **expire à 30 s** (`exit 2`).

- 🚨 **DEUX BRANCHES DE COÉQUIPIERS SE DISPUTENT `/profile` (30/07).** ✅ **Tranché par David : c'est ADRIEN qui gère le conflit sur SA branche** (`feature/f4-profil-page`) — ne pas le traiter à sa place, ne pas modifier `page/player` pour lui. Les deux branches ne partagent **AUCUN fichier**, donc **git mergerait les deux proprement et l'app perdrait silencieusement une des deux fonctionnalités** — aucun marqueur de conflit n'avertira personne. C'est le piège à garder en tête au moment des merges.
  - **`feature/f4-profil-page`** (acattet / Adrien, 3 commits, carte [F4] https://trello.com/c/73sIH2T8) : `/profile` = **page de réglages** (`pages/profile.tsx` + `components/profile/` : ProfileForm, AvatarUploader, PasswordChange, TwoFactorSettings, + `ui/textarea.tsx`).
  - **`page/player`** (Omshinwa / William, `fynmorph@gmail.com`) — ⚠️ **RÉINSPECTÉE LE 31/07, cette ligne était PÉRIMÉE** : la branche ne fait plus 197 lignes mais **1214**, commit courant `c603655` (message « . », à squasher), rebasée sur `25e9086`. 🔑 **Elle NE renomme PLUS la route au singulier** — elle reste sur `/players/$pseudo`, donc **le conflit `tsc -b` annoncé plus bas n'existe pas**. Livré : `PlayerHero` (calqué sur `TeamHero`), `ui/stat.tsx` extrait et partagé, `lib/player-detail.ts` + `lib/player-mutations.ts`, **et un enrichissement BACK de `GET /users/{pseudo}`** (la relation d'amitié entre visiteur et profil, `or` sur les deux orientations du couple, projection explicite). 🔑 **Sa page est DÉJÀ en lecture seule sur son propre profil** — commentaire textuel dans son code : « *Empty for the owner: this page is read-only, editing lives at /profile* », soit exactement le modèle tranché par David le 31/07. **Reste en travers** : `routes/_authenticated.profile.tsx` **redirige toujours** `/profile` → `/players/$pseudo` (le seul vrai conflit avec [F4]), et `ui/avatar-upload-button.tsx` (75 l.) n'est **importé nulle part**. ✅ **À GARDER au rebase** : son `vite.config.ts` ajoute `watch.ignored` sur `**/tests/**` et `**/*.md` — c'est le correctif **structurel** de l'invariant #10 (écrire un fichier sous `frontend/` pendant une campagne déclenchait un full reload et un `exit 2` imputé au ticket). Carte **[F-PLAYER]** créée le 31/07 : https://trello.com/c/AZ3U4BZl
  - ⚠️ **Conséquence selon l'ordre de merge** : William après Adrien → la page de réglages devient **inatteignable** (la route redirige avant de rendre), mot de passe et 2FA disparaissent de l'UI sans qu'une ligne soit supprimée. Adrien après William → la redirection est écrasée et l'upload d'avatar de la page joueur devient redondant.
  - 🔑 **Ça conditionne la carte « suppression de compte + liaison de provider »** : si le modèle William gagne, ces deux fonctionnalités n'ont plus de page évidente.
  - ⚠️ `page/player` n'a été qu'**inspectée, pas reviewée**. 🔑 **Les écarts notés ici le 30/07 sont pour la plupart CORRIGÉS** (réinspection du 31/07) : le `<h1>` **existe** (`PlayerHero.tsx:89`, avec `headingId`), et le code passe par **TanStack Query**, `useAnnouncement`, `ConfirmDialog`, une seule région live. Restent : **aucun scénario d'audit** pour cette page (⚠️ **c'est David qui l'écrira**, retiré de la charge de William) et le commit à squasher. **Ne pas se fier à une inspection ancienne pour juger cette branche — la relire avant d'en parler.**

- 📋 **[F4] REVIEWÉE ET RENVOYÉE À L'AUTEUR le 30/07** (carte repassée en **In Progress**, 5 commentaires postés 1/5→5/5, DoD complétée). ⚠️ **Le commentaire Trello doit être découpé** : le MCP passe le texte en paramètre d'URL, un seul commentaire de ~15 000 caractères sort en **414**. Verdict : **non mergeable en l'état**, mais **la couche métier tient** (les 7 endpoints confrontés un par un à `openapi.yaml` ET aux handlers : tous justes). 5 bloquants — ① les 2 rails (`LeftRail`/`RightRail`) modifiés depuis une base d'avant F-Nav : importent `layout/Logo` (**supprimé**, remplacé par `SiteLogo.tsx`) et pointent `to="/ranking"` (**route supprimée par FT-3**) → `build` ET `tsc -b` cassent, et la perte du `hidden lg:` fait 432 px de chrome pour 375 px d'écran sur **toutes** les pages authentifiées. **La carte interdisait déjà de toucher aux rails** → prendre master en entier au rebase, rien à récupérer. ② `PATCH /users/me/password` sans `skipAuthRefresh` : `lib/api.ts` lit **tout** 401 comme « token mort » → refresh, puis `logoutAfterAuthFailure()` si le cookie est absent → **déconnecté pour une faute de frappe**, et chaque essai part **2 fois** donc 429 dès le 3ᵉ. ③ « Change password » rendu aux comptes OAuth → **400 systématique** offert par l'UI. ④ `AvatarUploader` réimplémente `ui/image-picker.tsx` **sans la barre de progression** (`fetch` n'a pas d'événement de progression : c'est `lib/upload.ts`/`uploadFile` qu'il faut). ⑤ aucun scénario d'audit. 🔑 **Leçon transverse** : la carte disait « réutiliser les composants de **F0** » — liste écrite **avant FT-2B**, donc l'auteur a suivi la carte à la lettre et a quand même dupliqué. **Un brief de réutilisation doit porter l'inventaire À JOUR**, pas un renvoi à un ticket ancien.

- ✅ **[B-SOLO] + [F-SOLO] MERGÉS le 30/07** (merges `e9fce76` puis `8d7dd34`, **aucune migration**) — cartes https://trello.com/c/DruYEIQi et https://trello.com/c/dqViKaCA. Le détail est dans les lignes Backend et Frontend plus haut. Ce qu'il faut retenir pour la suite :
  - 🔑 **La question « enrichir une route existante ou en créer une pour le solo ? » a été tranchée avec un argument reproductible** : `/history` aura besoin de la forme polymorphe de toute façon, donc une route solo-only aurait fait écrire le shaping **deux fois** et serait devenue du poids mort. À réappliquer telle quelle si la question revient (elle reviendra sur `/history`).
  - ⚠️ **Deux checks manquaient à la première livraison de F-SOLO** et ont été ajoutés après review : l'ouverture d'une fiche de match **depuis** l'historique solo (`canOpenSheet: true` était codé en dur, un 403 y laisserait une ligne rouge) et le lien adversaire → `/players/$pseudo`. 🔑 **Le second est sorti ROUGE parce que le sélecteur était trop lâche** : `getByRole('link', {name: pseudo})` en attrapait **deux**, le lien de la date portant `aria-label="Match sheet against <pseudo>…"` — **Playwright matche le nom accessible en SOUS-CHAÎNE par défaut**. Viser le libellé exact.
  - ⚠️ **Un non-bloquant de review a été RÉFUTÉ, pas reporté** : « conditionner les 3 requêtes de `/solo/$ladderId` à la garde de format » n'économise **rien** — les trois hooks partent dans le **même rendu**, le format n'est pas encore connu. Le seul moyen serait de les enchaîner, ce qui coûte un aller-retour à **chaque visite légitime**. Ne pas le re-ticketer.
  - ⚠️ **Point de recette impossible à automatiser** : le rendu d'un classement **renseigné** (bande de stats + « Elo · #rang » sur la tuile) — fabriquer une ligne `rankings` exige un match terminé que le scénario ne produit pas. Se connecter en `alice@dev.local` / `Test1234!` → `/solo` → la tuile Chess doit afficher `1560 Elo · #1`. **Vérifié à l'œil le 30/07, conforme.**
  - ~~⚠️ **`MatchRow` ajoute un consommateur de `to="/players/$pseudo"`** — la branche `page/player` renomme cette route au singulier.~~ ❌ **FAUX depuis le 31/07** : William a **abandonné** le renommage, sa branche reste sur `/players/$pseudo` (pluriel). **Aucun conflit `tsc -b` à attendre de ce côté.**

- ✅ **[B-MM] + [F-MM] MERGÉS le 30/07** (B-MM commit `a03a4de` / merge `3cf425a` · F-MM commit `dc53ad3` / merge `b3278fb`, **aucune migration**) — cartes https://trello.com/c/mr6mP1pT et https://trello.com/c/ksD4k88P, toutes deux en Done. **`/matchmaking` livre la moitié ACCEPT du cycle** : avant, le front n'appelait **jamais** `POST /matches/{id}/accept`, seuls le seed et l'audit acceptaient par appel API direct — les phrases « atteignable / bouclable à la souris » de FT-2C et FT-4B étaient **fausses**, elles sont vraies maintenant. Campagne d'audit **exit 0 / 16 scénarios / 265 checks**. Les deux reviews (Opus) ont rendu **zéro bloquant** ; 6 non-bloquants back et 8 front appliqués.
  - 🔑 **TROIS défauts sont sortis de l'USAGE RÉEL de David, aucun des 265 checks** : un `?ladderId=` qui filtrait le tableau pendant que les deux sélecteurs affichaient « All games / All formats » (filtre actif et invisible) ; le sélecteur Format qui proposait les 4 formats du contrat, donc Valorant + 1v1 → tableau vide ; et un encadré « no open slot » affiché **au-dessus du créneau ouvert du lecteur** (l'API exclut les siens). **Une passe à l'œil vaut plus qu'un ticket de code** — voir [[feedback-ask-screenshot-before-done]].
  - 🔑 **Je peux prendre des captures et les LIRE** : script Playwright dans le scratchpad + `Read` sur le PNG. C'est comme ça que le 3ᵉ défaut est sorti. À refaire sur toute page front livrée.
  - ⚠️ **`MM9c` est sorti VERT sur du code volontairement cassé** : quand la valeur d'un `<select>` ne correspond à **aucune option rendue**, le navigateur affiche l'option vide de lui-même — lire le contrôle ne prouve rien, il faut lire le **tableau**.
  - ⚠️ **L'audit tourne sur l'HÔTE, jamais dans le conteneur front** (Chromium est dans `~/.cache/ms-playwright`) : `docker compose exec frontend npm run audit` sort « Aucun Chromium trouvé », assez discret pour passer pour un vert.
  - 🔑 **Deux trous jugés « non fabricables » l'étaient** : la panne de `GET /teams` (via `page.route`, **nouvelle capacité du harnais**) et le créneau pris sous le clic (un tiers l'accepte **par API** entre la confirmation et le clic — aucune course à gagner).
  - ⚠️ **Dates en `en-GB`** (`lib/match-slots.ts`, `lib/match-detail.ts`) : noms anglais, **ordre jour-mois et 24 h conservés**. Pas `en-US` (inverserait l'ordre et passerait en AM/PM). Locale **figée**, jamais celle du navigateur.
  - 🔑 **La leçon de méthode de ce lot** : le ticket a été cadré sur un fait FAUX que j'avais affirmé — « aucune route ne liste les créneaux ouverts ». `GET /matches?ladderId=` existait, documentée et testée ; j'avais lu le `post` dans `openapi.yaml` sans dérouler jusqu'au `get`. **Grep le HANDLER, pas seulement le contrat**, avant de conclure qu'une route manque.
  - 🚨 **Deuxième correction de cadrage, produit celle-là** : la première version de la carte front affichait l'adversaire et son Elo sur chaque ligne. Ça **contredisait l'anonymat B5b**, décision documentée et gardée par des tests. L'anonymat a été conservé et les deux cartes corrigées. **Ne pas le relever au prochain ticket** en croyant à un oubli.
  - ⚠️ **Ce que ça change pour la suite** : la phrase « le cycle est atteignable / bouclable à la souris » (FT-2C, FT-4B) était **fausse** jusqu'à F-MM — il manquait l'accept. Elle est vraie maintenant.

- ✅ **LES DEUX CHANTIERS DU 30/07 SONT SOLDÉS (31/07)** — **[F-HIST] mergé** (voir la ligne Frontend) et **la 2ᵉ passe de review de [FS-0] est faite**.
  - 📋 **[FS-0] — 2ᵉ passe rendue, rapport dans `/home/pinto/review-fs0-passe2.md`** (une copie est sur le bureau Windows de David). **Les 3 bloquants sont réellement corrigés**, vérifiés dans le code : `teams-manage` scopé à `<main>`, plus aucune région live permanente dans `components/social|layout`, et `sameSession` ne dépend plus que du `userId` (avec le *pourquoi* en commentaire — le back ne vérifie le JWT qu'au handshake). 8 non-bloquants sur 10 soldés, 1 reporté avec raison validée (la migration du modal vers `<dialog>` natif), 1 à moitié. 🚨 **CE QUI BLOQUE ENCORE N'EST PAS DANS LE CODE** : sa branche est rebasée sur `25e9086`, master a **beaucoup** bougé depuis (B-MM, F-MM, puis F-HIST + le fix d'audit → `d346356`). Sa campagne « 16 scénarios / exit 0 » **n'a jamais exécuté `matchmaking.mjs`** (23 checks, arrivé avec F-MM), qui bascule en **375 px** et y mesure le débordement du document — précisément le viewport où FS-0 monte un `MobileHeader` neuf en marges négatives. **Même classe de défaut que son bloquant ①**, sur un scénario qui n'existait pas à son rebase. → rebaser sur master, reséminer, campagne complète (**18 scénarios** désormais), exit 0 exigé. ⚠️ Non-bloquant le plus utile à lui faire faire tant qu'il est dans les fichiers : **11 sélecteurs d'onglet non scopés** (`teams-detail` ×3, `teams-matchmaking` ×5, `match-detail` ×1, `teams-manage` ×2) — verts **par chance de nommage** (le rail propose Friends/Messages/Add friend, pas Overview/Matches/Manage), et FS-1→5 les feront tomber en `exit 2`.
  - 🚨 **LA BASE DE DEV CONTIENT DES DONNÉES DE DÉMO POSÉES À LA MAIN** (30/07 19:37, uuids déterministes `bbbb2222-…`, 4 créneaux `pending` sur Valorant 2v2 / RL 3v3 / cs2 2v2 / Valorant 5v5, capitaines `alice`/`dave`/`judy`/`tester93@hotmail.fr`). **Ce n'est pas le seed.** Elles n'ont **pas** été purgées — c'est l'audit qui a été durci (voir la ligne Tests). **Ne pas les supprimer sans demander à l'équipe**, quelqu'un travaille peut-être avec.
  - *(Rappel de ce qui avait été décidé le 30/07 au soir, pour mémoire :)*
  1. ~~**[F-HIST]** (https://trello.com/c/uxsI4TUr) — `/history`, la dernière page vierge.~~ **FAIT.** **Front pur, aucun ticket back** : `GET /matches/me` est déjà exactement ça (c'est pour elle que B-SOLO l'a rendue polymorphe), sans pagination, en un appel. 🔑 **Périmètre tranché : MES matchs**, tous jeux et ladders confondus, solo et équipe réunis — **pas** un flux global du site (aucune route ne le sert, et lire les résultats d'inconnus n'est utile à personne). 🚨 **PAS de bilan V-D global** : un V-D inter-ladders ne veut rien dire (10 victoires aux échecs + 10 défaites cs2 ≠ 50 %), et le record par ladder existe déjà là où il a un sens. Le vrai gain de la page = **la section « à traiter »** (`awaiting_confirmation` / `disputed`), que rien ne regroupe aujourd'hui alors que le job d'auto-confirmation ferme à **24 h**.
  2. ~~**Review de [FS-0]** (https://trello.com/c/9Dz6Y3LN) — le rail social de **Walid**.~~ **FAITE** (2ᵉ passe, voir plus haut). Charger [[project-f-social-decomposition]] avant d'y retoucher. ⚠️ Sa branche reste basée sur un master d'avant F-SOLO / F-GAMES / F-MM : lire le diff contre `git merge-base`, **jamais** contre master, et son rebase conflictera sur `RightRail`, `AuthenticatedLayout`, `ui/tabs.tsx`, `index.css`, `openapi.yaml` et les fichiers de doc.

- 🎯 **Ensuite : à décider avec l'équipe** — la priorité a changé le 30/07. 🚨 **La liaison de compte externe est le SEUL trou front qui empêche un utilisateur réel d'utiliser la fonctionnalité centrale** : `GET`/`POST`/`DELETE /users/me/external-accounts` existent et fonctionnent, le front **ne les appelle JAMAIS** (seuls les types générés les mentionnent), et la garde est **active** (`matches.ts:311` refuse une lineup contenant un joueur non lié → 400 `unlinkedPlayers`, et `CreateMatchPanel.tsx:338` grise déjà ces joueurs). Donc un vrai utilisateur sur cs2 **ne peut pas lier son Steam, donc ne peut aligner personne, donc ne peut pas ouvrir de match** — le cycle n'est « atteignable à la souris » que parce que le seed et l'audit lient les comptes **par appel API direct** (`teams-matchmaking.mjs:157`). ✅ **Pas bloqué par le back restant** : `openapi.yaml:394` dit l'`externalId` « saisi manuellement au MVP » et `verified` « toujours false au MVP », et **la garde ne lit pas `verified`** — une liaison déclarative suffit. ⚠️ `components/home/LinkAccountBanner.tsx` existe **sans aucun appelant** mais **sa copie viole la décision produit** : « you cannot **enter the queue** » et « start **matchmatching** » (faute de frappe) — à réécrire, il n'y a pas de file d'attente. Autre trou pur : **`DELETE /users/me` (BX-DEL, back fini) n'a AUCUNE UI**. ✅ **Les deux sont réunis dans UNE carte, créée le 30/07 : [F4B]** (https://trello.com/c/WBWaXu34, Todo) — même page, même nature, même scénario d'audit, les séparer ferait payer deux fois la mise en place. ⛓️ **Elle ne démarre QUE quand [F4] est mergé sur master** (elle ajoute 2 sections à la page de réglages de F4 : la commencer avant garantit un conflit sur `pages/profile.tsx`). Autres candidats, aucun bloquant : **[B12B]** (https://trello.com/c/xC70Wqjf), le **stub `/home`** (`/games` est fait depuis [F-GAMES]), le **rail en `hidden lg:flex`** (sous 1024 px : ni navigation ni déconnexion), `server.ts:119` **`max: 100` en littéral** (source des 429 en campagne, invariant #13), et le **rail social** (F-Social, 7 cartes déjà découpées en mémoire).

- ✅ **[FT-3] MERGÉ le 28/07** (commit `715aa9c`, merge `65de356`) — page `/ladders/$ladderId` : identité + artwork, règles en clair, pool de maps servi par l'API, classement complet (joueurs **et** équipes), ligne de sa propre équipe surlignée. `GET /ladders/{id}` enrichi du **pool de maps du JEU** (même table que `POST /matches` : la page ne peut pas annoncer des maps que le serveur n'attribuera pas) ; section pilotée par la **donnée**, pas par une liste de jeux en dur — seuls **cs2 (7) et valorant (6)** ont des maps. `LadderRow` + `LadderBoard` extraits de `LadderExcerpt` (règle du second usage), helpers dans `lib/ladders.ts`, `ui/error-panel.tsx` extrait. ⚠️ **Le code venait d'un chat mobile, sans `coder-front` ni review** : le rapport `reviewer-front` (Opus) a sorti 3 bloquants + 9 non-bloquants, tous soldés avant merge. **3 écarts assumés, à ne pas « corriger » par erreur** :
  1. ⚠️ **La carte Trello ment sur une règle** : elle annonce « slot sans adversaire annulé au bout de 24 h ». **FAUX** — `cancelExpiredSlots` (`jobs/index.ts`) l'annule dès qu'il passe **sous `MIN_LEAD_MINUTES` (15 min) de son PROPRE coup d'envoi**, l'instant où plus personne ne peut l'accepter. Les 24 h de la règle « Score » sont un autre mécanisme (match joué mais non rapporté). C'est la règle du **code** qui est affichée.
  2. ⚠️ **`/ranking` SUPPRIMÉE** (page, route, `RankingTable`, entrée de nav) au lieu d'être migrée. Décision de David. ✅ **DETTE SOLDÉE par [F-GAMES] le 30/07** : `/games → /games/$gameId` est désormais le point d'entrée vers un classement pour un compte **sans équipe**. (`play`/`find party` restent des `MenuItem` sans `to`.)
  3. ⚠️ **NI pagination NI conteneur défilant** — décision de David prise **avec la mesure** : à 200 compétiteurs la page fait **11 431 px (16 écrans)** pour 2941 nœuds DOM et 1278 ms. Le coût DOM n'est pas le sujet ; on accepte de descendre la page et on garde le **Ctrl+F sur tout le classement** (une pagination ferait atterrir un 137ᵉ sur une page 1 sans lui). Rationale figée dans le docblock de `LadderBoard.tsx`. **Ne pas re-ticketer avant qu'un ladder dépasse la centaine.**
  - 🔑 **Défaut invisible trouvé au passage** : à 375 px le nom du compétiteur était rendu **0 px de large** (tracks fixes + gaps + padding = 268 px d'une boîte de 276, avatar `shrink-0`), 73 px sortaient **en silence** car `LadderBoard` clippe. Le check de largeur ne pouvait pas le voir — il mesurait `documentElement`, il était **vert par construction**. Corrigé le check d'abord (`L9b` mesure la boîte ET la largeur rendue du nom), le défaut ensuite : sous `sm` les 3 nombres passent sous le nom via **`sm:contents`**, au-dessus la grille à 5 colonnes est restaurée à l'identique.

- ✅ **[F-Nav] MERGÉ le 29/07** (commit `4a5c4bb`, merge `822a6eb`) — rail gauche : wordmark, recherche, 6 items, bloc compte. **Livré une 1ʳᵉ fois sans suivre la maquette, puis RECODÉ** : `vsmode-home-demo.html` (`.left`/`.nav-item`/`.sep`) fait foi pour la **structure et les dimensions**, `index.css` pour couleurs/radius/ombres. Écarts corrigés : 288→**264 px**, `p-6`→**18/16**, wordmark 48 px italique→**30 px droit**, icônes 20→**16 px**, et la recherche sortie du `<nav>` (ce n'est pas un lien). 🔑 **La maquette contenait déjà la solution d'un défaut trouvé en review** : l'actif se distingue du survol par la **bordure** (`.nav-item.active` ajoute `border-color`, `:hover:not(.active)` ne la touche pas) — la 1ʳᵉ passe avait gardé le fond et jeté la bordure, rendant les deux états **identiques au pixel**.
  - **`ranking` SUPPRIMÉ** du rail (absent de la maquette) : ça règle du même coup le fait que `to="/ranking"` **cassait `npm run build` de master au merge** (route retirée par FT-3, branche basée avant).
  - **Décision : aucun item grisé.** Les 6 items naviguent ; `/solo`, `/matchmaking`, `/history` sont des **pages vierges** créées ici. ⚠️ `MenuItem` garde `muted`/`disabled` **sans appelant** — ce sont l'état `.off` de la maquette, un futur ticket les redemandera. La prop `params`, elle, a été **supprimée** (morte ET non sûre : le wrapper cassait l'appariement `to`/`params`).
  - ⚠️ **`AuthNav` n'est PAS un composant du rail** : `LandingNav` le monte aussi, sur la **landing publique**. Piège vécu — le `<ConfirmDialog>` de déconnexion était rendu sans garde, donc son `<h2>` « Log out » était le **premier titre du DOM** d'un visiteur anonyme, avant le `<h1>`. Il est désormais sous `{isLogged && …}`, et `LandingNav` est passé de `<nav>` à **`<aside>`** (il *contient* une navigation, il n'en est pas une). **Toute modif d'`AuthNav` doit être vérifiée sur `/` autant que sur `/home`.**
  - ⚠️ **Lien d'évitement : jamais `sr-only` + `focus:not-sr-only`** — cette paire compile un `padding:0` de spécificité (0,2,0) qui **bat** `px-4`/`py-2` en (0,1,0) : le lien se rendait en 149 × 22 px, **sous les 24 px** de WCAG 2.5.8. Motif retenu : **translation** (`-translate-y-24` / `focus:translate-y-0`), sans conflit.
  - ⚠️ **`L7` de `ladder-detail` est scopé à `<main>` depuis ce ticket.** Il compte les mots `queue|matchmaking|auto-match` pour garder la décision produit challenge/accept — mais il balayait le **document entier**, et le rail persistant porte un item `Matchmaking` (le nom interne du cycle, `openapi.yaml` tague déjà `POST /matches` ainsi) sur **toutes** les pages authentifiées. Non scopé, il rendait rouge une page dont la copie est irréprochable. **Même piège pour tout check qui balaie la page : le rail est là aussi maintenant.**
  - 🔑 **Le scénario `f-nav.mjs` (18 checks) MESURE les 13 dimensions de la maquette** (`N4d`) : l'échelle dynamique de Tailwind v4 accepte n'importe quel nombre, donc `w-66` mal tapé en `w-64` passe lint **et** build en silence. Un critère visuel non mesuré n'est pas gardé.
- **Composants** : stratégie tranchée le 27/07 — un composant **sans connaissance du domaine** part tout de suite dans `components/ui/` ; tout le reste est extrait au **second usage réel**, par le ticket du second consommateur (détail et inventaire dans `docs/frontend.md`). **Chaque brief de `coder-front` doit porter la consigne de réutilisation** (interdiction de recopier les classes Tailwind d'un composant existant).
- **Front (reste)** : `/profile` ; liaison de compte (`LinkAccountBanner`) ; pages match + notifications ; rail social (voir mémoire F-Social). *(Les puces file upload et le câblage de la recherche sur `GET /search` sont faits — FT-2B ; le composant vit maintenant dans `components/search/SearchBar.tsx`, qui a **absorbé `UserSearch`** en F-Nav et sert le rail **et** `TeamInvitePlayer`.)* **Deux dettes d'accessibilité mesurées à ticketer** : `text-text-muted` sur carte = 4,23:1 (sous AA, 45 usages dans 25 fichiers → ticket design system) et l'historique de matchs qui laisse 262 px hors champ à 375 px (rendu en cartes sous `sm`).
- **Cartes à ouvrir après F-Nav** (aucune n'est bloquante, toutes sont argumentées) : ① **`server.ts:119` déclare `max: 100` en LITTÉRAL** au lieu de `rlMax(100)` → `RATE_LIMIT_FACTOR` ne s'applique pas au limiteur global, et `GET /ladders` étant **anonyme** (clé `ip:`, pas de `trustProxy`), tout le trafic navigateur partage **un seul seau de 100/min** — c'est la source des 429 en campagne (invariant #13). ② **`/home` est un stub** (`<h3>Page home!</h3>`, sans `<h1>`) **moins fini** que les pages vierges créées par F-Nav, et le rail le met à un clic depuis partout. (`/games` était dans le même cas : **fait par [F-GAMES]** le 30/07.) ③ **Rail en `hidden lg:flex`** : sous 1024 px, ni navigation ni moyen de se déconnecter. ④ **`landing-public.mjs` L4 est une tautologie** (`step('L4', true, …)`) qui visait le sélecteur de langue, supprimé par F-Nav : check mort.
- **Back** : vérification des comptes externes (OAuth Steam/Riot → `verified=true`), présence chat vers Redis (optionnel).
- 🚨 **UNE branche dort HORS de master** (au 31/07) :
  1. **`feature/fs0-social-foundation`** (commit unique `3cb16ac`) — **[FS-0] rail social, codée par WALID**, carte https://trello.com/c/9Dz6Y3LN **en Review**. **Relue DEUX FOIS** (30/07 puis 31/07), pas encore mergée : la balle est chez Walid — rebase sur master + campagne complète verte. Charger la mémoire `project_f_social_decomposition` (le découpage en 7 cartes FS-0→5, le protocole WS à 6 événements, la DoD transverse) avant d'y toucher. ⚠️ Review en local : diff contre **`git merge-base`**, jamais contre master (sa base est ancienne), et **jamais sa propre branche**.
  - ❌ **[B10] `playerCount` par jeu est ABANDONNÉ** (décision de David, 31/07). Le code était fini et vert sur `feature/b10-player-count` (`80c675b`), mais il ne servait qu'à **enrichir la landing**, qui n'est plus un besoin. **Ne pas le relancer, ne pas le recoder, ne pas s'étonner que `GET /games` ne rende pas `playerCount`.** Branche et carte à supprimer.
- **Hygiène du board (30/07)** : `[FT-4B]` était restée en In Progress **après son merge du 29/07** — corrigée, passée en Done. ⚠️ **Vérifier la colonne In Progress après chaque merge** : une carte oubliée là fait croire qu'un travail est en cours. Reste douteuse : **`[F-Home]` (card 48, In Progress)** alors que `/home` est toujours le stub `<h3>Page home!</h3>` — aucune branche, aucun commit, à redescendre en Todo. ⚠️ **Ne pas confondre `[FT-4B]`** (saisie du score, **mergée**) **et `[F4B]`** (liaison de compte + suppression de compte, **Todo**, https://trello.com/c/WBWaXu34) : les deux noms se ressemblent et la confusion a déjà eu lieu.

---

