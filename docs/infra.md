# Infrastructure — I2, I3, I4

> Extrait de CLAUDE.md (refacto 25/07). Origine unique HTTPS, proxy /api + /media, certs, env, bootstrap.

### Infrastructure — I2 + I3 + I4 mergés

- ✅ **I4 — origine unique HTTPS + proxy `/api`/`/media` + Docker fiable** (**MERGÉ sur `master`**, merge `b2800eb`, commit `fe585b6`). Rend l'infra locale cohérente : le navigateur ne connaît **qu'une** origine `https://localhost:5173` (Vite HTTPS), qui proxifie `/api/*` → `https://backend:3000` (retrait du préfixe `/api`, `ws:true` pour `/api/ws/chat`, `secure:false` interne) et `/media/*` → `http://minio:9000` (retrait du préfixe `/media`, `changeOrigin` pour que les URLs présignées SigV4 restent valides). Plus de `localhost:3000`/`localhost:9000` côté front, plus de contenu mixte.
  - 🔑 **INVARIANT du cookie refresh** (à ne jamais casser en isolation) : le backend pose `Path=/auth`, le proxy Vite le réécrit en `Path=/api/auth` (`cookiePathRewrite`), le front tape `/api/auth/*`. Les 3 forment un tout — modifier l'un sans les autres casse **silencieusement** la restauration de session. Cookie final navigateur : `HttpOnly; Secure; SameSite=Strict; Path=/api/auth`. Options centralisées dans `backend/src/auth/cookies.ts` (`setRefreshCookie`/`clearRefreshCookie`), utilisées par register/login/2FA/OAuth/logout/delete.
  - **Médias** : avatars stockés en **URL relative** `/media/avatars/<fichier>` (`buildPublicUrl`), preuves privées présignées contre l'**hôte interne** puis réécrites en `/media/evidence/<clé>?X-Amz-...` (`presignEvidenceUrl` → `toMediaPath`). Migration **`0020`** normalise les anciennes URLs d'avatar absolues → `/media/avatars/…` et retire (NULL) les logos d'équipe `http://` (contenu mixte). `updateTeamSchema.logoUrl` restreint désormais à **https uniquement**. Bucket `evidence` toujours **privé**.
  - **OAuth** : `GOOGLE_REDIRECT_URI=https://localhost:5173/api/auth/oauth/google/callback`, `FRONTEND_URL=https://localhost:5173`. Cookies d'état OAuth configurés `HttpOnly; Secure; SameSite=Lax` (Lax volontaire : le retour Google est une navigation top-level cross-site). ✅ **Dépendance humaine levée (David, 23/07)** : responsable Google Cloud = **David** (propriétaire du projet `transcendence-498815`) ; nouvelle URI `…:5173/api/auth/oauth/google/callback` **enregistrée** (redirect URI) + origine JS `https://localhost:5173` ajoutée, anciennes `:3000` conservées. **App publiée En production** : les scopes étant `profile`+`email` (non sensibles), aucune vérification Google requise → n'importe quel compte peut se connecter, plus de liste d'utilisateurs test à gérer. ⚠️ Vérifier l'état « En production » avant une soutenance (Google a déjà repassé l'app en Test lors de sa migration Auth Platform) ; filet de secours = ajouter l'adresse du correcteur en utilisateur test (propagation immédiate).
  - **Env** : validation Zod au démarrage (`backend/src/config/env.ts`, importée en 1er via `config/validate-env.ts`) — échoue tôt avec le **nom** de la variable fautive, jamais sa valeur. `MINIO_PUBLIC_URL` **supprimé** (devenu inutile). Nouvelles vars front `API_PROXY_TARGET`/`MINIO_PROXY_TARGET` (non préfixées `VITE_`). CORS lit `FRONTEND_URL` (pas de wildcard).
  - **Cert** : backend seul générateur ; monté en lecture seule dans le frontend (`backend_certs:/app/certs:ro`) ; Vite lit le cert pour `vite serve` uniquement (`existsSync` → `npm run build` n'en dépend pas) ; entrypoint régénère si manquant/illisible/invalide/expirant sous 7 j (`openssl x509 -checkend`) ; frontend a un healthcheck HTTPS. Cert régénéré → redémarrer le frontend et ré-accepter l'empreinte.
  - **Décisions tranchées (David, 23/07)** — 3 choix pris par l'agent puis validés en équipe, **aucun changement de code supplémentaire requis** (le code les reflète déjà) : ① **`MINIO_PUBLIC_URL` supprimé** partout (devenu mort : avatars en URL relative + présignature sur l'hôte interne) — vérifié absent du code/compose/`.env.example`, seule trace = cette doc. ② **Validation d'env au démarrage conservée**, seuil **`JWT_SECRET` ≥ 16 caractères** (plancher anti-`changeme`, n'exige pas d'entropie précise). ③ **Logos d'équipe restreints à `https`** + migration 0020 met à `NULL` les logos `http://` existants (contenu mixte) : la feature logo est **conservée** (c'est une URL, pas un upload → **aucun lien avec le module File upload**), seuls les `http://` sont retirés. **Impact données réel = 0** (base dev : 34/34 avatars déjà NULL, 11/11 logos déjà NULL — la migration est une précaution).
  - **Vérifié** : `tsc`+Vitest 21/21, front build+lint (conteneur), `docker compose config --quiet`, migration 0020 appliquée, smoke test proxifié (`tests/test_proxy_smoke.py` 18/18 : `/api/ping`, SPA, `/media` MinIO, evidence privé, avatar `/media`), disputes 82/0 (signé via `/media` → 200, non signé → 403), cookie réécrit `Path=/api/auth` + refresh proxifié OK. ✅ **Recette automatisée re-jouée sur stack live 7/7** (David, 23/07) : smoke 18/18, deep-links `/home /games /teams /ranking /profile` → 200 `text/html` (React, jamais du JSON), OAuth start → 302 vers Google avec bon `redirect_uri` + state, médias via `/media`, backend direct `:3000/ping` conservé, plus aucun `:3000`/`:9000` dans le bundle. ✅ **Recette navigateur (Chrome, David, 23/07)** : register + Google login **de bout en bout** → `/home` connecté, **F5 → session restaurée** (bug cross-scheme corrigé), console 0 warning/erreur, Network sans `:3000`/`:9000`. **Reste optionnel** : HMR `wss://` à cliquer. ✅ **MERGÉ sur `master`** le 23/07 (merge `b2800eb`). La dette cross-scheme F0-C (ticket https://trello.com/c/wvEfFTVq) est **résolue** par I4 : F5 restaure la session. Carte à passer en Done.
- ✅ **I2 — images Compose reproductibles** (mergé) : noms de registres complets et tags figés pour PostgreSQL 17.10, Redis 8.8.0, Adminer 5.4.2 et MinIO `RELEASE.2025-09-07T16-13-09Z`. Redis Commander migre de l'ancienne image Docker Hub `0.7.2-rc3` vers l'image officielle maintenue `ghcr.io/joeferner/redis-commander:0.9.1`.
- ✅ **I3 — bootstrap en une commande** (mergé, empilé sur I2) : auth Redis effective, clients backend/Redis Commander authentifiés, healthchecks + `depends_on: service_healthy`, certificat auto-signé dans `backend_certs`, migrations automatiques avant le backend, builds `npm ci` et resynchronisation conditionnelle des volumes selon le hash des lockfiles.
- ✅ Validé avec Podman : deux cycles `podman compose down` puis `podman compose up -d --build`, tous les services sains/accessibles ; Redis refuse sans mot de passe et répond avec authentification ; 12 tests backend passent ; build frontend passe ; lint frontend avec 0 erreur et 0 warning après le correctif Fast Refresh empilé sur I3.
- ⚠️ Le `.env` reste une configuration préalable volontaire : copier `.env.example`, remplacer les `changeme`, puis lancer Compose. Les certificats et migrations ne demandent plus de commande manuelle.

