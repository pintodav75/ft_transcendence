# Dépendances
node_modules/

# Données persistantes Docker
/data/

# Build outputs
dist/
build/

# Variables d'environnement
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Éditeur / OS
.vscode/*
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db

# Contexte Claude — CLAUDE.md est versionné (contexte projet partagé)
CLAUDE.local.md
.claude/
.claudeignore
.tokensave/

# Maquette HTML de référence pour /home : outil de travail local, pas un livrable.
# La version qui fait foi vit hors du dépôt (voir docs/frontend.md).
vsmode-home-demo.html

# TypeScript
*.tsbuildinfo

# certs
certs/

# Tests
coverage/

# ignoring it in dev, it's generated all the time anyway and it prevents me from switching
# between branches -william
frontend/src/routeTree.gen.ts
# Tests — gardés sur disque en local, jamais livrés dans le dépôt
# (e2e Python, Vitest, harnais d'audit console). Retirés le 03/08 : le sujet
# ne les demande pas et ils ne servent aucun module.
backend/tests/
frontend/tests/
