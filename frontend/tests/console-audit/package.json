{
  "name": "console-audit",
  "private": true,
  "type": "module",
  "description": "Audit automatisé de la console Chrome, un scénario par ticket front. Isolé de frontend/package.json pour ne pas alourdir le npm ci du Dockerfile.",
  "scripts": {
    "audit": "node run.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.62.0"
  }
}
