# CLAUDE.md — ft_transcendence

> Contexte de session. **Volontairement court** : le détail vit dans `docs/`, à lire **à la demande** (voir « Index de la doc »).

---

## 🎯 Le projet

Projet final du Common Core 42, équipe de 4, sujet libre : une web app validant ≥ 14 points de modules.

**Concept : plateforme compétitive multi-jeux type GameBattle / LiveNplay** — profils, équipes, ladders ELO, soumission de résultats, disputes, chat/amis/notifications temps réel, pattern config-driven multi-jeux.

> 🚨 **PAS de file d'attente, PAS de worker de matchmaking automatique.** Modèle = **challenge/accept** : un camp ouvre un slot → un autre l'accepte → les deux entrent le score → l'ELO bouge. Aucun bouton « chercher une partie », aucun appariement par ELO. (Décision explicite de David, 13/07 — ne jamais la réintroduire.) Le cycle est **déjà implémenté** (B5b→B6).

> 🚨 **Pas de jeu jouable dans l'app** (décision 13/07) : la plateforme _tracke_ des jeux externes (LoL/CS2/chess.com via liaison de compte).

**Statut** : équipe de 4 formée, deadline courte → focus.

---

## 🧩 Modules — 16 points (seuil 14)

5 majors + 6 minors, **vérifiés contre le PDF v21.1** (audit 23/07) : frameworks front+back, user management, WebSocket temps réel, user interaction, **organization system** (= nos teams) | ORM Drizzle, OAuth 2.0, 2FA TOTP, file upload, notifications, **advanced search**.

- Tous **✅ back**. **File upload est désormais complet et démontrable** : les puces FRONT (validation client du type et des 2 Mo, aperçu, barre de progression) sont livrées par `components/ui/image-picker.tsx`, câblé sur le logo d'équipe par **FT-2B**. Le repli « Custom design system » n'est plus nécessaire.
- « Game stats & match history » est **mort** (exige un jeu fonctionnel) → remplacé par Organization system. **Ne jamais le ticketer.**

📄 Détail, exigences PDF et candidats de réserve → **`docs/modules.md`**

### 🚨 Motifs de REJET du projet (hors modules)

1. **Privacy Policy + ToS** non vides / non placeholder — aujourd'hui 8 lignes chacune → **projet rejeté en l'état**. Carte `[FT]`, rédigée **en toute fin** de projet.
2. **Zéro warning/erreur dans la console Chrome.**
3. **README complet** : Team Info, Project Management, Technical Stack, Database Schema, Features (qui a fait quoi), Modules + calcul de points, contributions individuelles, usage de l'IA.

Reste aussi la **préparation de la soutenance** (chaque module revendiqué doit être démontrable en live).

---

## 🛠️ Stack (résumé)

- **Front** : Vite 8 + React 19 + TS strict + Tailwind v4, TanStack Router (file-based) + Query, Zustand, RHF + Zod, lucide. WebSocket **natif** (le back utilise `ws`, **jamais** socket.io-client).
- **Back** : Fastify v5 / Node 24 LTS, TS strict ESM — `@fastify/websocket|multipart|jwt|cookie|oauth2|cors|rate-limit`, Drizzle ORM, Zod, bcryptjs, speakeasy, minio, redis.
- **Data** : PostgreSQL 17.10, Redis 8.8.0 (auth activée), MinIO (buckets `avatars` public / `evidence` privé).
- **Infra** : Docker/Podman Compose, **pas de Nginx**. **Origine navigateur UNIQUE `https://localhost:5173`** (Vite HTTPS) qui proxifie `/api/*` → backend et `/media/*` → MinIO. Fastify ne sert **jamais** le build front. `docker compose up -d --build` suffit après le `.env`.

📄 Versions, arborescence complète et détail des libs → **`docs/stack.md`**

---

## ✅ État d'avancement (résumé — 28 juillet 2026)

- **Infra** : I2 + I3 + I4 mergés. Origine unique HTTPS, proxy, certs auto, migrations auto, validation d'env Zod. → `docs/infra.md`
- **Backend** : **terminé et fonctionnel** — auth (JWT typés, OAuth Google, 2FA, profil, avatar), social (amis, blocks, chat WS, DM, conversations), teams (CRUD + membres + édition + **cycle d'invitation B-INV** : `POST /teams/{id}/members` **supprimée**, remplacée par inviter / annuler / accepter / refuser / `GET /teams/invitations/me`, index unique **partiel** sur les `pending`, plafond `membres + en attente ≤ 10` sous verrous consultatifs triés), matchmaking complet (créer/accepter/annuler/résultat/ELO), **score Bo3 + delta d'Elo persistés par match (B14)**, **historique de match d'une équipe (B15, `GET /teams/{id}/matches`) + garde de `GET /matches/{id}` relâchée sur un match `completed`**, disputes + arbitrage, notifications (10 types), recherche avancée, jobs 24 h. **[B12] mergé le 28/07** (commit `531344f`, merge `487036b`) : `POST /auth/2fa/verify` compte ses essais **par compte visé par le tempToken**, plus par IP — montage à **deux étages** (plancher 30/min par IP en `onRequest` + 5/min par compte appelé à la main dans le handler via `server.createRateLimit()`). ⚠️ **Ne jamais remonter ce comptage dans un hook** : `hook: 'preValidation'` laisse passer les corps illisibles **sans aucun comptage**, et un second `server.rateLimit()` en hook est **neutralisé silencieusement** (symbole `rateLimitRan` partagé). ⚠️ **Pas de `trustProxy`** : `req.ip` est l'IP de **socket**, donc tout le trafic navigateur arrive par le conteneur front et **partage une seule adresse** — un compteur par IP est un point d'étranglement pour toute la plateforme, jamais une limite par utilisateur. **[B13] mergé le 28/07** (commit `fd69624`, merge `b499833`) : `POST /auth/refresh` rend **204** sans cookie (la ligne rouge que Chrome écrivait sur chaque page anonyme était un motif de rejet) et **purge** le cookie sur un refus, pour que ce 401 ne se répète pas indéfiniment — mais **pas** sur une panne (la vérification JWT a son propre `try` : purger sur une coupure de base déconnecterait durablement tout le monde). **[BX-DEL] mergé le 28/07** (commit `392dfb3`, merge `607a63a`, **migration `0023`** → `docker compose restart backend` au prochain pull) : `match_participants.user_id` passe de `restrict` à `cascade` (le `restrict` rendait la suppression de compte impossible **à vie**, en 500, pour tout joueur ayant été aligné une fois) et `DELETE /users/me` porte la règle produit — **409 `engaged_in_match`** (match non terminé) ou **409 `captain_of_team`**. 🔑 **Le parcours de sortie est imposé : annuler ses matchs → dissoudre son équipe → supprimer son compte** (`DELETE /teams/:id` refuse aussi une équipe engagée, **`team_engaged_in_match`**). ⚠️ **Ne pas relâcher `captain_of_team`** : `teams.captain_id` est en CASCADE, le départ d'un capitaine effacerait l'équipe, son roster et sa ligne `rankings` sans notifier personne. **[B16] mergé le 29/07** (commit `b1e00e4`, merge `d09aeab`, **aucune migration**) : `GET /matches/{id}` sert l'identité du **ladder et de son jeu** (une requête jointe, jamais une par side) et les **deux scores soumis par chaque camp** (sans eux le front ne peut pas proposer « Confirmer » — confirmer, c'est renvoyer le miroir exact de la soumission adverse). Garde de permission **inchangée**. 🔑 **`seed-dev` sème désormais SIX matchs de démo** Alpha vs Bravo sur cs2 5v5, un par état du cycle (créneau ouvert · accepté avant l'heure · accepté après l'heure · en attente de confirmation · en litige · terminé avec maps + Bo3 + Elo) : avant, *voir* un match en attente de confirmation demandait 2 comptes, 2 équipes, une invitation, un créneau, un accept et une soumission — personne ne le refaisait. **3 comptes sont connectables** (`alice`/`bob`/`carol`, capitaines des 3 équipes, mot de passe de fixture partagé avec `backend/tests/helpers.py`) ; les 8 autres joueurs n'ont **pas** de mot de passe, ce n'est que du remplissage de lineup. ⚠️ **`onConflictDoNothing` saute la LIGNE ENTIÈRE**, il ne complète pas les colonnes manquantes : le seed porte donc **deux passes de rattrapage explicites** (poser le hash sur les 3 comptes, le **retirer** des figurants qui l'avaient reçu d'un run antérieur) — sans elles une base semée avant B16 affichait des identifiants qui rendaient 401. ⚠️ **Le créneau ouvert est semé à 3 JOURS** : le job `cancelExpiredSlots` annule tout `pending` passé sous `MIN_LEAD_MINUTES`, à 3 h l'état disparaissait 2 h 45 après le seed. ⚠️ **Limite assumée, documentée dans le code** : la purge du seed efface les matchs où Alpha **ou** Bravo apparaît mais ne remet à zéro que ces 2 classements — la vraie réponse est un **clear complet**, réservé au futur ticket « seed propre ». **[B-SOLO] mergé le 30/07** (commit `7719256`, merge `e9fce76`, **aucune migration**) : `GET /matches/me` passe du gabarit maigre `{id, ladderId, status, scheduledAt, startedAt, maps}` à celui de `GET /teams/{id}/matches` — `opponent`, `score {self, opponent}`, `result`, `eloDelta`, `disputeId`/`disputeStatus`, `completedAt`, plus `format`/`gameId` (par **jointure**, pas une requête de plus) et un `?ladderId=` optionnel. 🔑 **`opponent` est POLYMORPHE et réutilise LITTÉRALEMENT le type `Competitor` de `utils/leaderboard.ts`**, celui que `GET /ladders/{id}/rankings` expose déjà : le domaine modélise le compétiteur comme joueur **ou** équipe (`match_sides.team_id` nullable, `rankings.user_id` XOR `team_id`), donc créer une famille de routes `/solo/*` aurait dupliqué un concept déjà unique — et `/history` aura besoin de la version polymorphe de toute façon. 🚨 **C'est le `format` du LADDER qui décide si l'adversaire est un joueur ou une équipe, JAMAIS la nullité de `team_id`** : une équipe dissoute laisse un camp orphelin sur un 5v5 terminé, et lire ce null comme « solo » est le bug corrigé côté front pendant FT-4A. ⚠️ **`opponent: null` a QUATRE causes** : personne n'a accepté · créneau annulé · équipe adverse dissoute · **adversaire ayant supprimé son compte** (CASCADE depuis BX-DEL). Tri passé à `scheduledAt desc nulls last` **départagé par `createdAt`** (la grille des quarts rend les ex æquo fréquents ; sans second critère un simple UPDATE de statut peut permuter deux lignes). `test_matches_me.py` passe de **23 à 65 checks** et couvre les 2 cas que le seed ne sait pas produire, le check du piège **vu ROUGE avant vert**. **[B-MM] mergé le 30/07** (commit `a03a4de`, merge `3cf425a`, aucune migration) : `GET /matches` passe du listing par ladder au **parcours global** — `ladderId` devient **optionnel**, filtres `gameId` / `format` / `acceptable` / `limit`, identité `ladderName` + `gameName` par jointure, et surtout un **verdict `canAccept` + `reason`** par créneau. 🔑 **La question « route neuve ou enrichissement ? » a été tranchée par un FAIT, pas par un goût** : `GET /matches?ladderId=` **existait déjà** (documentée, couverte par 4 suites Python) — créer `/matches/open` à côté aurait dupliqué un concept unique, exactement ce que l'arbitrage de B-SOLO avait déjà refusé. 🚨 **L'ANONYMAT B5b EST CONSERVÉ, ne jamais le lever** : ni créateur ni maps dans la réponse — on accepte un créneau **sans savoir qui l'a ouvert**, c'est ce qui empêche de choisir ses adversaires et protège l'intégrité de l'Elo. 🔑 **Le verdict doit MIROITER `validateSide` branche par branche** : s'il dit « tu peux » et que l'accept refuse, l'UI offre un bouton qui produit une ligne rouge en console (motif de rejet). Les 6 codes : `account_not_linked` (1v1) · `no_team` · `not_captain` · **`roster_too_small`** · **`roster_not_linked`** · `schedule_conflict`. ⚠️ **Les deux codes de roster sont distincts À DESSEIN** : « invite des joueurs » et « fais-leur lier leur Steam » sont deux remèdes, donc deux liens à l'écran. La 1ʳᵉ version les fusionnait — l'`INNER JOIN` sur `user_external_accounts` ne remontait **que les membres liés**, donc « 1 membre lié » et « 5 membres dont 1 lié » donnaient le même compte ; corrigé en `LEFT JOIN` + **deux compteurs**, sans requête de plus. ⚠️ **`schedule_conflict` lit `LOCKING_STATUSES`, pas `ENGAGING_STATUSES`** : mes propres slots `pending` qui chevauchent ne me bloquent PAS, l'accept les annule. ⚠️ **Un paramètre présent mais VIDE vaut « absent »** (`?gameId=`) : sans ce `preprocess` Zod, un front qui construit `?gameId=${state}` depuis un filtre non renseigné prenait un 400, donc une ligne rouge. ⚠️ **`limit` s'applique APRÈS le filtre `acceptable`** (borner en SQL rendrait moins de `limit` créneaux acceptables alors qu'il en reste) — mais il n'y a **ni curseur ni total**, donc la liste peut être tronquée en silence quand on décoche « seulement ce que je peux accepter ». Requêtes **constantes**, mesurées au `log_statement=all` : plancher 5, plafond 7, **jamais une par créneau ni par ladder**. `test_matches_open_slots.py` (**100 checks**), suite matchmaking **384 ✅ / 0 ❌**. **[F-DISPUTE] — extension de `GET /disputes/{id}` mergée le 31/07** (commit `fbcc356`, merge `f6345c8`, **aucune migration**, écrite **avant** de lancer le ticket front qui la consomme) : la carte annonçait « front pur, zéro back », **c'était faux**. Trois ajouts. ① **`dispute.createdAt`** — c'est lui qui arme le timeout de 24 h du job B7 (ni le coup d'envoi, ni la soumission d'un score) ; sans lui l'échéance exigée par la DoD était **littéralement incalculable**. ② Un bloc **`match`** (statut, coup d'envoi, `ladder {name, format, gameId, gameName}`) en **une requête jointe**, servie après la garde : 🔑 **`ladder.format` est la SEULE autorité pour dire « 1v1 »** (le piège FT-4A est vivant ici, une dispute reste consultable après arbitrage donc après qu'une équipe a pu être dissoute), et surtout 🚨 **la page ne doit JAMAIS appeler `GET /matches/{id}`** — cette route répond **403 à un admin non-participant** sur un match `disputed`, or [F-ADMIN] se greffera sur cette page. ③ **`settledBy: 'admin' | 'timeout' | null`** — sans lui, la page **attribuait à un arbitre** l'annulation automatique du job B7 et servait sa `resolutionNotes` (une **ligne de log interne en français**, `'auto-résolu : aucun arbitrage admin sous 24 h'`) sous le libellé « Admin's note ». 🚨 **Ce n'était PAS un cas limite : tant que [F-ADMIN] n'existe pas, le timeout est le SEUL chemin vers `resolved`**, et la dispute de démo y arrive toute seule 24 h après un `seed:dev`. ⚠️ **Le champ se dérive de `resolved_by_user_id` ET de la `resolution`**, jamais du seul resolver : la colonne est en `set null`, donc un admin qui supprime son compte ferait passer son arbitrage pour un timeout — or **le job n'écrit jamais qu'un `cancelled`**, donc un vainqueur désigné implique un humain. Reste lossy dans **un** cas assumé (admin qui règle `cancelled` puis supprime son compte → sa note est masquée) : taire une note vraie coûte moins que présenter un log comme la prose d'un arbitre. ⚠️ Les sous-objets `dispute`/`sides`/`evidence` ne déclaraient **aucun `required`** (même défaut que B16, corrigé par FT-4A) : la codegen sortait tout en optionnel, ajoutés en les confrontant un par un au handler. → `docs/backend.md`
- **Frontend** : F0, F0-A/B/C/D, FR1 Register, FR2 Login+2FA, F-Nav (teams + ranking), FL landing publique, **FT-1 + FT-1B** (`/teams`, mergés le 26/07 dans `640248b`) — **mergés**. **FT-2A** (`/teams/$teamId` en consultation) **mergé le 27/07** (commit `478c62b`, merge `9ef6406`). **FT-2B** (onglet Manage capitaine : renommer, logo, ajouter, exclure, dissoudre + Leave team) **mergé le 27/07** (commit `d7c6f93`, merge `6746e81`). **FT-INV** (invitations d'équipe) **mergé le 28/07** (commit `0108084`, merge `ab9a4c6`) — juste après **B-INV** (commit `ad97df0`, merge `a724fe4`), les deux **dos à dos** parce que B-INV supprime une route que le front de FT-2B appelait. **FT-2C** (ouvrir et annuler un créneau de match depuis la page équipe) **mergé le 28/07** (commit `1d74b90`, merge `f0e2369`) — le cycle challenge/accept est désormais atteignable à la souris. **[F-Nav] rail gauche mergé le 29/07** (commit `4a5c4bb`, merge `822a6eb`). **[FT-4A] puis [FT-4B] mergés le 29/07** : `/matches/$matchId` est **complète** — la fiche en lecture (4A) **et** la saisie du score, la confirmation et le litige (4B). Voir *Prochaines actions*. **[F-SOLO] mergé le 30/07** (commit `58ad523`, merge `8d7dd34`, **aucune migration**, dos à dos avec B-SOLO qu'il consomme) : `/solo` et `/solo/$ladderId` remplacent la page vierge de F-Nav — miroir de `/teams` et `/teams/$teamId`. 🔑 **L'asymétrie qui pilote tout : on n'ADHÈRE PAS à un ladder solo** (une ligne `rankings` naît du **premier résultat de match**, pas d'une inscription), donc `/solo` liste **les ladders qui EXISTENT** (`format === '1v1'`), jamais « ceux où je suis classé » — l'autre lecture donne une page vide **sans porte d'entrée** à un compte neuf. Détail : hero au pseudo/avatar, **Elo · Record · Rank — pas de case Roster**, onglets **Overview + Matches uniquement (PAS de Manage** : rien à renommer, pas de logo, pas de roster, pas de dissolution), la section Roster remplacée par **l'état du compte lié** (c'est lui qui garde `POST /matches`), ouverture et annulation d'un créneau. ⚠️ **`hasLinkedProvider` est TRIVALUÉE** : si `GET /users/me/external-accounts` échoue, « inconnu » n'est jamais traité comme « lié » — le bouton n'apparaît pas, parce qu'un 400 §5.1 laisse une ligne rouge en console (motif de rejet). ⚠️ **Cas hors carte tranché ici** : `/solo/<id-d-un-ladder-5v5>` répond **200** côté API — sans garde la page rendait « mon dossier » sur un ladder d'équipe ; c'est le **format** qui tranche, jamais l'absence d'équipe. **Extractions au second usage** : `lib/match-slots.ts` (grille des quarts, 15 min, chevauchement, plafond de 5) et `lib/match-history.ts`, `MatchRow` + `MatchHistoryTable` → `components/matches/`, `LadderExcerpt` → `components/ladders/`, `StatStrip` → `components/ui/`, `GamePosterTile`, et les 4 mutations de créneau enfin déplacées vers `match-mutations.ts` (intention écrite dans `team-mutations.ts`), **invalidation pilotée par clé de cache**. ⚠️ **`CreateMatchPanel` n'a PAS été généralisé** (décision de David) : ce sont les **règles** qui sont partagées, le JSX solo est autonome — fusionner 410 lignes gardées par un scénario de 532 était du risque de merge pour du balisage non commun. **[F-GAMES] mergé le 30/07** (commit `48e027b`, merge `57ba70e`, **aucune migration**) : `/games` (grille des 5 jeux) et `/games/$gameId` (identité, pool de maps, ses 1 à 3 ladders avec podium médaillé, ma ligne et un CTA) construisent le parcours `/games → /games/$gameId → /ladders/$ladderId`. 🔑 **Ça solde la DETTE OUVERTE de FT-3** : depuis la suppression de `/ranking`, un compte **sans équipe** n'avait plus aucun chemin cliquable vers un classement. ⚠️ **Le top 3 est un APERÇU, pas une page** : règles, maps et classement complet vivent sur `/ladders/$ladderId` (FT-3) — le podium ne sert qu'à faire cliquer. 🔑 **Un seul `useQueries` sur `ladderRankingsOptions()`** : la clé `['ladder', id, 'rankings']` est celle de la page ladder, donc « See the full standings » coûte **zéro requête** (mesuré par `G9`, avec `G4` en témoin). 🔑 **Le slug se résout depuis la liste `GET /games` en cache** : un slug inconnu rend son erreur **sans dépenser de requête**, donc sans ligne rouge (motif de rejet) — `GET /games/{id}` ne sert qu'aux maps et son échec ne fait pas tomber la page. 🚨 **Le CTA est décidé par le `format`, jamais par l'absence d'équipe** (1v1 → `/solo/$ladderId` · 2v2+ avec équipe → sa page · sinon `/teams?create=<ladderId>`, formulaire pré-sélectionné). ⚠️ **Les cartes sont triées par taille de format DÉCROISSANTE** : `GET /ladders` trie par nom, ce qui ouvrait la page sur « cs2 2v2 (Wingman) », vide, devant le 5v5 qui porte toutes les équipes. Pas de tri par nombre de classés — les cartes se réordonneraient à l'arrivée des classements. ⚠️ **Médailles = la recette de `ui/pill.tsx`** (`border-<token>/40 bg-<token>/10 text-<token>`, dont le ton `live` est déjà `rank-gold`) : deux tentatives ont échoué avant, l'icône lucide (contour trop fin, or et bronze indistinguables à 16 px) puis le disque plein (seul aplat opaque de l'écran). ⚠️ **Contraste mesuré du chiffre** : or 8,0:1, argent 11,4:1, **bronze 4,3:1 — sous AA d'un cheveu**, même famille que la dette `text-text-muted` ; le correctif est d'éclaircir `--color-rank-bronze` (ticket design system), pas de retoucher cette carte. **[F-MM] mergé le 30/07** (commit `dc53ad3`, merge `b3278fb`, aucune migration, **dos à dos avec B-MM qu'il consomme**) : `/matchmaking` n'est plus une page vierge — **le cycle challenge/accept est enfin bouclable de bout en bout à la souris**, ce qui n'était pas vrai avant (le front n'appelait **jamais** `POST /matches/{id}/accept`, et les affirmations « atteignable à la souris » de FT-2C/FT-4B étaient fausses : seuls le seed et l'audit acceptaient, par appel API direct). Livré : `lib/matchmaking.ts`, `components/matchmaking/{SlotFilters,OpenSlotRow,AcceptSlotPanel}.tsx`, `useAcceptMatch` dans `match-mutations.ts`, et **`components/matches/LineupPicker.tsx` extrait de `CreateMatchPanel` au 2ᵉ usage** (branché des deux côtés, DOM inchangé — `CreateMatchPanel` lui-même n'est toujours PAS généralisé). 🚨 **La page n'affiche PAS l'adversaire** : l'anonymat B5b n'est pas une limite technique mais la décision produit — une ligne dit jeu, format, ladder, heure, rien d'autre. 🔑 **Chaque refus porte la phrase ET le lien qui le résout** (`no_team` → `/teams?create=<ladderId>`, les deux codes de roster → la page de son équipe, etc.) : c'est ce qui fait qu'un compte neuf voit l'activité de la plateforme sans jamais se voir offrir un bouton refusé. `account_not_linked` et `schedule_conflict` restent en **texte seul** (F4B n'existe pas ; identifier le match adverse coûterait une requête par ligne). ⚠️ **Le dernier chemin vers une ligne rouge a été trouvé EN REVIEW, pas au codage** : le bouton ne se fiait qu'à `canAccept`, or le serveur ne filtre les 15 min qu'**au moment de la réponse** — onglet resté ouvert 2 min sur un créneau à T+16 min, clic, **409**. Fermé par une horloge `useSlotClock()` (tick 30 s) avec `dataUpdatedAt` en **plancher** (`Math.max`). ⚠️ **Nuance qui contredit FT-4B et qu'il ne faut pas « réaligner »** : ici `dataUpdatedAt` **seul** est un no-op (c'est l'instant où le serveur a appliqué la même règle, il ne peut pas contredire sa propre liste) et le sens de sûreté est **inversé** — un `now` en retard fait paraître le créneau plus loin, donc **offre** le bouton. ⚠️ `?ladderId=` est résolu contre `GET /ladders` **déjà en cache**, jamais en interrogeant l'API : un uuid inconnu rendrait 404, donc une ligne rouge (même discipline que le slug de F-GAMES). ⚠️ **`f-nav.mjs` N5/N6 ont dû être re-ciblés** : ils visaient « les 2 seules pages du rail qui n'émettent aucune requête », `/matchmaking` en émet désormais — repli documenté, il n'y aura plus de page libre quand `/home` cessera d'être un stub. Scénario `matchmaking.mjs` **23 checks**, 4 régressions **vues ROUGES avant vert**. **[F-HIST] mergé le 31/07** (commit `6ad9881`, merge `d346356`, **aucune migration**, front pur) : `/history` était la **dernière page vierge du rail** — elle liste désormais **tous mes matchs, tous jeux et tous ladders confondus** (solo et équipe réunis) en **un seul `GET /matches/me` sans paramètre**, la route que B-SOLO avait rendue polymorphe *pour elle*. 🔑 **La vraie valeur est la section de tête, pas le tableau** : `awaiting_confirmation` + `disputed` sont les seuls statuts sur lesquels court une **échéance de 24 h** et rien ne les regroupait. 🚨 **SA COPIE NE DOIT JAMAIS DIRE QUE LE MATCH ATTEND UNE RÉPONSE DE *MOI*** — elle s'appelait « Waiting on you » et c'était **faux dans trois cas** : le camp qui a **déjà soumis** (reproduit avec `bob@dev.local` : la fiche vers laquelle la carte l'envoie dit « **Team Alpha** has 20 h left to confirm », et `MatchResultPanel` ne lui rend rien), un **non-capitaine ou joueur du banc**, et un **`disputed`** qui attend un **admin**. Ce n'est **pas réparable côté front** (`GET /matches/me` n'expose que `score`, jamais `submitted_at` : savoir *qui* a soumis coûterait une requête par ligne). Le regroupement est juste, c'est le **libellé** qui a été corrigé en « **Matches on the clock** » — ne pas le repersonnaliser. 🚨 **PAS de bilan V-D global** (un V-D inter-ladders ne veut rien dire ; le record existe par ladder, là où il a un sens) et **pas d'annulation de créneau** ici. **Filtres 100 % client** (la route rend tout), **options dérivées de la donnée** jamais des enums du contrat (piège F-MM). ⚠️ **La colonne « Ladder » est OPT-IN**, dérivée exactement comme `showLineup` : le DOM de `SoloMatches` et `TeamMatches` est **inchangé** (gardé sur le même run, `H8` ici + `H9` sur l'historique solo). ⚠️ **Deux helpers de clé, pas un argument optionnel** : `historyMatchesKey()` et `myMatchesKey(ladderId)` — la 1ʳᵉ version avait rendu l'argument optionnel, ce qui compilait mais laissait un futur appelant invalider `/history` **en silence** à la place de son propre écran. ⚠️ **EXACTEMENT UNE région live** : changer un `<select>` annonce l'option et **rien de son effet**, or les 4 contrôles n'ont pas d'autre effet que de refaire la table (WCAG 4.1.3) — la ligne visible et l'annonce lisent la **même** fonction (`historySummary`), deux formulations tenues à la main divergeraient. ⚠️ **`f-nav.mjs` N5/N6 re-ciblés une 3ᵉ fois** : il ne reste **aucune paire de pages sans requête**, on repart d'un `page.goto` (cache vidé), et `N5` exige toujours que **`/home` ne demande pas les ladders** — il redemandera une cible le jour où `/home` cessera d'être un stub. **Dette de largeur ASSUMÉE, mesurée, à ne pas bricoler** : à 1280 px les colonnes veulent **646 px pour 575 disponibles** (Date 119 · **Ladder 136** · Opponent 101 · Score 71 · Elo 51 · **Status 168**), donc **71 px hors champ** — sans la colonne Ladder la table ferait 510 px et ne déborderait pas. Aucun levier ne rend ces 71 px sans perdre de l'information (tronquer le nom du jeu sur **la seule page dont le sujet est de mélanger des ladders** ; `MatchStatusPill` est partagé par 3 écrans ; la date ne rend que 44 px), et **la page équipe en cache déjà 122 px** depuis FT-2A. Le vrai correctif est le **rendu en cartes sous `sm`** dans `MatchHistoryTable`, qui servirait les 3 écrans — **carte à part, pas un bricolage local** : **[FX-TABLE]** https://trello.com/c/xdrVnG3H (Todo), qui porte toutes les mesures. **[F-HOME] mergé le 31/07** (commit `836f470`, merge `72c77c0`, **aucune migration**, front pur) : `/home` n'est plus le stub `<h3>Page home!</h3>` — c'est l'écran d'atterrissage d'un compte connecté. 🚨 **CE N'EST PAS UN MIROIR DES AUTRES ONGLETS** : ni grille de jeux ni liste d'équipes (`/games` et `/teams` existent), **uniquement des blocs ACTIONNABLES** — « ce qui demande mon attention ». Si un futur ajout n'est pas actionnable, il n'a pas sa place ici. Livré : hero (artwork `bg.webp` + scrim) · rappel des comptes externes non liés, **fermable définitivement** · « Next up » (carte vedette à compte à rebours + lignes) · « Matches on the clock » · compteur d'invitations · créneaux acceptables · onboarding 3 étapes. **5 requêtes**, `lib/home.ts` + 4 composants `components/home/`, `MatchLineLink` et `link-variants` extraits au 2ᵉ usage, `ActionRequired` remonté dans `components/matches/` (markup inchangé). 🔑 **Tous les blocs sont conditionnels**, donc l'onboarding est le **contenu par défaut** d'un compte neuf — et sa condition est « **aucun match** », pas « page vide » : un compte à 4 équipes et 0 match ne voyait sinon aucun guide (trouvé en usage réel, pas par un check). 🔑 **L'échéance de retrait d'un créneau (`< 15 min` du coup d'envoi) n'est affichée NULLE PART AILLEURS** dans l'app — c'est la seule information réellement neuve de la page ; **ne pas la confondre avec les 24 h** de « Matches on the clock », autre mécanisme. 🔑 **`N5` de `f-nav.mjs` n'a PAS été re-ciblé** (4ᵉ fois évitée) : `/home` n'appelle **pas** `GET /ladders`, le nom du ladder retombe sur `<jeu> <format>` — **exact pour 8 ladders sur 9**, « Counter-Strike 2 2v2 (Wingman) » perd son surnom ; prix choisi, pas une garantie. ⚠️ **`runner.mjs` a changé et il est PARTAGÉ par les 18 scénarios** : `login()` attend la quiescence réseau, sinon les 5 requêtes de `/home` sont avortées par le `goto` suivant et le `net::ERR_ABORTED` est imputé à un scénario innocent. ⚠️ **Le rappel de liaison pointe vers `/profile`** (encore un placeholder) — **arbitrage assumé de David**, qui **diverge volontairement** de `lib/matchmaking.ts:260` (refus en texte seul, « a button leading nowhere is worse than a sentence that stops ») : le pourquoi est écrit dans le code, **ne pas « corriger »**. Seule l'**annonce** au lecteur d'écran a été corrigée, elle affirmait faussement qu'on pouvait lier un compte depuis son profil. 🔑 **LES 5 DÉFAUTS DU TICKET ONT TOUS ÉTÉ TROUVÉS À L'ŒIL, AUCUN PAR LES 297 CHECKS** : liens d'onboarding lus comme des légendes · « in 18 h 43 » lisible comme un horaire (→ `in 18h 43m`) · pastille de statut absente des lignes · onboarding masqué à qui en avait besoin · **`<h1>` clippé de 63 px** à 375 px avec un pseudo long (le hero est `overflow-hidden`, le document ne débordait pas — leçon FT-3 sur un titre). ⚠️ **Contraste MESURÉ, pas estimé** : la recette de `HeroBanner` appliquée telle quelle donnait **1,83:1** sur le `<h1>` et **1,20:1** sur l'intro (l'artwork a une zone claire là où son dégradé devient transparent) → corrigé en **renforçant le scrim**, jamais en éclaircissant le texte (12,63:1 et 6,57:1). ⚠️ **Dette assumée** : `bg.webp` (336 kB, le plus gros asset du build) est désormais chargé sur `/home` alors que le scrim n'en laisse voir que ~16 % ; les 4 chaînes de classes reprises de `HeroBanner` restent — **le 3ᵉ usage devra extraire un `HeroFrame`** (noté en commentaire). **[F-DISPUTE] mergé le 31/07** (commit `fbcc356`, merge `f6345c8`, **aucune migration**, dos à dos avec l'extension back ci-dessus qu'il consomme) : `/disputes/$disputeId` existe — **un litige n'est plus un cul-de-sac**. On pouvait en ouvrir un depuis FT-4B et **aucun écran n'existait ensuite** ; or un litige que personne ne tranche est **annulé au bout de 24 h**, donc qui ne peut pas déposer sa preuve **perd son match par forfait de fait**. Le dossier rend en **un seul appel** le statut + son échéance, les **déclarations contradictoires des deux camps face à face**, le **fil de preuves** (auteur, camp, message, pièce jointe) et — pour le **capitaine** d'un camp, le **joueur** en 1v1 — le dépôt fichier + message avec barre de progression. Livré : `lib/dispute-detail.ts`, `lib/dispute-mutations.ts`, `components/disputes/{DisputeVerdict,DisputeClaims,EvidenceThread,EvidenceForm,EvidencePicker}.tsx`, **`components/ui/progress-bar.tsx` extraite d'`ImagePicker` au 2ᵉ usage** (DOM inchangé), `MatchLineLink` généralisé par **union discriminée** (une ligne en litige mène au dossier, jamais un `<a>` imbriqué), `lib/upload.ts` gagne `fields` (rétro-compatible — l'avatar et le logo envoient un corps **byte-identique**), et `isSoloMatch`/`sideName`/`sideAvatarUrl`/`sideInitials` **élargis à des types structurels** plutôt que recopiés. 🚨 **PAS de chat avec l'admin** (le modèle est : chaque camp dépose, l'admin tranche en laissant des notes — il ne répond pas dans le fil) et **aucun contrôle d'arbitrage ici** : c'est [F-ADMIN], qui ajoutera ses actions **sur cette page**. ⚠️ **`useDispute` porte `staleTime: 0` ET `gcTime: 0`, ne pas « optimiser »** : les `evidenceUrl` sont **présignées ~5 min** (bucket privé) et React Query **peint le cache avant de revalider** — un retour sur la page monterait des `<img>` sur des URLs périmées → 403 → ligne rouge. Le flash de chargement à chaque visite est le prix, écrit en commentaire. ⚠️ **`ImagePicker` n'a PAS été réutilisé** : quatre différences simultanées (types, plafond 5 vs 2 Mo, **un PDF n'a pas d'aperçu**, et sa forme est un contrôle d'avatar rond) ; seule la **barre de progression** était réellement partagée, donc extraite. ⚠️ **Le type de pièce jointe se déduit de l'extension de l'URL présignée**, fiable parce que **le serveur** construit la clé depuis une map fermée : `<img>` seulement sur une image connue, tout le reste en lien. 🔑 **Un `<img>` pointé sur un PDF n'écrit PAS de ligne console** (mesuré : MinIO répond 200, seul le décodage échoue) — la règle reste bonne, son motif est le téléchargement inutile, pas la console. Restent à remplir : `/profile` (F4, Adrien), `/privacy`, `/terms`. → `docs/frontend.md`
- **Tests** : `npm run audit` (audit console Chrome) sort **0 sans filtre** depuis FT-2A — **19 scénarios, 329 checks** (campagne rejouée verte après F-DISPUTE le 31/07, `EXIT=0` lu sur le **process**, sortie capturée dans un fichier et non passée dans un `tail`). 🔑 **F-DISPUTE a produit QUATRE faux verts, tous trouvés en review, aucun par les checks eux-mêmes** — c'est le meilleur inventaire du repo sur cette famille de défaut : ① `D15` comptait les `<img>` d'une ligne PDF, or le propre `onError` du composant retirait l'image **avant** la lecture du DOM → réécrit pour compter la **requête** vers l'objet `.pdf`, qu'aucun gestionnaire ne peut reprendre, avec un **témoin positif** (`D15b` : la vignette PNG **doit** être demandée) ; ② `D19` visait `h2:has-text("Next up") ~ a`, qui résout **0 élément** parce que `SectionTitle` enferme son `<h2>` dans un `<div>` — la carte vedette, seul site d'appel portant un `ariaLabel`, n'était **jamais** inspectée ; ③ le scénario forgeait son dossier « arbitré » **sans poser `resolved_by_user_id`**, donc le contrat le lisait comme un **timeout** : deux checks auditaient l'arbitrage humain sur une donnée qui n'en était pas un ; ④ `D19` n'a tenu que grâce à **5 lignes dont 3 venaient des créneaux de démo posés à la main par un coéquipier** (`bbbb2222-…`) — la dépendance exacte que [FX-AUDIT] interdit, supprimée en rendant la liste facultative. 🔑 **Leçon transverse : un check vert du premier coup ne prouve rien** — casser volontairement ce qu'il garde et le **voir rouge** est la seule vérification qui vaille. 🚨 **[FX-AUDIT] mergé le 31/07** (https://trello.com/c/lodPG1SU, commit `449c6a4`, merge `91325b7`) : **`/matchmaking` est un tableau GLOBAL et la base de dev est PARTAGÉE**, or `matchmaking.mjs` supposait être seul à tenir un créneau sur son ladder (`filter({ hasText: ladderName })`). Un coéquipier ayant ouvert 4 créneaux de démo à la main le 30/07 (uuids `bbbb2222-…`), le sélecteur a résolu **2 lignes**, Playwright a levé, et la campagne complète sortait en **`exit 2` pour TOUT LE MONDE** — un harnais en échec, qui se lit comme un problème d'environnement. 🔑 **Le défaut n'était pas la donnée du coéquipier, c'était le sélecteur** : rien n'a été purgé en base. Chaque créneau ouvert par le run **s'enregistre** (`registerSlot`) et `rowOf()` ne regarde que les lignes dont **le coup d'envoi** est l'un des siens — le seul discriminant disponible, puisque l'anonymat B5b interdit tout nom sur cette ligne. Les 18 appels existants n'ont pas bougé. ⚠️ **§14 est passé d'un `now() + interval '16 minutes'` à un instant ABSOLU** calculé côté Node : un intervalle SQL empêchait le run de connaître le libellé affiché de son propre créneau. ⚠️ **`MM8b` comptait un message sur toute la PAGE** alors que chaque ligne d'un format d'équipe le rend — même cause racine, scopé à sa ligne. ⚠️ Si `formatMatchDate` change de style, les lignes résolvent à **0** : les checks sortent **ROUGES**, jamais en `exit 2` — c'est le mode d'échec voulu. 🔑 **Leçon générale : un scénario ne doit jamais supposer qu'il est seul sur la base de dev** — on peut tous y créer des données à la main. ⚠️ **L'audit tourne sur l'HÔTE, jamais dans le conteneur front** : Chromium vit dans `~/.cache/ms-playwright`, `docker compose exec frontend npm run audit` sort « Aucun Chromium trouvé » — et le message est assez discret pour passer pour un vert si on ne lit que le code de sortie du wrapper. ⚠️ **Ce run-là est sorti `exit 2` la première fois, et ce n'était PAS une régression** : le job d'auto-confirmation 24 h avait fermé le match de démo `awaiting_confirmation` pendant la session, or `match-detail` exige les 7 états cs2 → `docker compose exec backend npm run seed:dev` avant toute campagne, **le seed se périme tout seul** (FX-FOCUS a ajouté 5 checks de focus, tous **au clavier**). 🔑 **UNE seule région live `role="status"` par écran** (FX-FOCUS) : deux se disputent la lecture, et un sélecteur `[role="status"]` en `.first()` prend la première venue — les `Callout` ne portent plus le rôle, l'annonce passe par `lib/use-announcement.ts`. ⚠️ Une région live est **montée en permanence**, donc `waitFor()` sur sa seule présence n'attend RIEN : passer par **`awaitAnnouncement(texte)`** (invariant #11). ⚠️ **`run.mjs` accepte un nom de scénario en argument** : pendant qu'on itère on filtre, la suite complète ne se lance **qu'à la fin** — une passe complète pilote un vrai Chrome sur 10 parcours (~4 min depuis `RATE_LIMIT_FACTOR`) et c'est le poste de temps dominant d'un ticket front. Vitest 27/27 (helpers purs) + **20 suites e2e Python** (`test_matches_me.py` porté de 23 à **65 checks** par B-SOLO). ⚠️ **`helpers.register()` REFUSE désormais un tag hors `alice|bob|carol|dave|erin`** : `cleanup()` ne sait supprimer que ce motif, donc tout autre tag laissait ses comptes en base de dev — on échoue à la création, là où la cause est lisible, plutôt qu'en silence au nettoyage (`cd backend/tests && python3 run_all.py`), sans mocks, sur la vraie base de dev. ⚠️ Les users de test sont **semés en SQL** avec un token forgé (la route `register` reste à 3/min, rien n'est désactivé) : `test_sentinel.py` garde ce couplage, `test_auth_contract.py` couvre la vraie route. → `backend/tests/README.md`

📄 Historique des merges et décisions datées → **`docs/journal.md`**

### Prochaines actions

- 🎯 **POINT DE REPRISE — 31/07 au soir.** **[F-DISPUTE] est mergé** (`f6345c8`), la campagne sort **19 scénarios / 329 checks / exit 0**, et **il ne reste plus aucune page vierge ni aucun cul-de-sac dans le rail**. Deux cartes prêtes en Todo, aucune n'est bloquée :
  1. ✅ ~~**[F-DISPUTE]**~~ **FAIT** (voir les lignes Backend et Frontend). 🔑 **Ce qu'il faut en retenir pour la suite** : la carte annonçait « front pur, zéro back » et **c'était faux** — l'échéance qu'elle exigeait était incalculable, et la page attribuait à un arbitre l'annulation automatique du job. **Grep le HANDLER avant de croire une carte sur ce que l'API sert déjà** (même leçon que B-MM, deuxième fois). ⚠️ **Dette assumée, décision de David** : l'en-tête du dossier est la **4ᵉ copie** de l'idiome `GameBanner` + `<h1>` + rangée de méta (après `MatchHeader`, `game-detail`, `ladder-detail`) et le `HeroFrame` promis par F-HOME **n'a PAS été extrait** — le gain est cosmétique, le risque serait de casser trois écrans qui vont bien. **Ne pas le « corriger » en croyant à un oubli.** ⚠️ **Passation pour [F-ADMIN]** : le lien d'en-tête du dossier mène à `/matches/$matchId`, que `GET /matches/{id}` **refuse en 403 à un admin non-participant** sur un match `disputed` — inatteignable aujourd'hui, mais à conditionner **dès que [F-ADMIN] rend cette page atteignable par un arbitre**. Rien d'autre sur cette page n'appelle `/matches/{id}`.
  2. **[F-ADMIN]** (https://trello.com/c/gkuGGusY, front + **1 ligne de back**) — la file d'arbitrage, invisible aujourd'hui. 🔴 **PRÉREQUIS : `isAdmin` n'est PAS exposé par l'API.** La colonne `users.is_admin` existe (`schema.ts:59`) et `frontend/src/types/auth.ts:11` la déclare **déjà**, mais ni `openapi.yaml` ni le handler `/users/me` ne la renvoient — elle vaut `undefined` en permanence. **Aucun contournement** : sonder `GET /disputes` produirait un 403 en console chez tous les non-admins. Ensuite : section « Disputed matches » sur `/home` (conditionnelle, **une requête de plus réservée aux admins**) + les 3 issues de `POST /disputes/{id}/resolve` **sur la page de F-DISPUTE**, pas sur un 2ᵉ écran. ⚠️ Le scénario d'audit aura besoin d'un **compte admin**, absent des fixtures (le runner sait faire un `psql` depuis FT-4B).
  3. **[F-PLAYER]** (https://trello.com/c/AZ3U4BZl, **branche de William déjà à 90 %**) — voir la ligne `page/player` plus bas : il ne reste qu'à retirer la redirection `/profile`, supprimer un composant mort, squasher, et ajouter **ses classements (Elo + V/D par ladder) et ses équipes** en enrichissant `GET /users/{pseudo}`. 🚨 **PAS d'historique de matchs ni de comptes de jeu liés sur le profil d'un inconnu** — décision de David (vie privée) : ça se consulte sur **son propre** compte (`/history`, `/profile`). **Ne pas le reproposer.**

- ✅ **LOT DE FIX TERMINÉ (28/07)** : les 5 cartes de *Issues / bug fixes* sont mergées — **[B12]** (rate limit 2FA par compte), **[B13]** (204 sur `/auth/refresh`), **[BX-DEL]** (suppression de compte, **migration `0023`**), **[FX-FOCUS]** (focus rendu après une suppression), **[FX-MIDNIGHT]** (jour incohérent à minuit). La liste ne contient plus que **[B12B]** (https://trello.com/c/xC70Wqjf), née de la review de B12 : `/2fa/enable` et `/2fa/disable` vérifient un code à 6 chiffres à 100/min, 20× le quota de `/verify`. ⚠️ **Au prochain pull : `docker compose restart backend`** (migration 0023).

- ✅ **[FT-4A] MERGÉ le 29/07** (commit `5729540`, merge `436531e`, **aucune migration**) — `/matches/$matchId` n'est plus un placeholder : fiche **en lecture seule**, les 7 états du cycle, les deux camps face à face (score Bo3, vainqueur, **Elo par CAMP jamais par joueur**), maps pilotées par la donnée, lineups avec capitaine, variante **1v1**, 403/404. L'historique d'équipe rend **toutes** ses lignes cliquables pour un membre (le lien passe sur la **date** quand il n'y a pas de nom d'adversaire). `MatchStatusPill` + `match-status.ts` remontés dans **`components/matches/`** (2ᵉ consommateur), `GameBanner` extrait au 3ᵉ usage, `RosterChips` rendu générique, `Callout` gagne un ton `danger`, `lib/match-detail.ts` porte le hook et les dérivations pures. **⚠️ Au prochain pull : `docker compose exec backend npm run seed:dev`** — le seed a changé, et le scénario `match-detail` **exige** la base semée (sinon `exit 2`).
  - 🔑 **LE PIÈGE À NE JAMAIS RÉINTRODUIRE** : « ce camp n'a pas d'équipe » **ne signifie PAS 1v1**. `match_sides.team_id` est en `set null` et une équipe dont tous les matchs sont terminés **peut** être dissoute — son camp survit alors avec `team: null` sur un 5v5 `completed`, lisible par n'importe quel compte depuis B15. Lire ça comme « solo » renommait le camp d'après son 1ᵉʳ joueur et **supprimait la lineup des 5 joueurs**. C'est le **format du ladder** qui tranche (`isSoloMatch`, enum fermé du contrat). Le libellé de repli est « Disbanded team », **non couvert par une donnée ni un check** → FT-4B ne l'a pas couvert non plus (il faut une équipe dissoute **après** un match terminé pour le voir), reporté au futur ticket « seed propre ».
  - ⚠️ **Un créneau ANNULÉ a la même forme qu'un créneau ouvert** (un seul camp) : la fiche doit lire le **statut**, jamais déduire « encore acceptable » de l'absence d'adversaire. Elle se titrait « open slot » et annonçait « any team can accept it » **sous une pastille CANCELLED**. Bloquant de review, corrigé, gardé par un check **vu ROUGE avant vert** — et c'est pour lui que le seed porte un **7ᵉ** match cs2.
  - ⚠️ **Le seed sème 8 matchs de démo** (7 cs2 + 1 chess 1v1) et le créneau « accepté, avant l'heure » est à **+2 JOURS**. Il était à +1 h : l'état cessait d'exister une heure après le seed et la campagne partait en `exit 2` l'après-midi. **Ne pas raccourcir** — rien ne plafonne l'avance côté API.
  - ⚠️ **`backend/openapi.yaml` a été modifié par un ticket FRONT** : le payload de B16 ne déclarait aucun `required`, la codegen sortait donc tout en optionnel. Vérifié en review — les `required` décrivent exactement ce que le handler renvoie, aucun handler touché, codegen byte-identique.
  - 🔑 **Deux checks ne gardaient rien**, trouvés en 2ᵉ review : l'un comptait 2 nœuds (`text=Cancelled` matchait la pastille **et** le mot dans la phrase) et serait resté vert en supprimant ce qu'il surveille ; l'autre lisait un attribut avec `getAttribute()`, qui **lève** quand rien ne matche → `exit 2` (harnais en échec) au lieu d'un rouge imputable au ticket. **Compter avant de lire.**

- ✅ **[FT-4B] MERGÉ le 29/07** (commit `c2f9019`, merge `8a39a92`, **aucune migration**, https://trello.com/c/3mjx0M31) — la fiche de match cesse d'être en lecture seule : **soumettre son score, confirmer celui de l'adversaire, le contester**. Le cycle challenge/accept est désormais bouclable **entièrement à la souris**, de l'ouverture du créneau à l'Elo appliqué. Livré : `components/matches/MatchResultPanel.tsx`, `lib/match-mutations.ts`, `lib/match-result-schema.ts`, `components/ui/option-tile.tsx` (extrait de `CreateMatchPanel` au 2ᵉ usage), `canReportResult` dans `lib/match-detail.ts`. **Le panneau ne rend RIEN à qui ne peut pas agir** (visiteur, banc, match hors des statuts scorables, camp ayant déjà soumis) et **aucun contrôle avant le coup d'envoi** — un bouton que l'API refuserait laisse une ligne rouge en console, motif de rejet.
  - 🔑 **`nowMs` vient de `matchQuery.dataUpdatedAt`, plus d'un instantané au mount** : une horloge figée gardait le formulaire caché tant que l'onglet restait ouvert, rechargement pour seul remède. ⚠️ Mais elle avance sur un **refetch**, pas sur l'horloge murale — un onglet resté au premier plan à travers le coup d'envoi n'ouvre toujours pas le formulaire tout seul. C'est assumé (`refetchOnWindowFocus` suffit : on rapporte des parties jouées **ailleurs**, donc l'onglet est quitté puis repris), et un `nowMs` en retard ne peut que **cacher** le formulaire, jamais offrir un bouton que le serveur refuserait.
  - 🔑 **`sql.mjs` : le runner d'audit sait désormais faire un appel système** (`docker compose exec postgres psql`, `spawnSync` sans shell, uuid passés par `assertUuid`). C'était la capacité manquante : reculer `scheduled_at` est la **seule** opération de la fixture que l'API ne sait pas faire, et sans elle aucun match ne peut être scoré dans un test. Le scénario tourne sur un ladder **1v1** (2 comptes jetables au lieu de 10).
  - ⚠️ **`R11b` garde un ORDONNANCEMENT, et c'est pour ça qu'il existe séparément de `R9`** : `ConfirmDialog` ne retombe sur `returnFocusRef` **que si son ouvrant a quitté le DOM** (`opener.isConnected`) — sur le chemin « Confirmer », la restitution de la plateforme court contre le démontage du bouton par le refetch. Mesuré vert 4 fois de suite, jamais déduit. ⚠️ **Un `.focus()` appelé sous un `<dialog>` modal est un NO-OP** (la page est inerte) : le commentaire qui affirmait le contraire était faux et a été corrigé.
  - ⚠️ **`awaitFocusRestored` trace désormais le délai réel au-delà de 1 s** (sans faire rougir). Le plafond avait été relevé 5 s → 15 s pendant ce ticket pour une instabilité d'`I6-bis` : à 15 s, « le focus arrive en 200 ms » et « en 9 s » étaient le **même vert**. Constat des deux campagnes complètes : **aucune ligne « ⏱ »**, donc le plafond n'est jamais approché — c'est une marge, pas un cache-misère.
  - ⚠️ **Toujours pas couvert : le libellé de repli « Disbanded team »** (hérité de la passation FT-4A) — ni donnée ni check. Il faut une équipe dissoute **après** un match terminé pour le voir ; à porter au futur ticket « seed propre » (clear complet), pas à re-ticketer seul.

- ✅ **[FX-ROW] MERGÉ le 30/07** (commit `c94d832`, merge `7ee0517`, **aucune migration**) — reviewé par `reviewer-front` Opus (**zéro bloquant**, 10 non-bloquants dont l'essentiel appliqué), build conteneur + lint + `tsc -b` propres, et **campagne `npm run audit` complète rejouée verte (exit 0)**.
  - 🔑 **L'exit code de `run.mjs` est l'agrégat qui fait foi**, pas la lecture du texte : `worst = max(report(...))` sur les 13 scénarios puis `process.exit(worst)`, et `report()` ne rend **0** que si **tous les checks sont verts ET zéro entrée console imputable** (1 sinon, 2 sur crash). ⚠️ **Ne jamais passer la sortie de l'audit dans un `tail`** — ça tronque les scénarios de tête, dont celui du ticket, et il ne reste que l'exit code pour conclure.
  - ⚠️ **Vérifier que la base est semée AVANT de lancer une campagne** : elle était **vide** le 30/07 (0 users / 0 teams / 0 matches, seuls les 9 ladders de référence) et `match-detail` **exige** les 8 matchs de démo → `docker compose exec backend npm run seed:dev`, sinon `exit 2`.
  - **Ce que ça corrige** : dans l'onglet Matches d'une équipe, le nom de l'équipe adverse ouvrait la fiche du **match** — cliquer « Team Bravo » n'amenait pas sur Team Bravo. Désormais **le nom → sa page d'équipe**, **le reste de la ligne → la fiche du match**.
  - 🔑 **La date porte le lien vers la fiche sur TOUTES les lignes ouvrables**, plus seulement celles sans adversaire : c'est l'**accès clavier et lecteur d'écran**. Un gestionnaire de clic sur une ligne n'est ni focusable ni ouvrable dans un onglet — il ne peut jamais être le seul accès. **Ne pas « simplifier » en retirant ce lien.**
  - ⚠️ **Les 4 gardes du clic de ligne, chacune gardée par un check** : élément interactif (`closest('a,button,input,select,textarea,summary,[role=button]')` — à **tenir à jour** si la ligne gagne un contrôle, sinon il est avalé **silencieusement**), sélection de texte par glisser, **clic modifié** (Ctrl/Maj/Alt demande un onglet que `navigate()` ne sait pas donner : la ligne reste **inerte**, le lien de la date l'ouvre normalement). Le **double-clic navigue quand même** — assumé et documenté, le 1ᵉʳ clic part avant que la sélection existe.
  - ⚠️ **Écart non couvert par un check** : le lien du nom est gardé par la présence de l'**équipe**, plus par le droit d'ouvrir la fiche — un **non-membre** gagne donc ce lien là où il avait du texte brut. Correct (une page d'équipe est lisible par tout compte connecté, aucun 403 possible), mais le scénario ne parcourt cette phase **qu'en membre**.
  - 🔑 **3 checks étaient verts PAR CONSTRUCTION avant d'être corrigés** : un comptage sur un locator déjà réduit par `.first()` ; un check pris sur la 1ʳᵉ ligne de l'historique, qui est le **créneau ouvert** (sans adversaire, donc sans cible concurrente — le seul cas que l'ancien code traitait déjà) ; et `waitForURL(/\/teams\/…/)` **rend la main immédiatement** quand l'URL courante matche déjà le motif → passer un **prédicat sur la cible exacte** (`url.pathname === href`). Corollaire vécu deux fois : un check doit sortir **ROUGE**, jamais faire échouer le harnais (`boundingBox`/`waitForURL` **lèvent** → `.catch()`, et chaque check du groupe repart de l'historique via `ensureHistory()`, sinon UN rouge en cascade fait sortir les suivants en `exit 2`).
  - ⚠️ **`TeamOverview` porte le MÊME `aria-label` « Cancel the slot of … »** que la ligne, et l'onglet Overview **reste monté, masqué**, derrière l'onglet Matches : un sélecteur non scopé au tableau attrape ce bouton invisible et le clic **expire à 30 s** (`exit 2`).

- 🚨 **DEUX BRANCHES DE COÉQUIPIERS SE DISPUTENT `/profile` (30/07).** ✅ **Tranché par David : c'est ADRIEN qui gère le conflit sur SA branche** (`feature/f4-profil-page`) — ne pas le traiter à sa place, ne pas modifier `page/player` pour lui. Les deux branches ne partagent **AUCUN fichier**, donc **git mergerait les deux proprement et l'app perdrait silencieusement une des deux fonctionnalités** — aucun marqueur de conflit n'avertira personne. C'est le piège à garder en tête au moment des merges.
  - **`feature/f4-profil-page`** (acattet / Adrien, 3 commits, carte [F4] https://trello.com/c/73sIH2T8) : `/profile` = **page de réglages** (`pages/profile.tsx` + `components/profile/` : ProfileForm, AvatarUploader, PasswordChange, TwoFactorSettings, + `ui/textarea.tsx`).
  - **`page/player`** (Omshinwa / William, `fynmorph@gmail.com`) — ⚠️ **RÉINSPECTÉE LE 31/07, cette ligne était PÉRIMÉE** : la branche ne fait plus 197 lignes mais **1214**, commit courant `c603655` (message « . », à squasher), rebasée sur `25e9086`. 🔑 **Elle NE renomme PLUS la route au singulier** — elle reste sur `/players/$pseudo`, donc **le conflit `tsc -b` annoncé plus bas n'existe pas**. Livré : `PlayerHero` (calqué sur `TeamHero`), `ui/stat.tsx` extrait et partagé, `lib/player-detail.ts` + `lib/player-mutations.ts`, **et un enrichissement BACK de `GET /users/{pseudo}`** (la relation d'amitié entre visiteur et profil, `or` sur les deux orientations du couple, projection explicite). 🔑 **Sa page est DÉJÀ en lecture seule sur son propre profil** — commentaire textuel dans son code : « *Empty for the owner: this page is read-only, editing lives at /profile* », soit exactement le modèle tranché par David le 31/07. **Reste en travers** : `routes/_authenticated.profile.tsx` **redirige toujours** `/profile` → `/players/$pseudo` (le seul vrai conflit avec [F4]), et `ui/avatar-upload-button.tsx` (75 l.) n'est **importé nulle part**. ✅ **À GARDER au rebase** : son `vite.config.ts` ajoute `watch.ignored` sur `**/tests/**` et `**/*.md` — c'est le correctif **structurel** de l'invariant #10 (écrire un fichier sous `frontend/` pendant une campagne déclenchait un full reload et un `exit 2` imputé au ticket). Carte **[F-PLAYER]** créée le 31/07 : https://trello.com/c/AZ3U4BZl
  - ⚠️ **Conséquence selon l'ordre de merge** : William après Adrien → la page de réglages devient **inatteignable** (la route redirige avant de rendre), mot de passe et 2FA disparaissent de l'UI sans qu'une ligne soit supprimée. Adrien après William → la redirection est écrasée et l'upload d'avatar de la page joueur devient redondant.
  - 🔑 **Ça conditionne la carte « suppression de compte + liaison de provider »** : si le modèle William gagne, ces deux fonctionnalités n'ont plus de page évidente.
  - ⚠️ `page/player` n'a été qu'**inspectée, pas reviewée**. 🔑 **Les écarts notés ici le 30/07 sont pour la plupart CORRIGÉS** (réinspection du 31/07) : le `<h1>` **existe** (`PlayerHero.tsx:89`, avec `headingId`), et le code passe par **TanStack Query**, `useAnnouncement`, `ConfirmDialog`, une seule région live. Restent : **aucun scénario d'audit** pour cette page (⚠️ **c'est David qui l'écrira**, retiré de la charge de William) et le commit à squasher. **Ne pas se fier à une inspection ancienne pour juger cette branche — la relire avant d'en parler.**

- 📋 **[F4] REVIEWÉE ET RENVOYÉE À L'AUTEUR le 30/07** (carte repassée en **In Progress**, 5 commentaires postés 1/5→5/5, DoD complétée). ⚠️ **Le commentaire Trello doit être découpé** : le MCP passe le texte en paramètre d'URL, un seul commentaire de ~15 000 caractères sort en **414**. Verdict : **non mergeable en l'état**, mais **la couche métier tient** (les 7 endpoints confrontés un par un à `openapi.yaml` ET aux handlers : tous justes). 5 bloquants — ① les 2 rails (`LeftRail`/`RightRail`) modifiés depuis une base d'avant F-Nav : importent `layout/Logo` (**supprimé**, remplacé par `SiteLogo.tsx`) et pointent `to="/ranking"` (**route supprimée par FT-3**) → `build` ET `tsc -b` cassent, et la perte du `hidden lg:` fait 432 px de chrome pour 375 px d'écran sur **toutes** les pages authentifiées. **La carte interdisait déjà de toucher aux rails** → prendre master en entier au rebase, rien à récupérer. ② `PATCH /users/me/password` sans `skipAuthRefresh` : `lib/api.ts` lit **tout** 401 comme « token mort » → refresh, puis `logoutAfterAuthFailure()` si le cookie est absent → **déconnecté pour une faute de frappe**, et chaque essai part **2 fois** donc 429 dès le 3ᵉ. ③ « Change password » rendu aux comptes OAuth → **400 systématique** offert par l'UI. ④ `AvatarUploader` réimplémente `ui/image-picker.tsx` **sans la barre de progression** (`fetch` n'a pas d'événement de progression : c'est `lib/upload.ts`/`uploadFile` qu'il faut). ⑤ aucun scénario d'audit. 🔑 **Leçon transverse** : la carte disait « réutiliser les composants de **F0** » — liste écrite **avant FT-2B**, donc l'auteur a suivi la carte à la lettre et a quand même dupliqué. **Un brief de réutilisation doit porter l'inventaire À JOUR**, pas un renvoi à un ticket ancien.

- ✅ **[B-SOLO] + [F-SOLO] MERGÉS le 30/07** (merges `e9fce76` puis `8d7dd34`, **aucune migration**) — cartes https://trello.com/c/DruYEIQi et https://trello.com/c/dqViKaCA. Le détail est dans les lignes Backend et Frontend plus haut. Ce qu'il faut retenir pour la suite :
  - 🔑 **La question « enrichir une route existante ou en créer une pour le solo ? » a été tranchée avec un argument reproductible** : `/history` aura besoin de la forme polymorphe de toute façon, donc une route solo-only aurait fait écrire le shaping **deux fois** et serait devenue du poids mort. À réappliquer telle quelle si la question revient (elle reviendra sur `/history`).
  - ⚠️ **Deux checks manquaient à la première livraison de F-SOLO** et ont été ajoutés après review : l'ouverture d'une fiche de match **depuis** l'historique solo (`canOpenSheet: true` était codé en dur, un 403 y laisserait une ligne rouge) et le lien adversaire → `/players/$pseudo`. 🔑 **Le second est sorti ROUGE parce que le sélecteur était trop lâche** : `getByRole('link', {name: pseudo})` en attrapait **deux**, le lien de la date portant `aria-label="Match sheet against <pseudo>…"` — **Playwright matche le nom accessible en SOUS-CHAÎNE par défaut**. Viser le libellé exact.
  - ⚠️ **Un non-bloquant de review a été RÉFUTÉ, pas reporté** : « conditionner les 3 requêtes de `/solo/$ladderId` à la garde de format » n'économise **rien** — les trois hooks partent dans le **même rendu**, le format n'est pas encore connu. Le seul moyen serait de les enchaîner, ce qui coûte un aller-retour à **chaque visite légitime**. Ne pas le re-ticketer.
  - ⚠️ **Point de recette impossible à automatiser** : le rendu d'un classement **renseigné** (bande de stats + « Elo · #rang » sur la tuile) — fabriquer une ligne `rankings` exige un match terminé que le scénario ne produit pas. Se connecter en `alice@dev.local` / `Test1234!` → `/solo` → la tuile Chess doit afficher `1560 Elo · #1`. **Vérifié à l'œil le 30/07, conforme.**
  - ~~⚠️ **`MatchRow` ajoute un consommateur de `to="/players/$pseudo"`** — la branche `page/player` renomme cette route au singulier.~~ ❌ **FAUX depuis le 31/07** : William a **abandonné** le renommage, sa branche reste sur `/players/$pseudo` (pluriel). **Aucun conflit `tsc -b` à attendre de ce côté.**

- ✅ **[B-MM] + [F-MM] MERGÉS le 30/07** (B-MM commit `a03a4de` / merge `3cf425a` · F-MM commit `dc53ad3` / merge `b3278fb`, **aucune migration**) — cartes https://trello.com/c/mr6mP1pT et https://trello.com/c/ksD4k88P, toutes deux en Done. **`/matchmaking` livre la moitié ACCEPT du cycle** : avant, le front n'appelait **jamais** `POST /matches/{id}/accept`, seuls le seed et l'audit acceptaient par appel API direct — les phrases « atteignable / bouclable à la souris » de FT-2C et FT-4B étaient **fausses**, elles sont vraies maintenant. Campagne d'audit **exit 0 / 16 scénarios / 265 checks**. Les deux reviews (Opus) ont rendu **zéro bloquant** ; 6 non-bloquants back et 8 front appliqués.
  - 🔑 **TROIS défauts sont sortis de l'USAGE RÉEL de David, aucun des 265 checks** : un `?ladderId=` qui filtrait le tableau pendant que les deux sélecteurs affichaient « All games / All formats » (filtre actif et invisible) ; le sélecteur Format qui proposait les 4 formats du contrat, donc Valorant + 1v1 → tableau vide ; et un encadré « no open slot » affiché **au-dessus du créneau ouvert du lecteur** (l'API exclut les siens). **Une passe à l'œil vaut plus qu'un ticket de code** — voir [[feedback-ask-screenshot-before-done]].
  - 🔑 **Je peux prendre des captures et les LIRE** : script Playwright dans le scratchpad + `Read` sur le PNG. C'est comme ça que le 3ᵉ défaut est sorti. À refaire sur toute page front livrée.
  - ⚠️ **`MM9c` est sorti VERT sur du code volontairement cassé** : quand la valeur d'un `<select>` ne correspond à **aucune option rendue**, le navigateur affiche l'option vide de lui-même — lire le contrôle ne prouve rien, il faut lire le **tableau**.
  - ⚠️ **L'audit tourne sur l'HÔTE, jamais dans le conteneur front** (Chromium est dans `~/.cache/ms-playwright`) : `docker compose exec frontend npm run audit` sort « Aucun Chromium trouvé », assez discret pour passer pour un vert.
  - 🔑 **Deux trous jugés « non fabricables » l'étaient** : la panne de `GET /teams` (via `page.route`, **nouvelle capacité du harnais**) et le créneau pris sous le clic (un tiers l'accepte **par API** entre la confirmation et le clic — aucune course à gagner).
  - ⚠️ **Dates en `en-GB`** (`lib/match-slots.ts`, `lib/match-detail.ts`) : noms anglais, **ordre jour-mois et 24 h conservés**. Pas `en-US` (inverserait l'ordre et passerait en AM/PM). Locale **figée**, jamais celle du navigateur.
  - 🔑 **La leçon de méthode de ce lot** : le ticket a été cadré sur un fait FAUX que j'avais affirmé — « aucune route ne liste les créneaux ouverts ». `GET /matches?ladderId=` existait, documentée et testée ; j'avais lu le `post` dans `openapi.yaml` sans dérouler jusqu'au `get`. **Grep le HANDLER, pas seulement le contrat**, avant de conclure qu'une route manque.
  - 🚨 **Deuxième correction de cadrage, produit celle-là** : la première version de la carte front affichait l'adversaire et son Elo sur chaque ligne. Ça **contredisait l'anonymat B5b**, décision documentée et gardée par des tests. L'anonymat a été conservé et les deux cartes corrigées. **Ne pas le relever au prochain ticket** en croyant à un oubli.
  - ⚠️ **Ce que ça change pour la suite** : la phrase « le cycle est atteignable / bouclable à la souris » (FT-2C, FT-4B) était **fausse** jusqu'à F-MM — il manquait l'accept. Elle est vraie maintenant.

- ✅ **LES DEUX CHANTIERS DU 30/07 SONT SOLDÉS (31/07)** — **[F-HIST] mergé** (voir la ligne Frontend) et **la 2ᵉ passe de review de [FS-0] est faite**.
  - 📋 **[FS-0] — 2ᵉ passe rendue, rapport dans `/home/pinto/review-fs0-passe2.md`** (une copie est sur le bureau Windows de David). **Les 3 bloquants sont réellement corrigés**, vérifiés dans le code : `teams-manage` scopé à `<main>`, plus aucune région live permanente dans `components/social|layout`, et `sameSession` ne dépend plus que du `userId` (avec le *pourquoi* en commentaire — le back ne vérifie le JWT qu'au handshake). 8 non-bloquants sur 10 soldés, 1 reporté avec raison validée (la migration du modal vers `<dialog>` natif), 1 à moitié. 🚨 **CE QUI BLOQUE ENCORE N'EST PAS DANS LE CODE** : sa branche est rebasée sur `25e9086`, master a **beaucoup** bougé depuis (B-MM, F-MM, puis F-HIST + le fix d'audit → `d346356`). Sa campagne « 16 scénarios / exit 0 » **n'a jamais exécuté `matchmaking.mjs`** (23 checks, arrivé avec F-MM), qui bascule en **375 px** et y mesure le débordement du document — précisément le viewport où FS-0 monte un `MobileHeader` neuf en marges négatives. **Même classe de défaut que son bloquant ①**, sur un scénario qui n'existait pas à son rebase. → rebaser sur master, reséminer, campagne complète (**18 scénarios** désormais), exit 0 exigé. ⚠️ Non-bloquant le plus utile à lui faire faire tant qu'il est dans les fichiers : **11 sélecteurs d'onglet non scopés** (`teams-detail` ×3, `teams-matchmaking` ×5, `match-detail` ×1, `teams-manage` ×2) — verts **par chance de nommage** (le rail propose Friends/Messages/Add friend, pas Overview/Matches/Manage), et FS-1→5 les feront tomber en `exit 2`.
  - 🚨 **LA BASE DE DEV CONTIENT DES DONNÉES DE DÉMO POSÉES À LA MAIN** (30/07 19:37, uuids déterministes `bbbb2222-…`, 4 créneaux `pending` sur Valorant 2v2 / RL 3v3 / cs2 2v2 / Valorant 5v5, capitaines `alice`/`dave`/`judy`/`tester93@hotmail.fr`). **Ce n'est pas le seed.** Elles n'ont **pas** été purgées — c'est l'audit qui a été durci (voir la ligne Tests). **Ne pas les supprimer sans demander à l'équipe**, quelqu'un travaille peut-être avec.
  - *(Rappel de ce qui avait été décidé le 30/07 au soir, pour mémoire :)*
  1. ~~**[F-HIST]** (https://trello.com/c/uxsI4TUr) — `/history`, la dernière page vierge.~~ **FAIT.** **Front pur, aucun ticket back** : `GET /matches/me` est déjà exactement ça (c'est pour elle que B-SOLO l'a rendue polymorphe), sans pagination, en un appel. 🔑 **Périmètre tranché : MES matchs**, tous jeux et ladders confondus, solo et équipe réunis — **pas** un flux global du site (aucune route ne le sert, et lire les résultats d'inconnus n'est utile à personne). 🚨 **PAS de bilan V-D global** : un V-D inter-ladders ne veut rien dire (10 victoires aux échecs + 10 défaites cs2 ≠ 50 %), et le record par ladder existe déjà là où il a un sens. Le vrai gain de la page = **la section « à traiter »** (`awaiting_confirmation` / `disputed`), que rien ne regroupe aujourd'hui alors que le job d'auto-confirmation ferme à **24 h**.
  2. ~~**Review de [FS-0]** (https://trello.com/c/9Dz6Y3LN) — le rail social de **Walid**.~~ **FAITE** (2ᵉ passe, voir plus haut). Charger [[project-f-social-decomposition]] avant d'y retoucher. ⚠️ Sa branche reste basée sur un master d'avant F-SOLO / F-GAMES / F-MM : lire le diff contre `git merge-base`, **jamais** contre master, et son rebase conflictera sur `RightRail`, `AuthenticatedLayout`, `ui/tabs.tsx`, `index.css`, `openapi.yaml` et les fichiers de doc.

- 🎯 **Ensuite : à décider avec l'équipe** — la priorité a changé le 30/07. 🚨 **La liaison de compte externe est le SEUL trou front qui empêche un utilisateur réel d'utiliser la fonctionnalité centrale** : `GET`/`POST`/`DELETE /users/me/external-accounts` existent et fonctionnent, le front **ne les appelle JAMAIS** (seuls les types générés les mentionnent), et la garde est **active** (`matches.ts:311` refuse une lineup contenant un joueur non lié → 400 `unlinkedPlayers`, et `CreateMatchPanel.tsx:338` grise déjà ces joueurs). Donc un vrai utilisateur sur cs2 **ne peut pas lier son Steam, donc ne peut aligner personne, donc ne peut pas ouvrir de match** — le cycle n'est « atteignable à la souris » que parce que le seed et l'audit lient les comptes **par appel API direct** (`teams-matchmaking.mjs:157`). ✅ **Pas bloqué par le back restant** : `openapi.yaml:394` dit l'`externalId` « saisi manuellement au MVP » et `verified` « toujours false au MVP », et **la garde ne lit pas `verified`** — une liaison déclarative suffit. ⚠️ `components/home/LinkAccountBanner.tsx` existe **sans aucun appelant** mais **sa copie viole la décision produit** : « you cannot **enter the queue** » et « start **matchmatching** » (faute de frappe) — à réécrire, il n'y a pas de file d'attente. Autre trou pur : **`DELETE /users/me` (BX-DEL, back fini) n'a AUCUNE UI**. ✅ **Les deux sont réunis dans UNE carte, créée le 30/07 : [F4B]** (https://trello.com/c/WBWaXu34, Todo) — même page, même nature, même scénario d'audit, les séparer ferait payer deux fois la mise en place. ⛓️ **Elle ne démarre QUE quand [F4] est mergé sur master** (elle ajoute 2 sections à la page de réglages de F4 : la commencer avant garantit un conflit sur `pages/profile.tsx`). Autres candidats, aucun bloquant : **[B12B]** (https://trello.com/c/xC70Wqjf), le **stub `/home`** (`/games` est fait depuis [F-GAMES]), le **rail en `hidden lg:flex`** (sous 1024 px : ni navigation ni déconnexion), `server.ts:119` **`max: 100` en littéral** (source des 429 en campagne, invariant #13), et le **rail social** (F-Social, 7 cartes déjà découpées en mémoire).

- ✅ **[FT-3] MERGÉ le 28/07** (commit `715aa9c`, merge `65de356`) — page `/ladders/$ladderId` : identité + artwork, règles en clair, pool de maps servi par l'API, classement complet (joueurs **et** équipes), ligne de sa propre équipe surlignée. `GET /ladders/{id}` enrichi du **pool de maps du JEU** (même table que `POST /matches` : la page ne peut pas annoncer des maps que le serveur n'attribuera pas) ; section pilotée par la **donnée**, pas par une liste de jeux en dur — seuls **cs2 (7) et valorant (6)** ont des maps. `LadderRow` + `LadderBoard` extraits de `LadderExcerpt` (règle du second usage), helpers dans `lib/ladders.ts`, `ui/error-panel.tsx` extrait. ⚠️ **Le code venait d'un chat mobile, sans `coder-front` ni review** : le rapport `reviewer-front` (Opus) a sorti 3 bloquants + 9 non-bloquants, tous soldés avant merge. **3 écarts assumés, à ne pas « corriger » par erreur** :
  1. ⚠️ **La carte Trello ment sur une règle** : elle annonce « slot sans adversaire annulé au bout de 24 h ». **FAUX** — `cancelExpiredSlots` (`jobs/index.ts`) l'annule dès qu'il passe **sous `MIN_LEAD_MINUTES` (15 min) de son PROPRE coup d'envoi**, l'instant où plus personne ne peut l'accepter. Les 24 h de la règle « Score » sont un autre mécanisme (match joué mais non rapporté). C'est la règle du **code** qui est affichée.
  2. ⚠️ **`/ranking` SUPPRIMÉE** (page, route, `RankingTable`, entrée de nav) au lieu d'être migrée. Décision de David. ✅ **DETTE SOLDÉE par [F-GAMES] le 30/07** : `/games → /games/$gameId` est désormais le point d'entrée vers un classement pour un compte **sans équipe**. (`play`/`find party` restent des `MenuItem` sans `to`.)
  3. ⚠️ **NI pagination NI conteneur défilant** — décision de David prise **avec la mesure** : à 200 compétiteurs la page fait **11 431 px (16 écrans)** pour 2941 nœuds DOM et 1278 ms. Le coût DOM n'est pas le sujet ; on accepte de descendre la page et on garde le **Ctrl+F sur tout le classement** (une pagination ferait atterrir un 137ᵉ sur une page 1 sans lui). Rationale figée dans le docblock de `LadderBoard.tsx`. **Ne pas re-ticketer avant qu'un ladder dépasse la centaine.**
  - 🔑 **Défaut invisible trouvé au passage** : à 375 px le nom du compétiteur était rendu **0 px de large** (tracks fixes + gaps + padding = 268 px d'une boîte de 276, avatar `shrink-0`), 73 px sortaient **en silence** car `LadderBoard` clippe. Le check de largeur ne pouvait pas le voir — il mesurait `documentElement`, il était **vert par construction**. Corrigé le check d'abord (`L9b` mesure la boîte ET la largeur rendue du nom), le défaut ensuite : sous `sm` les 3 nombres passent sous le nom via **`sm:contents`**, au-dessus la grille à 5 colonnes est restaurée à l'identique.

- ✅ **[F-Nav] MERGÉ le 29/07** (commit `4a5c4bb`, merge `822a6eb`) — rail gauche : wordmark, recherche, 6 items, bloc compte. **Livré une 1ʳᵉ fois sans suivre la maquette, puis RECODÉ** : `vsmode-home-demo.html` (`.left`/`.nav-item`/`.sep`) fait foi pour la **structure et les dimensions**, `index.css` pour couleurs/radius/ombres. Écarts corrigés : 288→**264 px**, `p-6`→**18/16**, wordmark 48 px italique→**30 px droit**, icônes 20→**16 px**, et la recherche sortie du `<nav>` (ce n'est pas un lien). 🔑 **La maquette contenait déjà la solution d'un défaut trouvé en review** : l'actif se distingue du survol par la **bordure** (`.nav-item.active` ajoute `border-color`, `:hover:not(.active)` ne la touche pas) — la 1ʳᵉ passe avait gardé le fond et jeté la bordure, rendant les deux états **identiques au pixel**.
  - **`ranking` SUPPRIMÉ** du rail (absent de la maquette) : ça règle du même coup le fait que `to="/ranking"` **cassait `npm run build` de master au merge** (route retirée par FT-3, branche basée avant).
  - **Décision : aucun item grisé.** Les 6 items naviguent ; `/solo`, `/matchmaking`, `/history` sont des **pages vierges** créées ici. ⚠️ `MenuItem` garde `muted`/`disabled` **sans appelant** — ce sont l'état `.off` de la maquette, un futur ticket les redemandera. La prop `params`, elle, a été **supprimée** (morte ET non sûre : le wrapper cassait l'appariement `to`/`params`).
  - ⚠️ **`AuthNav` n'est PAS un composant du rail** : `LandingNav` le monte aussi, sur la **landing publique**. Piège vécu — le `<ConfirmDialog>` de déconnexion était rendu sans garde, donc son `<h2>` « Log out » était le **premier titre du DOM** d'un visiteur anonyme, avant le `<h1>`. Il est désormais sous `{isLogged && …}`, et `LandingNav` est passé de `<nav>` à **`<aside>`** (il *contient* une navigation, il n'en est pas une). **Toute modif d'`AuthNav` doit être vérifiée sur `/` autant que sur `/home`.**
  - ⚠️ **Lien d'évitement : jamais `sr-only` + `focus:not-sr-only`** — cette paire compile un `padding:0` de spécificité (0,2,0) qui **bat** `px-4`/`py-2` en (0,1,0) : le lien se rendait en 149 × 22 px, **sous les 24 px** de WCAG 2.5.8. Motif retenu : **translation** (`-translate-y-24` / `focus:translate-y-0`), sans conflit.
  - ⚠️ **`L7` de `ladder-detail` est scopé à `<main>` depuis ce ticket.** Il compte les mots `queue|matchmaking|auto-match` pour garder la décision produit challenge/accept — mais il balayait le **document entier**, et le rail persistant porte un item `Matchmaking` (le nom interne du cycle, `openapi.yaml` tague déjà `POST /matches` ainsi) sur **toutes** les pages authentifiées. Non scopé, il rendait rouge une page dont la copie est irréprochable. **Même piège pour tout check qui balaie la page : le rail est là aussi maintenant.**
  - 🔑 **Le scénario `f-nav.mjs` (18 checks) MESURE les 13 dimensions de la maquette** (`N4d`) : l'échelle dynamique de Tailwind v4 accepte n'importe quel nombre, donc `w-66` mal tapé en `w-64` passe lint **et** build en silence. Un critère visuel non mesuré n'est pas gardé.
- **Composants** : stratégie tranchée le 27/07 — un composant **sans connaissance du domaine** part tout de suite dans `components/ui/` ; tout le reste est extrait au **second usage réel**, par le ticket du second consommateur (détail et inventaire dans `docs/frontend.md`). **Chaque brief de `coder-front` doit porter la consigne de réutilisation** (interdiction de recopier les classes Tailwind d'un composant existant).
- **Front (reste)** : `/profile` ; liaison de compte (`LinkAccountBanner`) ; pages match + notifications ; rail social (voir mémoire F-Social). *(Les puces file upload et le câblage de la recherche sur `GET /search` sont faits — FT-2B ; le composant vit maintenant dans `components/search/SearchBar.tsx`, qui a **absorbé `UserSearch`** en F-Nav et sert le rail **et** `TeamInvitePlayer`.)* **Deux dettes d'accessibilité mesurées à ticketer** : `text-text-muted` sur carte = 4,23:1 (sous AA, 45 usages dans 25 fichiers → ticket design system) et l'historique de matchs qui laisse 262 px hors champ à 375 px (rendu en cartes sous `sm`).
- **Cartes à ouvrir après F-Nav** (aucune n'est bloquante, toutes sont argumentées) : ① **`server.ts:119` déclare `max: 100` en LITTÉRAL** au lieu de `rlMax(100)` → `RATE_LIMIT_FACTOR` ne s'applique pas au limiteur global, et `GET /ladders` étant **anonyme** (clé `ip:`, pas de `trustProxy`), tout le trafic navigateur partage **un seul seau de 100/min** — c'est la source des 429 en campagne (invariant #13). ② **`/home` est un stub** (`<h3>Page home!</h3>`, sans `<h1>`) **moins fini** que les pages vierges créées par F-Nav, et le rail le met à un clic depuis partout. (`/games` était dans le même cas : **fait par [F-GAMES]** le 30/07.) ③ **Rail en `hidden lg:flex`** : sous 1024 px, ni navigation ni moyen de se déconnecter. ④ **`landing-public.mjs` L4 est une tautologie** (`step('L4', true, …)`) qui visait le sélecteur de langue, supprimé par F-Nav : check mort.
- **Back** : vérification des comptes externes (OAuth Steam/Riot → `verified=true`), présence chat vers Redis (optionnel).
- 🚨 **UNE branche dort HORS de master** (au 31/07) :
  1. **`feature/fs0-social-foundation`** (commit unique `3cb16ac`) — **[FS-0] rail social, codée par WALID**, carte https://trello.com/c/9Dz6Y3LN **en Review**. **Relue DEUX FOIS** (30/07 puis 31/07), pas encore mergée : la balle est chez Walid — rebase sur master + campagne complète verte. Charger la mémoire `project_f_social_decomposition` (le découpage en 7 cartes FS-0→5, le protocole WS à 6 événements, la DoD transverse) avant d'y toucher. ⚠️ Review en local : diff contre **`git merge-base`**, jamais contre master (sa base est ancienne), et **jamais sa propre branche**.
  - ❌ **[B10] `playerCount` par jeu est ABANDONNÉ** (décision de David, 31/07). Le code était fini et vert sur `feature/b10-player-count` (`80c675b`), mais il ne servait qu'à **enrichir la landing**, qui n'est plus un besoin. **Ne pas le relancer, ne pas le recoder, ne pas s'étonner que `GET /games` ne rende pas `playerCount`.** Branche et carte à supprimer.
- **Hygiène du board (30/07)** : `[FT-4B]` était restée en In Progress **après son merge du 29/07** — corrigée, passée en Done. ⚠️ **Vérifier la colonne In Progress après chaque merge** : une carte oubliée là fait croire qu'un travail est en cours. Reste douteuse : **`[F-Home]` (card 48, In Progress)** alors que `/home` est toujours le stub `<h3>Page home!</h3>` — aucune branche, aucun commit, à redescendre en Todo. ⚠️ **Ne pas confondre `[FT-4B]`** (saisie du score, **mergée**) **et `[F4B]`** (liaison de compte + suppression de compte, **Todo**, https://trello.com/c/WBWaXu34) : les deux noms se ressemblent et la confusion a déjà eu lieu.

---

## 🔑 Invariants à ne jamais casser

1. **Cookie refresh** : back pose `Path=/auth`, le proxy Vite le réécrit en `Path=/api/auth`, le front tape `/api/auth/*`. Les 3 forment un tout — en casser un casse **silencieusement** la restauration de session. Options centralisées dans `backend/src/auth/cookies.ts`.
2. **Notifications** : `notify()` INSÈRE **dans la transaction métier**, `pushNotifications()` pousse **après le commit**. Règle produit : on notifie le camp concerné, **jamais l'acteur** ; en 2v2+ seulement les joueurs **alignés** (banc exclu).
3. **Fenêtres de temps** : inégalités **strictes** (`<`, jamais `<=`) — deux matchs qui se touchent (21h–22h / 22h–23h) ne se chevauchent pas, c'est le cas d'usage central (enchaînement dos à dos).
4. **`scheduled_at` est LA référence temporelle** ; `started_at` n'est **lu par aucune règle**.
5. **Params d'URL validés par Zod** avant toute requête Drizzle, **après** `authenticate` (anonyme → 401, jamais 400). Malformé → **400**, bien formé mais absent → **404**.
6. **Jamais de `select()` nu** sur `users` : projection explicite (fuite `email`/`passwordHash`).
7. **`IMAGE_MIME` et `EVIDENCE_MIME` restent séparés** : un avatar est une image, une preuve de dispute peut être un PDF.
8. **Contrat API** : `openapi.yaml` est la source de vérité ; **régénérer `frontend/src/lib/api-types.gen.ts`** après toute modif. Ne **jamais** importer les types du backend côté front (ils décrivent la DB). ⚠️ `scheduledAt` arrive en **string ISO**, pas en `Date`.
9. **Type-check front** : `npx tsc --noEmit` est un **faux vert** (fichier solution + `references`). Utiliser **`npx tsc -b --noEmit`** ou `npm run build`.
10. **`npm run audit` sort 0 sans filtre** depuis FT-2A : une entrée console nouvelle est donc **imputable au ticket en cours**, plus noyée dans le bruit. Une erreur réseau *provoquée volontairement* par un scénario se déclare avec `expectHttp(motif, raison)` — cloisonnée à sa phase, flux réseau uniquement. ⚠️ Un `exit 2` = harnais en échec, **jamais** « console propre ». ⚠️ **NE JAMAIS ÉCRIRE UN FICHIER SOUS `frontend/` PENDANT UNE CAMPAGNE** (même un `.md`, même hors du graphe de modules) : Vite déclenche un **full reload**, la SPA rebootstrape (`/auth/refresh` + `/users/me` + toutes les queries de la page) et retombe sur l'onglet par défaut. Vécu le 28/07 : `teams-manage` a pris un `B14b` rouge à 6 requêtes **puis** un `locator.focus` expiré à 30 s (le bouton « Kick » n'existe que dans l'onglet Manage) → **exit 2**. Le même scénario seul : 35/35 en 23,7 s. Le rapport accuse le **ticket**, jamais l'éditeur — d'où le coût de diagnostic. Éditer la doc **après** le run, ou lancer un scénario filtré.

11. **Lire une région live : TOUJOURS `awaitAnnouncement(texte)`** (helper du `runner.mjs`, exposé à chaque scénario). ⚠️ La règle « attendre le texte, pas la présence » était déjà écrite ici et a **quand même produit 2 faux rouges** (`ft1c` 4.1b, `teams-manage` B13c-bis) — parce qu'une règle en prose ne s'applique pas toute seule. Elle est donc désormais **outillée** : `page.locator('[role=status]').waitFor()` rend la main **immédiatement** (la région est montée en permanence) et on lit soit du vide, soit **l'annonce précédente**. ⚠️ **`focusLanding()` est un INSTANTANÉ synchrone** — il n'attend ni le focus ni l'annonce : appeler `awaitAnnouncement()` **avant** lui (attendre un texte ne déplace pas le focus, donc ne fausse pas la mesure). ⚠️ **Leçon de méthode** : ces 2 courses étaient **latentes depuis toujours** et se gagnaient uniquement parce que la campagne dormait entre scénarios (attentes de quota) ; `RATE_LIMIT_FACTOR` a supprimé ce sommeil et les a fait tomber d'un coup. **Un rouge qui apparaît en campagne mais reste vert en isolation est une course du harnais, pas une régression du ticket** — chercher un `waitFor` qui n'attend rien avant d'accuser le code applicatif.
12. **`routeTree.gen.ts` et `api-types.gen.ts` sont générés, jamais édités à la main** — mais leur statut git diffère : `routeTree.gen.ts` est **gitignoré** (régénéré au build) ; `api-types.gen.ts` est **tracké** et doit être **committé à chaque régénération** (invariant #8). ⚠️ Erreur vécue : cette ligne affirmait à tort les deux gitignorés, ce qui a fait sauter la régénération sur B14 — vérifier avec `git ls-files`/`git check-ignore` avant de faire confiance à un souvenir sur le statut d'un fichier généré.

13. **`RATE_LIMIT_FACTOR` multiplie TOUS les quotas de l'API** (défaut 1, commit `9f5cdec`). Les quotas sont calibrés pour un humain, pas pour un harnais : l'audit console et les e2e Python passaient ~10 min par campagne à **attendre** la reconstitution des fenêtres (`awaitRegisterSlot` 3/min, `awaitGlobalQuota` 100/min), plus ~60 s à chaque relance filtrée. Toute route qui déclare un quota **doit** passer par `rlMax()` (`backend/src/utils/rate-limit.ts`) — un `max:` littéral oublié rend le harnais bloquant sur cette seule route, symptôme pénible à relier à sa cause. ⚠️ **Ce n'est PAS un interrupteur off** : plugin, hooks, clés de comptage et montage à deux étages de `/2fa/verify` inchangés, seul le nombre bouge (à 1000, `/auth/register` 429 toujours, au 3001ᵉ appel) — la mécanique reste démontrable en soutenance. ⚠️ **DOIT valoir 1 à la livraison** : `.env.example` est à 1, compose retombe sur 1 si la variable est absente, et le backend écrit un WARN à chaque démarrage tant que ce n'est pas le cas (`console.warn`, **pas** `server.log.warn` — le serveur est instancié sans `logger`, `server.log` est muet). ⚠️ Le `.env` local de David est à **1000** ; changer la valeur exige `docker compose up -d backend` (recréation), pas un simple `restart`. ⚠️ **Effet de bord à surveiller** : supprimer les pauses de quota supprime ~60 s de repos entre scénarios d'audit — suspect n°1 devant un check rouge **en campagne mais vert en isolation**. ⚠️ **Vérifié le 28/07** : ce n'était pas une régression mais **2 courses latentes du harnais** (`ft1c` 4.1b, `teams-manage` B13c-bis), corrigées par `awaitAnnouncement` — voir invariant #11.

📄 Les 21 pièges rencontrés, version longue et expliquée → **`docs/pieges.md`** (TOCTOU/verrous, ordre des verrous, tests de course **sans barrière ET sans balayage de décalage**, écritures par **cascade** hors inventaire, slots périmés, `.env`, WSL2, Drizzle…)

---

## 📋 Conventions

**Code** — TS strict partout, ESM, Node 24 (`nvm use`, `.nvmrc`). Imports nommés > default. Validation **Zod** systématique côté API. Front : tokens depuis `frontend/src/index.css` (aucune couleur/police/radius/shadow en dur dans les pages), composants de `components/ui`, imports `@/...`, icônes `lucide-react`. Lancer `npm run build` **et** `npm run lint` avant review.

**Tests** — côté **front**, la console sans warning est un motif de rejet : chaque ticket front a son scénario dans `frontend/tests/console-audit/scenarios/`, et `npm run audit` doit sortir **0** avant la review (⚠️ un code 2 = harnais en échec, pas un vert). Côté **back**, le scratchpad (`/tmp/claude-*/`) est réservé au vrai jetable : one-liner de debug, inspection ponctuelle. Dès qu'un script est relancé une 2ᵉ fois ou valide un comportement durable de l'API, il est **promu** dans `backend/tests/` : nom `test_<domaine>.py`, réutilisation de `helpers.py` (ne jamais réécrire login / création d'user / gestion de token), enregistrement dans `run_all.py`, ligne ajoutée dans le README. ⚠️ **Lire `backend/tests/README.md` avant d'écrire un test** : les suites existantes couvrent déjà largement l'API — le README liste ce que couvre chacune. Si le comportement y est, on l'exécute, on ne le recrée pas.

**Git** — dépôt de travail = **Git vogsphere 42** (pas de PR : review en local via `git diff master..<branche>`, jamais sa propre branche). Branches `feature/<code-ticket>-<sujet>` / `fix/<sujet>` en kebab-case. Commits **Conventional Commits**, atomiques, un ticket = un commit (squash). Pas de force push sur `master`. Identité git = identité 42. ⚠️ **Pas de trailer `Co-Authored-By: Claude`.**

**Docker** — env uniquement dans `.env` (`${VAR}` dans compose), bind mounts sous `./data/`, polling pour le hot reload (WSL2), images avec registre complet + version figée (jamais `latest`), `npm ci`.

**Sécurité** — `.env` jamais commité (`.env.example` versionné, obligatoire), HTTPS partout, validation front **et** back, Fastify écoute sur `0.0.0.0`.

---

## 🗂️ Organisation (Trello)

Board 4 colonnes **Todo / In Progress / Review / Done**. Une carte = ~1-3 j = une branche = une review. Review = diff relu par un coéquipier → merge sur `master` → carte en Done. Gabarit : titre (verbe + objet), description, Definition of Done, assigné, label (`backend`/`frontend`/`infra`/`docs`). **Planif le dimanche**, tickets de la semaine uniquement (backlog just-in-time).

---

## 💬 Style d'interaction

Équipe venue du **C**, débutante en TS/Node/Docker → explications claires, on veut le **pourquoi**, questions de clarification bienvenues.

⚠️ **Préférence par défaut (brahim, créateur du repo)** : **ne pas générer le code à sa place** — décrire les concepts + donner les commandes shell, il code lui-même.
⚠️ **David surcharge cette règle** : il veut le **code directement** quand il le demande ou bloque sur une syntaxe TS/JS. En cas de doute sur qui tu assistes, demander.

---

## 🔗 Commandes utiles

```bash
cd ~/ft_transcendence
nvm use                                 # Node 24 (.nvmrc)
docker compose up -d --build            # build + démarrage complet
docker compose ps / logs -f <service>   # état / logs
docker compose exec <service> sh        # entrer dans un conteneur
docker compose down                     # arrêter (garde les données)
docker compose down -v                  # supprime cert/node_modules ; garde ./data
cd backend && npm test                  # Vitest (helpers purs)
cd backend/tests && python3 run_all.py  # e2e (vraie base de dev)
```

**UIs locales** : Front **5173** (seule origine navigateur), Adminer 8080, redis-commander 8081, MinIO console 9001, backend direct 3000 (diagnostic).

---

## 📚 Index de la doc (lire à la demande)

| Fichier                   | Contenu                                                                                                      |
| ------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `docs/schema.md`          | Design complet du domaine jeu (§5.1→§5.4, tables, state machine)                                             |
| `docs/backend.md`         | Détail par domaine : auth, social, teams, matchmaking, disputes, notifications, search, jobs + reste à faire |
| `docs/frontend.md`        | Détail par ticket : F0/F0-A/B/C/D, FR1, FR2, F-Nav, FL + règles front et dette                               |
| `docs/infra.md`           | I2/I3/I4 : proxy, certs, env, cookie, OAuth, médias                                                          |
| `docs/modules.md`         | Les 11 modules, exigences PDF, candidats de réserve                                                          |
| `docs/stack.md`           | Versions des libs + arborescence réelle du repo                                                              |
| `docs/pieges.md`          | Les 21 pièges rencontrés, version longue                                                                     |
| `docs/journal.md`         | Historique daté des merges et décisions                                                                      |
| `backend/tests/README.md` | Les suites e2e Python : ce que couvre chacune, helpers disponibles                                           |
| `frontend/tests/console-audit/README.md` | Audit console Chrome automatisé : lancer, écrire le scénario d'un ticket, la dette connue |

_Refacto du 25 juillet 2026 : CLAUDE.md est passé de 116 Ko à ~9 Ko ; rien n'a été perdu, tout le détail est dans `docs/`._
